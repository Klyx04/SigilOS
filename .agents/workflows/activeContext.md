# Active Context — SigilOS
## ✅ Session 19/09/2026 (suite 6) — **`migrate deploy` figé : VRAIE cause trouvée (schema-engine Prisma 7 en boucle CPU) + migration réécrite** → branche `dev`
> **Demande user (verbatim)** : « jamais attendu aussi longtemps » (capture de `./scripts/deploy-cd.sh beta 5439f21` arrêté sur `Application des migrations Prisma...`), puis `debug.md` : 1ᵉʳ run interrompu au Ctrl+C, 2ᵉ run qui meurt en `P1002 Timed out trying to acquire a postgres advisory lock (72707369)`.
> **Vraie cause (reproduite et mesurée)** : le **schema-engine de Prisma 7 part en boucle CPU à 100 %** sur `prisma/migrations/20260919130000_add_dungeon_slug/migration.sql` quand les instructions sont **écrites sur plusieurs lignes**. Effet : **aucune** instruction exécutée (la colonne `Dungeon.slug` n'existait même pas) alors que l'enregistrement `_prisma_migrations` était déjà inséré → verrou advisory conservé → `migrate deploy` paraît figé à l'infini, et toute tentative suivante échoue en `P1002` après 10 s. Le CLI retéléchargé par `npx` (1-3 min, sortie redirigée dans un `mktemp`) explique seulement l'absence d'affichage, pas la panne.
> **Preuves (banc local : Docker + Postgres 15 jetable + CLI Prisma 7.10.0)** : contrôle = fichier committé ⇒ schema-engine à **98 % CPU**, migration jamais terminée ; bisection : `ALTER` seul ✅, `+UPDATE` ✅, `+CTE` ✅, `+SET NOT NULL` ✅, **le tout en « une instruction par ligne » ✅** (`nullable=NO`, index unique créé) ⇒ E/F/G/H (multi-lignes) ❌ vs A/B/C/D ✅. Slugs vérifiés sur de vraies données : `Comte Harebourg → comte-harebourg`, `Île des Wabbits → ile-des-wabbits`, `Ægir → aegir`, `Œuf de Pâques → oeuf-de-paques`, `  Minôtorôr--X  → minotoror-x` ✅.
> **Corrigé** : `migration.sql` réécrit (**une instruction par ligne**, commentaires ASCII, `translate()` au lieu de 27 `replace()` imbriqués — mêmes résultats, toujours aligné avec `src/lib/boss-slug.ts`) + **garde de non-régression** dans `tests/unit/boss-slug.test.ts` (toute ligne de code doit se terminer par `;`).
> **Confort de déploiement (2ᵉ sujet, indépendant)** : `Dockerfile` → stage **`prisma-cli`** copié dans `/opt/prisma-cli` (plus de retéléchargement npx à chaque deploy) ; `scripts/deploy-cd.sh` → helper **`compose_exec_streamed`** (sortie en direct, `exec -T` + `</dev/null`) + **`PRISMA_TIMEOUT`** (900 s → code `124` + message au lieu d'une attente infinie) + **repli npx automatique** ; `PRISMA_PIN=7.10.0` aligné sur `package.json`/`Dockerfile`.
> **Déblocage beta** (documenté dans `MAINTENANCE.md`) : `restart app-beta` → `pg_terminate_backend` → `migrate resolve --rolled-back 20260919130000_add_dungeon_slug` → `git pull` → `./scripts/deploy-cd.sh beta`. Base **locale** : `DELETE FROM "_prisma_migrations" WHERE migration_name='20260919130000_add_dungeon_slug';` puis `migrate deploy` (SQL idempotent) — le checksum du fichier a changé.

## ✅ Session 19/09/2026 (suite 5) — **Deslop de la modale de raid : vrais pictos Dofus, vrai visuel de raid, rouge = danger** → branche `dev`
> **Demande user (verbatim)** : « j'ai besoin d'un deslop IA aussi la modale d'un raid, vrai icone etc les couleurs rouge les icones IA... » (capture de la modale « RAID : GIGALODON »).
> **Constats retirés** (tous dans `src/components/calendar/event-detail-modal.tsx`) : dégradés `bg-gradient-to-br … opacity-40` (dont un `from-danger to-danger` — un dégradé d'une couleur vers elle-même), tuile « icône d'appli » `rounded-2xl shadow-inner backdrop-blur-sm` avec un `Swords` lucide, barres de progression `bg-gradient-to-r from-success to-success` / `from-danger to-warning`, emojis dans l'UI (`✅ RAID GO`, `⏳ n manquant(s)`, `↩`), palette arc-en-ciel sur les icônes (`text-warning` / `text-info` / `text-pink-400`), titre et noms en `italic uppercase`, `ring-1 ring-` (classe cassée) et `bg-surface/98 backdrop-blur-xl` sur le `DialogContent`, badge de statut en dur (`bg-green-600`), « +n WAITING » en anglais, insert Kralamoure avec `rotate-12 shadow-2xl backdrop-blur-md`.
> **Fait** : le visuel de l'en-tête est le **vrai** visuel de l'événement (`resolveEventImagePath` — source unique déjà partagée avec l'embed Discord et les cartes `event-card`) : illustration **3:4 affichée entière** pour un raid (765×1024, jamais rognée) ; tous les pictos de la modale viennent de **`DofusUiIcon`** (`calendar`, `date`, `player`, `leader`, `dungeon`, `world`, `guild`, `success`, `hourglass`, `trophy` — « une notion = un picto ») ; le **rouge (`danger`)** porte l'identité du raid (aplat d'en-tête, panneau « Détails du raid », bouton « Clôturer le Raid » plein, barre de quota tant que le minimum n'est pas atteint, modale de clôture) ; `TYPE_CONFIG` n'expose plus `icon`/`gradient` (le « gradient » local disparaît), `STATUS_CONFIG` passe aux tokens (`bg-success`), « Raid go »/« n manquant(s) » en texte + pictos, « n en attente » en français, `Undo2` pour annuler la clôture, en-tête **plat** (ni dégradé, ni blur, ni ombre, ni `italic`) — idem pour la **modale de clôture** (`ring-1 ring-` cassé retiré, `from-danger to-danger` du bouton de validation remplacé par un aplat `bg-danger`) et le **roster** (titre/dates/commentaires sans `italic`, « +n WAITING » → « n en attente »).
> **Preuves** : `tsc` **0** · `eslint` **0 erreur** (5 imports lucide devenus inutiles supprimés) · **160 fichiers / 1711 tests** ✓ (les 2 gardes de source qui visent cette modale — `calendar-krala-metamob-ui`, `dungeon-finder-filters-ui` — restent vertes).
> **Restes possibles (non demandés)** : même passe sur la carte/liste du calendrier (`event-card.tsx`, `calendar-grid.tsx` gardent leurs `TYPE_THEMES.icon` lucide + gradients) et sur `event-form.tsx`.


## ✅ Session 19/09/2026 (suite 4) — **Rappel automatique des raids : ping des INSCRITS 1 h avant (jamais les rôles de l'embed)** → branche `dev`
> **Demande user (verbatim)** : « Rajoute un ping qui va ping uniquement les membres inscrit (et pas les rôles mentionné dans l'embed) 1h avant un raid , un ping automatique de rappel »
> **Livré** : nouveau cron **`/api/cron/raid-reminders`** (protégé `x-cron-secret`, télémétrie God `raid_reminders`, toutes les 10 min) → `src/server/raid-reminder-service.ts` (`sendRaidReminders` passe globale, `sendRaidReminderForEvent` à la demande) ; **décision pure** dans `src/lib/raid-reminder.ts` (`shouldSendRaidReminder`, `resolveRaidReminderLeadMinutes`, `buildRaidReminderMentions`, `pickRaidReminderChannelId`, `raidReminderLeadLabel`).
> **Garantie « inscrits uniquement »** : Discord ne ping **que** depuis le `content` du message, et `sendChannelMessage` ne construit `allowed_mentions` qu'à partir de ce contenu ⇒ le rappel n'écrit QUE des `<@id>` d'inscrits (REGISTERED + CONFIRMED ; snowflakes validés via `@/lib/discord-ids`, dédupliqués, ≤ 80 mentions / ≤ 1800 car.) : **aucun** `<@&rôle>` (ceux de `metadata.mentionRoleIds` restent sur l'embed de publication), **aucun** `@everyone`, et la file d'attente n'est pas notifiée — verrouillé par test de source.
> **Idempotence & anti-doublon** : un seul ping par raid (`metadata.raidReminderSentAt`, écrit après l'envoi), silence **30 min** après un rappel MANUEL (`lastRemindedAt`), fenêtre = `GuildEvent.notifyBefore` (**60 min par défaut**, borné 5 min → 24 h), jamais une fois le raid commencé, aucun ping sans inscrit. Le message de rappel est tracé dans `metadata.reminderMessages` ⇒ supprimé par les clôtures existantes (annuler / terminer / auto-close).
> **Ops** : `MAINTENANCE.md` (ligne crontab `0,10,20,30,40,50 * * * *` + rappel du piège `\%`) · `KNOWN_CRON_TASKS.raid_reminders` (panneau God) · doc `donjons-et-quetes` **§6** (l'ancienne §6 devient §7).
> **Preuves** : `tsc` **0** · `eslint` **0 erreur** · **160 fichiers / 1711 tests** (dont **23 neufs** : `tests/unit/raid-reminder.test.ts` 15 · `raid-reminder-wiring.test.ts` 8).
> **Restes possibles (non demandés)** : pinguer aussi la file d'attente (option), réglage de guilde (activer/désactiver le rappel auto + délai par défaut), notification in-app des inscrits — à faire si besoin.


## ✅ Session 19/09/2026 (suite 3) — **Raids du calendrier : embed Discord figé (« les gens s'inscrivent mais personne ne s'affiche ») + inscription/classe en une seule fois** → branche `dev`
> **Demande user (verbatim)** : « les raid bug encore : qqun a créé un raid depuis le calendrier · les gens s'inscrivent mais personne ne s'affiche en temps réel dans l'embed · je ne comprends pk les boutons s'inscrire et choix classe sont pas liés ? genre si je clique sur m'inscrire ca affiche juste le pseudo sans classe et si dans un second temps je clique sur choisi ma classe ca ajoute le nom de ma classe à mon pseudo dans l'embed / dashboard ? »
> **① Cause mesurée (embed figé)** : la bêta a `DISCORD_OUTBOX_ENABLED=true` ⇒ `sendChannelMessage` ne renvoie pas un ID Discord mais **`outbox:<jobId>`** (écriture **en file**). `publishDiscordEvent` le stockait tel quel dans `GuildEvent.discordMessageId` (aucune clé `storeMessageIdKey`, aucune validation snowflake) ⇒ chaque inscription PATCHait `PATCH /channels/{salon}/messages/outbox:…` = **404 définitif** : compteur **1/12** et roster figés à l'instant de la publication pendant que 10 joueurs étaient bien en base, et le cliqueur lisait « ⚠️ **L'embed Discord n'a pas pu être rafraîchi** — le dashboard fait foi » (message exact de la capture). Même piège déjà corrigé ailleurs : status Discord (`isDiscordSnowflake`, 17/09) et Marché (`resolveMarketMessageId`, 18/09) — le calendrier était le dernier module non protégé.
> **② Correctif** : nouveau module **pur** `src/lib/discord-ids.ts` (source unique : `DISCORD_SNOWFLAKE_RE`, `isDiscordSnowflake`, `isOutboxMessageId`, `discordIdKind`) réutilisé par `status-ping-core` (ré-export, test existant intact), `discord-outbox` et `market/discord` (les 3 copies du regex disparaissent) · `calendar-service.ts` : `publishDiscordEvent` passe **`storeMessageIdKey`** (`calendar:msg:<eventId>`, TTL 30 j) pour que le worker ré-ancre le VRAI ID dans Redis, journalise un ID différé, et `refreshDiscordEventEmbed` **résout** l'ID (`resolveEventMessageId`) avant tout PATCH — un snowflake passe tel quel, un `outbox:` est lu dans Redis, tout le reste rend `null` (on n'écrit **jamais** dans le vide) + log `warn` diagnostic. Aucune migration : les embeds **déjà publiés** (ID `outbox:` jamais ré-ancre) sont **réparés au premier clic suivant** — `recoverEventMessageId` relit le salon, reconnaît le message à ses boutons (`calendar:join:<eventId>`, module pur `@/lib/discord-components`) et **répare le lien en base**, avec **une seule tentative par événement et par process** (jamais d'appel Discord en boucle ; repli = republier depuis le dashboard).
> **③ Inscription + classe « liées » (2ᵈ constat)** : « S'inscrire » **ouvre désormais une modale** (`calendar:apply:<eventId>`, traitée en `type 5`) — classe Dofus **facultative** + message, enregistrés dans **la même écriture** que l'inscription (avant : participant « Sans classe » puis select qui n'ajoutait la classe qu'après coup). RBAC = carte partagée `DISCORD_PERM_MAP` (`isDiscordPrefixAuthorized`), classe normalisée par `matchDispatchClass` (accents/casse), borne dure **30 car.** sur la classe stockée (elle part dans un field d'embed), et le message éphémère **confirme la classe retenue** (« ✅ Bien inscrit ! — 10/12 places. 🧩 Classe : **Cra** »). Le menu select reste (s'inscrire avec une classe en 1 clic / corriger la sienne sans annuler).
> **Preuves** : `tsc` **0** · `eslint` **0 erreur** (warnings `any` préexistants) · suite complète verte (**16 tests neufs** : `tests/unit/discord-ids.test.ts` 5 · `tests/unit/discord-components.test.ts` 4 · `tests/unit/calendar-embed-sync.test.ts` 7 — verrous de source : ré-ancre outbox, résolution avant PATCH, réparation du lien, plus de `PATCH …event.discordMessageId`, plus de regex snowflake locale) · doc `donjons-et-quetes` **§5** réécrite (bouton → modale) + 2 pièges ajoutés à `.agents/workflows/discord-module.md`.
> **Reste ouvert (hors périmètre du correctif)** : `sendCalendarDiscordNotification` (« Rappel Discord ») poste un **second** embed pour le même événement sans enregistrer son ID ⇒ cet embed-là ne suivra jamais les inscriptions (l'embed de référence, celui de « Publier », est bien rafraîchi) : à aligner si on veut un seul message vivant par événement.


## ✅ Session 19/09/2026 (suite 2) — **URL des fiches boss en slug (`/boss/nom-du-boss`), anciens CUID en 308, « Page avec redirection » de `/roadmap` corrigée** → branche `dev`
> **Demande user (verbatim)** : « pk on renvoie une chaine aussi laide au lieu du nom du boss (anglais/francais), idem en interne dans le module fiche boss · google console commence à explorer et indexer ça, si on change les nom ca impacte l'index ? · je comprends pas ca non plus : Page avec redirection, ces pages ne sont pas indexées ni diffusées sur Google » · puis « pk ya plus rien ? » (catalogue `/boss` vide).
> **① URL en slug** : la route publique était `src/app/boss/[dungeonId]` avec `Dungeon.id` (CUID) — sitemap (`/boss/${d.id}`), canonical et tous les liens internes (catalogue, avis, overlay, Rush, `?dungeon=<id>`) portaient l'identifiant. Ajouts : colonne **`Dungeon.slug String @unique`** + migration **SQL écrite à la main** `prisma/migrations/20260919130000_add_dungeon_slug/` (slug = `bossName` slugifié — accents français dépliés, non-alphanumérique → `-` — puis suffixe **ordinal déterministe** `-2`, `-3`… pour les homonymes ; `NOT NULL` + index unique dans la même migration ⇒ aucun intervalle sans slug). **Validé sur les 134 donjons de la base** : 0 slug nul, 0 doublon, 0 collision avec `Titan.slug`/`Bounty.slug` (`minotoror` / `minotoror-2`, `comte-harebourg` / `comte-harebourg-2`). Route : résolution **slug OU id** (`Dungeon` → `Titan.slug` → `Bounty.slug`) + **`permanentRedirect()` (308)** vers l'URL canonique ⇒ les liens Discord déjà publiés continuent de fonctionner. Sitemap (134 URL `/boss/<slug>`), canonical, JSON-LD, catalogue public, `SuccesAvisTab`, `BossOverlayClient`, `RushOverlayDungeonCard` et le deep-link interne (`?dungeon=<slug>`, accepté en id **ou** slug) alignés. Nouveau `src/lib/boss-slug.ts` (pur) + `src/server/game/dungeon-slug.ts` (`resolveUniqueDungeonSlug`, utilisé par **les 4** créations de donjon : God `createDungeon`/import, `createDungeonAction`, siphon d'anomalie) + champ `slug` optionnel dans `DungeonFormSchema` (à la modification, le slug est **conservé** : une URL publiée ne bouge pas toute seule).
> **② « Page avec redirection » = `/roadmap`** : la page faisait `redirect("/")` dès que `!hasAccess`, avec `hasAccess = isAdmin || (roadmapEnabled && isInAnyGuild)` — **Googlebot n'a ni session ni guilde** ⇒ redirection systématique, alors que `robots.ts` autorise `/roadmap` **et** que le sitemap l'annonce (`roadmapEnabled` est même `@default(false)`). Corrigé : règle pure `canViewRoadmap({ isAdmin, roadmapEnabled })` (`src/lib/roadmap-access.ts`) ⇒ la page est **publique dès que le toggle God est actif**, et le sitemap ne l'annonce **que** dans ce cas (métadonnées localisées au passage).
> **③ Réponse SEO (à retenir)** : les 2 URL `/boss/...` étaient « **Explorée, actuellement non indexée** » ⇒ **aucun coût** à changer d'URL maintenant (c'était le bon moment). Une fois indexée, un changement d'URL = **301/308 permanents** + canonical/sitemap/liens internes alignés, redirection conservée « pour toujours » ; jamais de 302/307, ni de doublon laissé en 200. Un slug **unique** par entité (FR) évite le doublon d'URL entre langues (le nom affiché, lui, reste localisé FR/EN).
> **④ Incident « ya plus rien » (catalogue `/boss` vide)** : **pas un bug de logique** — le serveur `next dev` tournait depuis avant le `prisma generate` et gardait **l'ancien client Prisma** en mémoire (`Unknown field 'slug' for select statement on model 'Dungeon'` dans `.next/dev/logs/next-development.log`) ⇒ `getBestiaireCatalog` renvoyait `success:false`. **Après redémarrage du serveur** : catalogue 200, liens en slug, `/boss/tournesol-affame` 200, `/boss/<cuid>` **308 → slug**, sitemap 134 URL `/boss/<slug>` **sans identifiant**. ⚠️ **Règle** : après un `prisma generate` (nouvelle colonne/champ), **redémarrer `next dev`**. Et à chaque déploiement, la migration doit passer **avant** le code (`deploy.sh` fait bien `migrate deploy` puis `db push`) — sinon toute requête qui sélectionne `slug` échoue en `P2022`.
> **Preuves** : `tsc` **0** · `eslint` **0 erreur** sur les fichiers touchés · **155 fichiers / 1669 tests** (dont **10** neufs : `tests/unit/boss-slug.test.ts` — slug des noms réels, unicité/backfill, canonical + 308, sitemap en slug, liens internes, garde `canViewRoadmap` et annonce conditionnelle dans le sitemap) · base locale : 134 slugs distincts + index unique `Dungeon_slug_key` + migration enregistrée (`_prisma_migrations`) · **sondes live** : catalogue, fiche en slug, 308 depuis l'ancien CUID, sitemap.

## ✅ Session 19/09/2026 — **i18n : les 7 guides publics + le journal des mises à jour traduits en anglais (vocabulaire Dofus officiel, zéro invention) + zaap de l'Almanax corrigé** → branche `dev`
> **Demande user (verbatim)** : « maintenant faut traduire les guides https://beta.sigilos.fr/guides · aucune invention de voc etc ni ai slop · base toi sur le vocab réel de dofus, si besoin dofusdb […] est ton ami · idem : https://beta.sigilos.fr/changelog · /almanax : change le nom c'est pas plaine des scarafeuilles mais plaine des porkass le zaap ».
> **Guides (7/7)** : l'index servait déjà l'UI EN mais **les fiches restaient FR** (titres, catégories, corps HTML). Ajout de `src/content/guides/en/*.ts` (un fichier miroir par guide, structure HTML **identique**) + registre localisé : `getAllGuides(locale)`, `getPublishedGuides(locale)`, `getGuideBySlug(slug, locale)`, `getGuideContent(slug, locale)` (l'export FR `publishedGuides` est conservé pour le sitemap et les grilles internes). Métadonnées des pages `/guides` et `/guides/[slug]` passées en `generateMetadata` (les clés `metaTitle`/`metaDesc` existaient mais n'étaient **pas consommées**), fil d'Ariane + `inLanguage` du JSON-LD localisés, dates au format de la locale, 3 clés i18n ajoutées (`allGuidesLink`, `readingTimeSuffix`, `notFoundTitle`) et la catégorie EN corrigée (« Magecraft » → **Smithmagic**).
> **Vocabulaire « zéro invention »** : chaque nom propre a été vérifié sur **DofusDB** (champ `name.en`) — Smithmagic / Smithmagus, crushing / crusher, `<Stat> Rune` / `Pa … Rune` / `Ap Ga Rune` / `Mp Ga Rune`, Retrait PA → **AP Reduction**, Fuite → **Dodge**, Tacle → **Lock**, Puissance → Power, Dragoturkey / Seemyool / Rhineetle, **Manger** (+ Extrait/Philtre/Potion/Élixir), Lightning Thrower / Drinking Trough / Dragobutt / Slapper / Patter, In Love / Hardy / Precocious / Reproductive / Wise / **Chameleon**, Paddock Catalyst, Breeder Village, Koalak Mountain, Guildalogem, **Guildokens**, **Guild Gratitude**, Régulation → **Culling**, Gigalodon's Chasm, Eternal Gardens Sanctuary, Scarlet Queen, Cursed Princess, **Moraympress**, **Crabinator**, **Willorka**, Madreporrible, Shebble, Hate'Raken, Gigagroan, Spinnafin, Belladonna, Salt of the Depths, Thistle Bow Meow, Guilda, Homer Cenary… Les **18 termes non attestés** (puits FM → « well (residual) », SC/SN/EC → CS/NS/CF, jalon → Milestone, salles internes du Sanctuaire, Lame Fleurie, Dents de l'Amer…) sont **documentés et justifiés** dans `src/content/guides/GLOSSARY-EN.md`.
> **Journal des mises à jour** : colonnes `titleEn` / `summaryEn` / `contentEn` ajoutées à `ChangelogEntry` (migration **SQL écrite à la main** `20260919120000_add_changelog_translations`, appliquée en local via `prisma db execute` + `migrate resolve --applied`, **pas** de `migrate dev` vu la dérive connue). **Repli FR** (`localizeChangelogEntry`) branché sur la page publique (locale de la requête), la modale (`checkChangelogVisibility` → `getServerLocale()`) ; le détail non traduit n'affiche **jamais** du vide. God → Changelog gagne un bloc « Traductions anglaises » (Title/Summary/Full content EN, saisie manuelle : aucune traduction automatique) et `scripts/backfill-changelog-en.mjs` remplit de façon **idempotente** (`COALESCE`) les releases déjà publiées (v2.2 → v1.1) sans jamais écraser une saisie God.
> **Almanax** : le zaap du Sanctuaire `[-4,-24]` est **« Plaine des Porkass »** — nom officiel EN **« Lousy Pig Plain »** (sous-zone DofusDB **178**). L'ancien libellé « Zaap Plaine des Scarafeuilles » désignait un **autre** zaap (sous-zone 170, `[-1,24]`) et « Scarafly Plain Zaap » n'existe pas dans le client anglais. Métadonnées de la page passées en `generateMetadata` (clés i18n déjà présentes mais inutilisées).
> **Preuves** : `tsc` **0** · `eslint` **0 erreur** sur les fichiers touchés (10 avertissements `any` préexistants) · **147 fichiers / 1581 tests** (dont **9** neufs : `tests/unit/guides-i18n.test.ts` — parité structurelle FR/EN **balise par balise** sur les 7 fiches, absence de mots-outils français dans l'EN, repli FR du journal, garde du libellé de zaap) · **comparaison directe FR↔EN** des 7 paires de fichiers : `h2/h3/table/thead/tbody/tr/th/td/ul/ol/li/img/p/strong/em/div/span/code/pre/hr/a` → **0 écart** · **rendu live** sur le serveur de dev (`fetch` avec `?lang=en` **et** `?lang=fr`) : `/guides`, les **7 fiches**, `/changelog`, `/almanax` → contenu EN servi, FR **sans régression**.
> **Reste (hors périmètre de la demande)** : les `contentEn` détaillés des releases existantes (saisie God, faute de contenu lisible sans authentification), les libellés du dashboard de guilde (widget changelog, `GuidesGrid`) qui restent FR par défaut, et `docs/ROADMAP.md` / `chantier*.md` non touchés.


## ✅ Session 18/09/2026 — **God → Marché : UI réparée (marges + débordement) et périmètre resserré (rôles notifiables côté guilde, journal déplacé dans les logs)** → branche `dev` (constats beta de l'utilisateur)
> **Cause mesurée de « l'UI est cata »** : le shell God ne pose **aucune** marge horizontale (contrairement au dashboard de guilde, `container … p-4 sm:p-6 lg:p-8`) — chaque page fournit la sienne, patron de référence = `god-dashboard-client` : `w-full max-w-[2500px] mx-auto px-4 lg:px-12 pt-10 pb-40`. `src/app/god/market/page.tsx` n'avait que `space-y-8 py-8` ⇒ titre/paragraphe **collés à la sidebar** et boutons collés au bord droit ; la page reprend le conteneur de l'onglet principal. La grille des rôles (`sm:grid-cols-2 lg:grid-cols-3`, libellés longs + `truncate` sans `min-w-0`) **débordait horizontalement** (3ᵉ colonne coupée par le bord de la fenêtre) : elle disparaît avec le bloc. Les dates de la vue d'ensemble passent au helper **déterministe** du module (`formatMarketDateTime`, BUG-9) : plus de `toLocaleString` (écart d'hydratation SSR/navigateur).
> **Décisions user appliquées** : (1) **« Rôles notifiables » retiré de God → Marché** — réglage **local par nature**, il vit **uniquement** dans *Réglages → Marché* (`PingRolesSelector` + `updateMarketSettings`, déjà en place) : `getGodMarketPingRoles` / `saveGodMarketPingRoles` **supprimées** (avec l'import `fetchGuildRoles`), la carte rôles et sa liste de cases disparaissent de l'écran de supervision ; (2) **« Journal du Marché » déplacé vers God → Audit Logs → onglet « Marché »** — nouvelle lecture **légère** `listGodMarketGuilds()` (id **interne** + nom, `take` 200) pour le filtre, `getGodMarketOverview` **ne lit plus** `MarketAuditLog` (clé `logs` retirée du contrat), nouvel écran `src/app/god/logs/market-log-view.tsx` (compteur, filtre par guilde interne, dates **UTC**, aucune donnée personnelle), onglet branché dans `logs-tabs.tsx` + `logs/page.tsx`.
> **Preuves** : `tsc` **0** (les 2 seuls échecs restants sont **préexistants** et hors périmètre : `PublicRushGuideClient.saveCharacterForm`, `src/lib/i18n/locales/en.ts`) · **146/146 fichiers / 1537 tests** (dont **8** neufs : `tests/unit/god-market-logs-tab.test.ts` — gardes de source « une seule maison » pour les rôles, onglet Marché présent, marges de page, plus de `toLocaleString`) · `eslint` **0 erreur** sur les fichiers touchés · `tests/unit/action-contracts.test.ts` embarque désormais `market-log-view.tsx` dans ses panneaux surveillés · `npm run seed:docs` = **30 fiches mises à jour** (doc `admin-marche` : journal dans Audit Logs, rôles pinguables = réglage de guilde, tableau « quand un changement de seuil s'applique »).
> **Question utilisateur — « les seuils ou le cron, qui fait foi ? »** : les réglages sont écrits **immédiatement** pour **toutes** les guildes (`GuildConfig.updateMany`, un seul passage) et chaque consommateur les relit **au moment d'agir** (publication → `expiresAt`, réservation/offre → échéance calculée à la création, rappels → paliers relus à **chaque passe** du cron de 10 min) ⇒ **le cron ne fait jamais foi**, il exécute les échéances **stockées**. Ce qui n'est **pas** rétroactif : les échéances déjà posées (`expiresAt`) — une annonce en ligne garde la sienne ; les **paliers de rappel** et le **plafond d'annonces actives** (contrôlé à chaque écriture) s'appliquent, eux, aussi à l'existant. Documenté dans la fiche `admin-marche`.

## ✅ Session 17/09/2026 (suite) — **Guide Sylvestre : pagination, progression par personnage, sommaire replié + cohérence logs/bans + sondages** → branche `feat/refonte-landing-anti-slop` (**PR vers `dev` en fin de session**)
> Même chantier que le bloc ci-dessous (fiche `refonte_landing/REPRISE-LANDING-ANTI-SLOP.md`, **§12.6 → §12.9** = cette session). Deux lots : **ceux de l'agent** (guide + encodage) et **ceux de l'utilisateur** (logs/lifecycle/sondages), livrés **ensemble** dans la même PR.
>
> **Agent — pagination par chapitre (le mur de 379 étapes disparaît)** : `chapterPages` dérivé des blocs, **une vue = un chapitre**, pager haut/bas + sommaire repliable, recherche et filtre Dofus qui ouvrent **tous** les chapitres, mode « Tout » conservé. **Public** : `<nav aria-label="Sommaire du guide">` avec **59 titres gardés dans le HTML** (indexation) + ancres partageables `#bloc-…` (hashchange pour le bouton retour, `history.replaceState`, **ISR intacte**). **Interne** : blocs transposés à la grammaire publique (panneaux plats `rounded-[4px]`, bandeau `bg-elevated`, ancre Dofus nue, logos de marque locaux) — rail vert, `font-serif`, `line-through`, favicons Google et double barre de progression **supprimés**. Preuves : 1 seul panneau rendu sur 59, sommaire **678 px → 40 px** replié (336 px ouvert, liste plafonnée 288 px), hauteurs de page 3 733 → 5 586 px selon le chapitre, mobile 390 px identique.
> **Agent — progression locale PAR PERSONNAGE (guide public)** : nouveau module **pur** `src/lib/guest-progress.ts` (`guestProgressPrefix`, `readGuestProgress`, `readGuestCharacter`, `adoptAnonymousGuestProgress`) — personnage déclaré ⇒ emplacement dédié `sigil_guest_<slug>_c_<pseudo>-<idServeur>_…`, sans personnage ⇒ **clés historiques (aucune migration)**. **Adoption à la première déclaration seulement** (un 2ᵉ perso démarre vierge), **19 icônes de classe du jeu** (`DOFUS_CLASSES`) + pseudo + serveur (`DOFUS_UNITY_SERVERS`, pas d'icône de serveur dans le dépôt), l'overlay reçoit `guestStoragePrefix` + `key` (il lit **le même** emplacement) et le BroadcastChannel est indexé sur l'emplacement. Preuves : adoption **OUI**, séparation par serveur **OUI**, retour au perso **OUI**, popup détachée à `1 / 379`, icône de classe en overlay large (`probe-guest-character.mjs`, `probe-guest-overlay.mjs`).
> **Agent — sommaire public replié** (§12.9 fiche) : il était **ouvert d'office** (59 lignes = 678 px avant le 1ᵉʳ chapitre) ⇒ `<details>` `group/som` + `max-h-72`, compteur « n / 59 blocs terminés » dans la ligne repliée. **Rien ne quitte le HTML** (59 `href="#bloc-…"` vérifiés sans JS).
> **Agent — 🐛 « bugs de polices partout » = double encodage du CSS** (§12.7 fiche) : `src/app/globals.css` (fichier **non commité**) était en UTF-8 → cp1252 → UTF-8 ⇒ le `content` de `.reg-external::after` valait `" "` + **3 caractères parasites (U+00E2 U+2020 U+2014)** au lieu de `" ↗"` (**U+2197**) — d'où les flèches cassées **partout** (en-tête, pied, « Soutenir SigilOS ») — plus **452 chaînes** mangées (`é`, `─`, `•`…). Réparé par **inverse exact run par run** (`fix-mojibake.mjs`), + 1 chaîne dans `src/server/actions/ocre-actions.ts` (`👈`) trouvée par le scan global. **Preuves** : `::after` = `" ↗"` (**U+2197**) sur les 3 liens, `scan-mojibake-all.mjs` = **0 fichier** (`src/ public/ tests/ scripts/ docs/ prisma/ .agents/ .github/` ; les formes fautives ne sont **jamais recopiées** dans ces dossiers, même en exemple), `git diff --numstat` de `globals.css` 831/115 → **796/80**. **Garde-fou** : jamais de `Get-Content`/`Set-Content`/`Out-File` PowerShell 5.1 sur un source (règle ajoutée à `CONTEXT.md`).
> **Utilisateur — logs, cycle de vie des membres & bans Discord (cohérence)** : intents **`GuildModeration`** + `GUILD_BAN_ADD/REMOVE` dans `services/discord-bot/index.ts` (ban manuel ou bot tiers ⇒ `BANNED`, y compris **après** un départ) ; `GUILD_MEMBER_REMOVE` **vérifie la ban-list** avant de classer « parti » ; `sync-actions.ts`/`lifecycle-actions.ts` : les profils **`ARCHIVED` bannis** sont promus `BANNED` (avant : seuls les `ACTIVE` étaient comparés aux bans) et les `ARCHIVED` non bannis ne voient plus leur rétention 12 mois réinitialisée ; `discord-account-hygiene.ts` : `DEAUTHORIZED` **archive** les profils `ACTIVE` (plus de fiche fantôme) ; `reintegrateMember` **refuse** tant que le ban Discord est actif. **Libellés humains** de bout en bout : acteurs (« Bot SigilOS », « Vérification auto »), motifs (« Banni sur Discord », « A quitté le serveur ») et traduction à l'écran des **anciens codes** (`BANNED_AFTER_LEAVE`…) dans `audit-logs-client.tsx` (les codes inconnus en MAJUSCULES_UNDERSCORE sont **masqués**, jamais exposés bruts).
> **Utilisateur — sondages (perf & lecture)** : `poll-detail.tsx` retravaillé en profondeur (lignes d'options **mémoïsées**, barres en `transform: scaleX` — compositeur GPU au lieu du layout —, `content-visibility: auto`, liste des votants datée, `PollVote` typé), `poll-card.tsx` / `poll-list.tsx` alignés.
> **Preuves de la session** : `npx tsc --noEmit` **0** · `npx eslint` **0 erreur** (avertissements préexistants uniquement) · `npx vitest run` **138 fichiers / 1469 tests** (dont `guest-progress` 15 tests ; **1484 tests** après fusion de `dev`) · `next build` **OK en CI de PR** (le build **local** est resté bloqué par Turbopack : ~20 min sans aucune sortie ⇒ la CI fait foi) · HTML brut sans JS : 59 ancres + titres présents · mojibake **0 fichier**.
> ✅ **Livré** : **PR #683 mergée dans `dev`** (squash `81bef3b10`) — CI `Verify & Build` **verte en 7m52s** (lint + types + **types du bot Discord** + 1484 tests + **build de prod**), CodeQL/Semgrep/Trivy verts, images GHCR beta en cours de build.
> **Reste** : 3 décisions user (numérotation chapitre guide vs ordinal du pager · bouton « Tout valider » **au niveau chapitre** côté interne, qui demanderait une action serveur groupée · 7 dégradés + 4 ombres restants **dans les modales** du dashboard) · **1 seule image de bloc sur 60** en base (import côté God à faire, l'upload envoie maintenant au bon `scope`) · lot « code mort » à part (`DofusProgressStrip.tsx`, `split-implementation.ts`, `guide-styles.css` 86 Ko qui ne sert plus qu'à `OptimizedGuideClient`).
## ✅ Session 16-17/09/2026 — **Refonte du guide Sylvestre : pictos du jeu + alignement en direct + remise à zéro** → branche `feat/refonte-landing-anti-slop` (**RIEN de commité**)
> Chantier « Refonte anti-AI-slop des pages publiques ». Fiche : `refonte_landing/REPRISE-LANDING-ANTI-SLOP.md` (**§5.3** = plan du guide interne, **§11** = cette session) · amorce neuve `amorces/amorce-2026-09-17-rush-guide-sylvestre.md` (+ `AMORCE-RUSH-GUIDE-SYLVESTRE-A-COLLER.txt`) · mémo `memo-2026-09-17-rush-guide-sylvestre.md`.
> **Demande user** : « sur le guide public, les pictos d'activité sont des illustrations IA → **change les assets** » (3 fichiers désignés + « etc etc »), puis « l'alignement doit se voir **en temps réel** » et « il me faut un **bouton reset** ».
> **Fixe 25 — les 3 dernières signatures « IA » du guide public** (`RushGuideView.tsx` + `rush-guide-view.css`) : **assets servis NUS** (plus de tuile teintée sous le picto), coches de quête = **filet + coche fine 2 px** (plus de rond plein vert), étiquettes de statut = **texte** (plus de pilule), en-tête gauche-aligné, eyebrow muet, rayons ramenés sur la couche (`sm/md/lg`). CSS réécrit : **42 sélecteurs / 46 accolades**, `lightningcss` **0 avertissement**.
> **Fixe 26 — pictos = assets réels du jeu** : 10 illustrations **1024² (~1 Mo pièce)** remplacées par les **pictos colorés d'Ankama** via un **import curé** (`import-dofus-pictos.mjs` → `public/assets/dofus-ui/pictos/`, 10 fichiers, **48 Ko**, max 16 Ko). Tri décisif : `icons_assets_*` = **masques blancs** (luminance 255, invisibles en clair) → écartés, seules les sources **colorées** retenues (`figs_assets_/`, `uiassets_assets_2x/`, `uidarkstone_assets_all/`). `public/assets/rush-sylvestre/` : **8,83 Mo → 156 Ko**. **Une seule vérité** : `RUSH_ACTIVITY_TAG_CONFIG` (`src/lib/rush-guide-utils.ts`) — vue publique, overlay (`getTagBadgeIcon`, `resolveRushSequenceIcons`) et **God** (`ACTIVITY_TAGS`, désormais **dérivé**) la lisent ; donjon public (`/assets/nav/boss.png`, masque blanc) → picto du référentiel. **3 garde-fous** : existence disque · **< 64 Ko** · dossier unique.
> **Fixe 27 — alignement calculé en direct** : `computeRushAlignment()` (`src/lib/rush-guide-view.ts`) lit le **dernier `alignment_set` validé** (alias `bonta`/`brakmar`), exposé par `buildRushGuideView().alignment` ; rendu `.rgv-align` = **blason nu 16 px + « Alignement Bontarien · tranche 1 »**, sinon « **neutre** — aucune quête validée ».
> **Fixe 28 — remise à zéro** : prop **optionnelle** `onResetProgress` sur `RushGuideView` (la vue n'écrit rien) + composant `ResetProgress` **en 2 temps** (la confirmation **nomme** les 3 clés) ; `handleResetProgress` efface **ensemble** `completed_ms` · `steps` · `bookmarks`, vide les états React et **toaste** — l'**overlay ouvert se réaligne seul** (BroadcastChannel).
> **Preuves** : `tsc` **0** · `eslint` **0 erreur** · **137 fichiers / 1454 tests** · live sombre **et** clair (`verify-alignment-reset.mjs`) : neutre → **Alignement Bontarien · tranche 1** (blason réellement chargé) → reset → 3 clés `null`, compteurs **0 / 379 / 59** · pictos **10 × 200 `image/png`**, anciens **10 × 404**, **0 image cassée**.
> **Reste** : lot **guide interne** (plan prêt, fiche §5.3 — commencer par la vue en `variant="guild"` : elle hérite alignement + reset dès qu'on lui passe `onResetProgress`, puis retirer `guide-styles.css` **morceau par morceau**) · ouverts : barre collante (y répéter l'alignement ?), rayon `.rgv-head`, thème du guide interne (dark-locked), arbitrage de 2 pictos, pictos de métier à reclasser · favicons Google (~22 emplacements) · 2 guides publics en classes sombres (ratio **1,81** en clair) · coquille de l'overlay · **commit + PR**.
## ✅ Session 16/09/2026 (suite 3) — **« Avis de recherche » : suppression God persistante + simulation sur GRILLE VIDE (fin des « Abysses du temps »)**
> **Demande user (verbatim)** : « j'aimerai pouvoir **supprimer des avis** stp » · « supprime la map **abysse du temps** pour les avis **laisse juste dispo la map vide** ».
> **① Suppression d'avis (God)** : `src/lib/bounty-ignore.ts` (**nouveau**, serveur) = liste d'exclusion `public/game-data/ignored-bounties.json` (`{ entries: [{ dofusdbId, name, deletedAt }], updatedAt }`, parse **tolérant** + format historique `dofusdbIds`/tableau d'ids) · `deleteBountyAction(bountyId)` (supprime `Bounty` **+** sa fiche `MonsterStat` **+** inscrit l'id à l'exclusion) · `restoreBountyAction(dofusdbId)` · `getIgnoredBountiesAction()` (3 actions, garde `requireGameDataBounties` + `logGameDataWrite`, branche **`game-data-bounties`**) → **bouton « Supprimer »** rouge dans la barre collante de l'onglet God (confirmation expliquant l'exclusion) et **section « Avis supprimés (N) » en bas de liste** avec **« Restaurer »**.
> **Pourquoi une liste d'exclusion** : le siphon recrée tout ce qu'il trouve dans les 5 races DofusDB — une suppression en base seule serait **annulée par le cron**. Le siphon lit la liste en début de passe (`ignored` remonté dans le résultat, jamais silencieux) et **saute** ces avis : ni réécriture, ni recréation.
> **② Simulation sur grille vide** : `BOUNTY_FALLBACK_MAP` / `BOUNTY_MAP_FALLBACK_LABEL` (**supprimés**) → `BOUNTY_MAP_EMPTY_LABEL = "Map vide"` ; `BountyMapSource = "none" | "dofensive"` ; `pickBountyBattleMap()` ⇒ `{ id: 0, source: "none" }` dès qu'aucune carte n'est exposée ; le siphon n'emprunte **plus** la carte d'un donjon (`battleMapId: null`, `battleMapSource: "none"`, **plus d'appel** à `siphonDofensiveMapById`) ; `getBountyFiche` ne charge une carte que si la source est `dofensive` (sinon `dungeonMaps: null` ⇒ `SpellRangeGrid` « Map vide » 17×17) ; `bountyBattleMapFallbackLabel` → `bountyBattleMapLabel` (libellé **neutre** « Simulation : Map vide » à la place de l'avertissement ambre) dans la fiche publique **et** l'onglet Succès.
> **Preuves** : `tsc` **0** · **134/134 fichiers / 1419 tests** (dont **+8** : `tests/unit/bounty-ignore.test.ts` ×7 + exclusion dans le siphon) · `eslint` **0 erreur** · **base live** : `cartes d'emprunt restantes = 0` / `91 avis actifs` (les **5 avis que le user a supprimés pendant la session** — 3 × Ronce, Ronce animée, Œuf de la Mort, Carlita de l'Agerfelde — sont **restés supprimés** malgré une passe de siphon complète : `5 exclu(s)`, `91 inchangés`), fiche `/boss/gein-3761` **HTTP 200** avec **`Abysses` = 0 · `Carte de chasse` = 0 · `Map vide` = 2** et **0** trace des avis supprimés.
> **Reste** : Lot **5** (God relié : statut de siphon + liens fiche/DofusDB) · Lot **6** (dette : valeurs « estimées », lien fiche depuis la carte du monde) · **PR Lots 3→5** à ouvrir (branche `feat/avis-de-recherche-lot3-5`).

## ✅ Session 16/09/2026 (suite 2) — **« Avis de recherche » Lots 3 & 4 : catégorie OVERLAY + onglet SUCCÈS → PR #682**
> **Livré** : `BossOverlayClient` — `CatalogFilter` accepte **`"bounty"`**, chip **« Avis »** (contrôle segmenté passé à 5 colonnes), pool filtré sur `type === "bounty"`, entrées d'avis teintées **rose**, et pour un avis la fiche + la carte sont lues **localement** (`getBountyFiche`) au lieu des resolvers live (stats, sorts, butin et **simulation** fonctionnent donc en overlay, en fenêtre PiP par-dessus le jeu).
> **Onglet Succès** : `SuccesClient` → **« Fiches Avis de recherche »** (`?view=bounties`, icône `/assets/avis/avitons.png`) + **nouveau** `SuccesAvisTab` : liste des **96 avis** (recherche, niveau, image), fiche avec **onglets Fiche / Sorts / Simulation / Butin**, sélecteur de **grade** (5 ou 7), bandeau « zone de traque, `/travel`, prime, critères de chasse, **repli de carte déclaré** », boutons **Overlay** et **Fiche publique**.
> **Preuves** : `tsc` **0** · **133/133 fichiers / 1411 tests** · `eslint` **0 erreur** · runtime : `/boss` **HTTP 200** (avis présents) ; l'overlay et l'onglet sont **client-rendus** (le HTML SSR ne les contient pas ⇒ vérification par compilation + logique de filtre, pas par `curl`).
> **Reste** : Lot **5** (God relié : statut de siphon + lien fiche) · Lot **6** (dette : valeurs « estimées » de `syncBountiesCompleteFromDofusDb`, lien fiche depuis la carte du monde).

## ✅ Session 16/09/2026 (suite) — **« Avis de recherche » Lot 2 : fiche publique + catalogue → PR #681**
> **Livré** : `src/lib/bounty-fiche.ts` (**pur** : entête publique, prime, critères, repli de carte déclaré, entrées fiche + catalogue) · `src/server/actions/bounty-actions.ts` (**lecture 100 % locale** : `findBountyRow` par id **ou slug**, `getBountyBattleMap` (grille `DofensiveMap` ou repli), `getBountyFiche`) · **3ᵉ repli** dans `/boss/[dungeonId]` (Dungeon → Titan → **Avis**) avec `kind: "bounty"`, **zéro appel réseau** (fiche + carte pré-chargées) · `PublicBossDetailClient` : badge de race, **zone de traque**, `/travel` copiable, milice, **prime**, **critères de chasse**, **« Simulation sur carte de chasse générique »** (repli annoncé), liens de source + date de synchro, onglet « Sorts du **monstre recherché** » · `getBestiaireCatalog` : entrées `type: "bounty"` (uniquement `isBountyMonster`, les 51 lignes historiques exclues) · filtre **« Avis de recherche »** du catalogue `/boss` · `getLocalMonsterStatByIdAny` (fiche locale **par ID**, indispensable aux homonymes).
> **Preuves** : `tsc` **0** · **133/133 fichiers / 1411 tests** (dont **9** nouveaux) · `eslint` **0 erreur** · **runtime** (dev server) : `/boss/<avisId>` = **HTTP 200** (73,6 ko) contenant « Zone de traque », « Avis de recherche », « Sorts du monstre recherché », « Simulation sur » (repli) et la zone réelle (`Kaliptus`) ; `/boss` = **HTTP 200** avec les avis présents en SSR (`Aermyne`, `Scalptaras`, `Nimotopia`, `Padgref`) ⇒ catalogue bien alimenté.
> **Reste** : Lot **3** (catégorie overlay `bounty`) · Lot **4** (onglet Succès) · Lot **5** (God relié) · Lot **6** (dette).


## ✅ Session 16/09/2026 — **chantier « Avis de recherche » : Lot 1 (siphon des 96 avis + migration) → PR #680**
> **Demande user** : « j'aimerais … la même chose côté module fiche boss / succès avec **tous les avis de recherche** … **Siphon dofensive/dofusdb**, onglet **sort/simulation** etc, **nouvelle catégorie dans l'overlay** — on a un **onglet dédié** (God `game-data/bounties`), faudrait ptet **tout relier** ».
> **APIs mesurées d'abord** (sondes `_probe-bounties-gap.mjs`, `_probe-bounty-list.mjs`, `_probe-bounty-map.mjs`, `_probe-map-cells.mjs`) : DofusDB `monster-races/32|90|127|147|156` = **96 avis** (38+21+15+19+3 · **0 doublon**) · **`isBounty` NON fiable** (faux pour 6/38 et **14/96**) ⇒ c'est la **RACE** qui fait foi · **`$select` inutilisable** (ne renvoie que `_id`) · Dofensive `/monsters/{id}` : **38/38 exposés** (`Race.Name` « Avis de recherche », famille **27**, `Subareas[]` = **zone de traque**, `Drops[].Criteria` = quête) mais **`PreferredMaps`/`Dungeons` VIDES** et les maps sauvages renvoient un **stub** (`Cells: {}`) ⇒ **aucune grille** pour un avis.
> **Base de départ** : `Bounty` = 83 lignes dont **32/38** avis seulement (**51 lignes historiques** archimonstres/boss) · **6 avis manquants** · **aucune** colonne `dofusdbId` · `MonsterStat` **0/38** fiche d'avis.
> **Livré (Lot 1 — 1 lot = 1 branche = 1 PR)** : `src/lib/bounty.ts` (**pur**) + `src/lib/bounty-siphon.ts` (liste par races → **preuve** Dofensive → zone de traque → sorts de combat Dofensive + **repli DofusDB** → carte de **repli déclaré** → icônes WebP) · **migration additive** `20260916120000_bounty_avis_de_recherche_siphon` (`dofusdbId @unique`, `slug`, `raceId`, `raceName`, `subareaIds`, `isBountyMonster`, `battleMapId`, `battleMapSource`, `dofusdbSyncedAt` + **retrait de `name @unique`** : 3 homonymes « Ronce ») · **phase 4** du cron `sync-monster-stats` (compteurs **séparés** `bountySynced/bountyErrors/bountyIcons/bountyUnproven`) · bouton God **« Siphonner les avis de recherche »** (brique **`game-data-bounties`** acceptée) · allowlist DofusDB élargie à `monster-races` · champs **curés** de `Bounty` (`rewards`, `doplons`, `milice`, `mechanics`, `position`…) **jamais écrasés**.
> **🐞 Cause racine trouvée par la PREUVE LIVE (pas la lecture)** : `getMonsterStats` résolvait la fiche **par NOM** (`monsters?name.fr=`) ⇒ les homonymes (3 × « Ronce », « Mouchâme ») recevaient la fiche du **premier** homonyme (**2 avis sans fiche**). Corrigé **à l'étage fautif** : `getMonsterStats(..., monsterId?)` résout **par ID** (+ garde d'identité dans le siphon : jamais de fiche croisée) ⇒ **96/96 fiches**.
> **Preuves** : `tsc` **0** · **132/132 fichiers / 1402 tests** (dont **18** nouveaux dans `tests/unit/bounty-siphon.test.ts`) · `eslint` **0 erreur** · **sonde live** `npx tsx _probe-bounty-siphon.ts` : `96 avis · 96 écrits · 0 erreur · 466 icônes · 62,3 s` puis **2ᵉ passe `96 inchangés`** (**idempotence** prouvée) · base : **96 lignes `isBountyMonster`** + **83 lignes historiques intactes** · **15 avis sans zone** (== la mesure) · **96 en carte « default »** · **3 « Ronce » coexistants** (slugs `ronce-3530`/`ronce-3555`/`ronce-animee-3531`) · **96/96 icônes locales** · map de simulation `Abysses du temps` (40 lignes de grille) · 2 « Ronce » **sans aucun sort** (ni DofusDB ni Dofensive n'en donnent ⇒ l'UI dira « aucun sort », sans invention).
> **Reste (même chantier, PRs suivantes)** : Lot **2** fiche publique `/boss` (+ `getBountyBattleMap`) · Lot **3** catégorie **overlay** `bounty` · Lot **4** onglet **Succès** « Fiches Avis de recherche » · Lot **5** God relié (statut de siphon + lien fiche) · Lot **6** consolidation/dette. Doc de chantier : `amorces/amorce-2026-09-16-avis-de-recherche.md`.
> **Ops** : la migration doit être jouée sur **beta/prod** par `./scripts/deploy.sh` (`migrate deploy`) — **dépôt + base locale à jour** ; `prisma migrate dev` a été **refusé volontairement** (drift pré-existant `20260819000000_add_inter_guild` ⇒ Prisma proposait un **RESET** = perte de données) ⇒ SQL **écrit à la main**, appliqué + enregistré (`_apply-bounty-migration.mjs`).

## ✅ Session 15/09/2026 (nuit) — **3 constats user : parité landing/interne (« Boss libre ») · zoom molette des maps isométriques · alliés/placement/butin sur la landing d'anomalie**
> **Demande user** : « en landing on a la possibilité du boss libre on/off mais pas en interne » · « le scroll zoom molette est ko à l'intérieur des composants isométriques (interne/externe) » · « sur les pages landing : on peut pas poser d'allié / pas de placement de départ dispo / pas de placement / pas de butin ».
> **① Parité « Boss libre »** : ajouté à **`SuccesBossGuide`** (« Fiches Boss » + « Fiches Anomalies ») et **`SuccesTitanTab`** (« Fiches Titans ») — la landing et l'overlay l'avaient déjà. Même défaut que la landing (libre) ; repasser en épinglé repose le boss sur sa case de placement réelle. **Aucune donnée écrite.**
> **② Molette = zoom (interne ET landing)** : cause = zoom uniquement si `compact || Ctrl/Cmd` **alors que** le `preventDefault()` était déjà inconditionnel ⇒ la molette bloquait le scroll **sans** zoomer (zone morte). Correctif : **zoom toujours** (listener natif non passif conservé, normalisation `deltaMode` Firefox, bornes 0.3–3.5) + légendes/tooltips mis à jour.
> **③ Landing × donjons **anomalie** (cause racine mesurée)** : la landing n'avait **aucun** chemin anomalie — `getDofensiveDungeonForBoss` ne peut pas résoudre ces donjons (**Dofensive n'expose aucun gardien d'anomalie** : 133 donjons vérifiés en direct) ⇒ `initialDungeonMaps = null`, **mis en cache ISR** (`revalidate = 3600`) et **jamais re-tenté** (garde `!== undefined`) ⇒ fiche vide : ni « Salle », ni « Placements de départ », ni `Placement`, ni `Butin` (tous conditionnés par `mapData`). Correctif : **`getAnomalyBossBattleMap` + `getAnomalyBossFamily`** branchés dans `page.tsx` **et** en lazy côté client (mêmes lecteurs que l'interne) + `initialDungeonMaps ?? undefined` (un échec de préchargement est re-résolu) + badge « **Anomalie · 3 au hasard parmi 16** » et libellé « Monstre de l'anomalie · <race> » sur l'onglet « Monstres de la salle ».
> **🐞 Cause n° 2 (la VRAIE bloqueuse, mesurée en navigateur)** : `SpellRangeGrid` **restaurait** sa sauvegarde `localStorage` **APRÈS** l'auto-sélection de la carte et **réinjectait l'état transitoire `mapId: "empty"`** (écrit au 1er rendu) ⇒ carte auto-sélectionnée **écrasée**, **aucun** appel serveur, fiche bloquée sur « Map Tactique Isométrique » — **même quand** le sélecteur listait « ⚔ Tour minérale ». Correctif : **garde-fou `restoredKeyRef`** (restauration **une seule fois** par carte/donjon) + **aucune écriture avant restauration**.
> **④ Alliés** : « Placements de départ » **posait le boss mais jamais les alliés** (contradiction avec sa légende) ⇒ pose désormais **boss + ≤ 4 alliés** sur les cases de départ **joueurs** (`enemyCells`, inversion conforme Dofus) via le helper **pur** `allyStartPositions` (`src/lib/dofus-grid.ts`), **sans jamais écraser** des Fécas déjà placés.
> **⑤ Dégâts des sorts de monstre (constat user : « Dofensive a un réglage pour le calcul des dégâts »)** : cause mesurée = l'API Dofensive renvoie les **jets BRUTS** (« Râle d'Agonie » = 74 à 86) et la fiche les affichait, alors que Dofensive (réglage **activé**) publie **666 à 774** — notre `description` (chemin DofusDB, déjà recalculé) affichait elle aussi 666 à 774 (incohérence interne). Règle vérifiée : `dégâts = floor(jet × (1 + caractéristique/100))` avec **Agilité 800 ⇒ ×9** (74×9 = 666 · 86×9 = 774 · poison 12-14 → 108-126 = valeurs exactes de Dofensive). Correctif : module **pur** `src/lib/dofus-monster-damage.ts` (+ 7 tests) branché dans `getDofensiveSpells` ⇒ **cron, siphon anomalies et lectures live** corrigés ; **Terre=Force, Feu=Intelligence, Eau=Chance, Air=Agilité**, **Neutre non boosté** (convention DofusDB existante), aucun payload muté.
> **Preuves** : `tsc` **0** · **130/130 fichiers / 1371 tests** (+11) · `eslint` **0 erreur** (10 fichiers) · **vérif RUNTIME navigateur (Playwright)** : carte « Tour minérale · 547 cases », Salle = « ⚔ Tour minérale », Placement + Butin visibles, « Placements de départ » → **Alliés 0/4 → 4/4**, **molette 100 % → 175 %** sans défilement (`scrollY` 0), 0 erreur console · **sonde live** `_probe-damage-scaling.ts` = ✅ **parité Dofensive**.
> **Reste (données)** : siphon anomalie à lancer (bouton God / cron phase 3) pour `Dungeon.anomalyMapId`/`anomalyFamily` + `anomaly.companions` ; les fiches d'effets **déjà** en cache (< 24 h) gardent les jets bruts jusqu'au prochain siphon/cron.



## ✅ Session 15/09/2026 (soir, suite) — **polish « Boss Anomalie » (renommage · filtres épurés · image Défi) + bug bloquant de la modale DJ « Mode Défi »**
> **Demande user** : « épurer un peu plus les filtres dans mes succès », **renommer** « Boss Anomalie » → **« Fiches Anomalies »** et « Fiche Titans » → **« Fiches Titans »**, **grossir l'image** de l'onglet Défi (« on voit rien »), et corriger la modale « Nouveau Groupe Défi » (« on trouve les défis mais **on ne peut pas avancer et créer le post** »).
> **Renommage (libellé unique partout)** : `SuccesClient` (barre de vues) · puce de filtre `SuccesTracker` · God `DungeonManager` (filtre **type** + case de la modale de création) · catalogue public `/boss` (`TYPE_FILTERS`) · **tour** « Sept vues ». La **catégorie d'overlay** reste « Anomalies » (libellé court, aligné sur « Titans » / « Monstres ») ; aucune donnée ni badge d'élément modifié.
> **Filtres « Mes Succès » épurés** : statut = **un seul contrôle segmenté** (Tous / À faire / Finis) ; anomalies = **une puce** « Fiches Anomalies » ; tranches de niveau = **un `Select` compact** (`Tous les niveaux` / `Niveau 1-99` / `100-149` / `150-199` / `200+`) **au lieu de 5 puces** ; actions de tranche (valider / décocher) inchangées. **Aucun filtre perdu**, moins de contrôles à l'écran.
> **Image de défi (onglet Défi)** : les visuels sont des **panoramas**, illisibles dans un cadre carré de 80 px ⇒ **bandeau pleine largeur** (`h-44`, `sm:h-60`, `object-cover`, repli icône si absente/cassée), vignette de liste élargie (`w-16 h-11`), idem vignettes de la modale DJ (`w-20 h-12` / `w-14 h-10`).
> **🐞 Modale DJ « Mode Défi » (bloquante)** : le mode Défi était le **seul** des 4 modes **sans bouton de validation** (Donjon = clic direct, Titan/Quête = bouton « Suivant ») ⇒ après sélection d'un défi, **impossible de passer à l'étape 2** (donc impossible de créer le post). Correctif : bouton « **Suivant** » (désactivé tant qu'aucun défi n'est choisi — parité Titan), couleur en **token sémantique**. Le reste du flux (`handleSubmit` → `createDjPost` avec `defiId`/`defiName`) était déjà correct.
> **🧯 Réparation d'environnement (base LOCALE)** : `createDungeon` échouait avec `Unknown argument isAnomalyBoss` **puis** `P2022 column does not exist` ⇒ **3 causes** : (1) **bundle `next dev` obsolète** (client Prisma régénéré non repris à chaud → **redémarrer le serveur**) ; (2) **colonne absente** en base → SQL de la migration additive appliqué + `migrate resolve --applied` ; (3) **`migrate deploy` bloqué en `P3009`** par `20261113000000_add_market_notifications` (`42710 enum label "MARKET" already exists`) → libellés vérifiés (9/9 présents) + `migrate resolve --applied` + `migrate deploy` (**2 migrations en retard appliquées**). État final : **`Database schema is up to date!`**. ⚠️ Même recette à appliquer si beta/prod renvoient `P3009` (sonde `_fix-notification-enums.js`).
> **Preuves** supplémentaires : `prisma.dungeon.create({ … isAnomalyBoss: true, achievements.create })` **passe** (transaction volontairement annulée, 0 donnée écrite).
> **🌀 Monstres de l'anomalie (Briko / Bruto / Gromo)** : rattachement **sourcé** = **famille Dofensive 35** (« Créatures des Anomalies Temporelles », la même que les gardiens race 191), via 3 races DofusDB (**Gromorphes 192 = 6**, **Brutomorphes 194 = 5**, **Brikomorphes 195 = 5**), tous niv. 200, `isBoss=false`, **sans carte** (`PreferredMaps: []`) ⇒ rattachement **par famille**, pas par carte. Siphon étendu (`resolveAnomalyCompanions`, preuve `Family.Id === 35`, fail-soft) : fiche `MonsterStat` + sorts + icône WebP pour chacun, **aucune ligne `Dungeon`** ; gardiens enrichis de `anomaly.companions` / `companionFamilyId` / `companionPick = 3`. Lecteurs : `getAnomalyBossFamily` (+ `companions` + « 3 au hasard parmi 16 »), `getAnomalyBossBattleMap` (pool = gardien + co-gardiens + accompagnateurs ⇒ simulation « 1 boss + 3 mobs »). UI : badge + libellé « Monstre de l'anomalie · <race> ». Télémétrie `anomalyCompanions` (cron + toast God). Sonde live : **16 validés** (le guide DPNL en liste 15 — la source API 16 ; bascule possible en 1 ligne).
> **Preuves** : `tsc` **0** · **129/129 fichiers / 1360 tests** (+5) · `eslint` **0 erreur**.


## ✅ Session 15/09/2026 (soir) — **onglet « Boss Anomalie » (Gardiens des anomalies)**
> **Livré** : siphon complet des **16 gardiens d'anomalie** (DofusDB `monsters?race=191` = liste ; Dofensive `/monsters/{id}` = carte `PreferredMaps` + famille + type), **map par défaut** (« Abysses du temps ») quand la carte manque (**Qilby 8131**, absent de Dofensive → sorts **recalculés** depuis DofusDB `spells`/`spell-levels` : AP, portée, LdV, ligne/diagonale, critique, **zone** calibrée `P/L/C/X`), icônes monstre + sorts siphonnées en WebP.
> **Modèle** : `Dungeon.isAnomalyBoss` + `anomalyMapId` + `anomalyFamily` (migration additive `20261216000000_add_dungeon_anomaly_boss`, **à jouer** en beta/prod) ; `name` = **carte de combat**, `bossName` = gardien, `isNoAchievement` ⇒ pseudo-succès « Donjon validé » (source de vérité déplacée dans `src/lib/dungeon-no-achievement.ts`, réutilisée par le siphon).
> **Cron** : phase 3 de `sync-monster-stats` (compteurs **séparés** : `anomalySynced`/`anomalyErrors`/`anomalyDefaultMap`) + bouton God `siphonAnomalyBossesNow()` dans **game-data → Donjons**.
> **UI** : nouvel onglet **« Boss Anomalie »** (`SuccesClient` → `SuccesBossGuide anomalyOnly`, **même fiche boss**, map/famille résolues localement via `getAnomalyBossBattleMap`/`getAnomalyBossFamily`) ; filtre **« Boss Anomalie »** dans **Mes Succès** (+ badge) ; God : **filtre type** + **case « Boss Anomalie »** dans la modale de création (icône `assets/missions/ano1.png`) + badge carte ; **catégorie « Anomalies »** dans l'overlay interne (`BossOverlayClient`) et le catalogue public `/boss` (`BestiaireEntry.type = 'anomalie'`) ; tour « **Sept vues** ».
> **Preuves** : `prisma generate` · `tsc` **0** · **129/129 fichiers** de tests (nouveau `tests/unit/anomaly-boss.test.ts`, 13 tests) · `eslint` **0 erreur** · sonde live `_probe-anomaly.ts` (**16 gardiens**, 15 cartes Dofensive, Qilby = map par défaut + sorts DofusDB).
> **Règle de contenu** : seuls les gardiens **`isBoss`** obtiennent une fiche « Boss Anomalie » (**11 boss**) ; les 5 accompagnateurs (niv. 20) restent les **« monstres de salle »** de leur carte. `prisma migrate deploy` reste **à jouer** (migration additive).

## ✅ Session 15/09/2026 (nuit) — **amorce « indépendance totale » : Lot 1 (`stale-while-offline`) + D6 (test de panne)** → PR **#678**
> **Contexte** : « Fiches Anomalies » (+ monstres de l'anomalie) commité/PR-é d'abord (**PR #677**, `feat/fiches-anomalies`) pour repartir d'un `dev` propre ; l'amorce a ensuite été attaquée seule sur `feat/independance-lot1`.
> **D6** : `DOFUSDB_OFFLINE=1` / `DOFENSIVE_OFFLINE=1` coupent **tout** appel sortant des deux fetchers (refus + log + `null`), **avant** le cache mémoire ⇒ le **test de panne** devient exécutable (l'assertion fait échouer `fetch` s'il est appelé).
> **Lot 1** : getters **non gatés** `getLocal*Any` → `{ data, lastSyncedAt, stale }` (`LocalStaleResult`/`toStaleResult`/`pickCombatSpells`), les getters **gatés** deviennent **délégants** (zéro régression) ; `getMonsterStats` sert une fiche **périmée** (`stale` + `syncedAt`, cache 5 min) ; `getDofensiveDungeonForBoss` lit le **local AVANT** le live (y compris double-boss) ; `getDofensiveMap`/`getDofensiveSpells` propagent `stale`/`syncedAt`. **Seul « aucune ligne » autorise le repli réseau.**
> **Tests** : `tests/unit/offline-stale-serve.test.ts` (**13 tests**) — 40 j ⇒ **servi** avec `fetch` **interdit** (4 chemins), non-régression, « aucune ligne ⇒ live », payload sans sorts de combat refusé, D6 (+contrôles).
> **Preuves** : `tsc` **0** · **130/130 fichiers / 1373 tests** (+13) · `eslint` **0 erreur** · **PR #678**.
> **✅ Rebasé sur `dev` post-squash de #677 (16/09)** : conflits résolus en **cumulant les deux apports** — `siphonDofensiveMapById` (#677) **et** getters `…Any` du Lot 1 ; la lecture des sorts **par nom** (`getLocalDofensiveSpellsByName`) n'a **pas** de variante `…Any` ⇒ conservée telle quelle (fraîcheur stricte).
> **Reste** : Lot 4 🟠 · Lot 5 · Lot 2 · Lot 3 · Lot 6.

## ✅ Session 15/09/2026 (nuit) — **amorce soldée (A/B/C) + 6 correctifs user** → branche `feat/marche-publication-discord`
> **Demande user** : « one shot toute l'amorce » (§A marché, §B barre de défilement overlay, §C « Boss libre ») **plus** les constats beta : menus déroulants **sous** le contenu (salles + niveaux/type), `/boss` « trop IA-like », icônes de sorts qui ne chargent pas, fiches mobs/simulation **en spinner infini** quand DofusDB/Dofensive tombe, **maps manquantes**, 2 crons « Inconnu ».
> **§A1** : `componentId` optionnel (`mkt:<action>:<listingId>[:<componentId>]`, **3 OU 4 segments** acceptés = rétro-compatibilité), routeur → `reserveBundleComponentCore` / `releaseBundleComponentCore`, **boutons remis** dans `syncBundleComponentMessages` (état **par objet**), pied d'embed « Objet #… ».
> **§A2** : étape 4 « Publication » **dédiée** + estimation « 👥 X membres seront notifiés » (`estimateMarketPingAudience` — dédupliqué, rôles **intersectés**, borné 1000, `approximate`, jamais de liste de membres).
> **§A3** : « rôles notifiables » configurable **uniquement** dans God → Marché (`getGodMarketPingRoles` / `saveGodMarketPingRoles`, super-admin, ids internes, `@everyone` écarté, audit) ⇒ le ping ne restera plus vide.
> **§A4** : fiche `/marche/[listingId]` — badge **`X/N disponibles`**, prix **par objet** (icône kamas), état par objet, bouton **« Réserver cet objet »** (`reserveMarketBundleComponent`).
> **§A5** : suppression des **N messages d'objet** au retrait + message d'objet supprimé à la main **recréé** (§13.6).
> **§B** : `.overlay-scroll` (globals.css, copiée dans le document PiP) — gouttière 12 px, `pointer-events:auto`, `overscroll-behavior:contain`.
> **§C** : « Boss libre » piloté dans l'overlay (`freeCasterMove` + `onFreeCasterMoveChange`), retour au placement réel en épinglé.
> **🐞 dropdowns** : contexte d'empilement corrigé (`relative z-30` barre de filtres `/boss`, `z-40` sélecteur de **Salle** compact + complet, fond de menu opaque).
> **🎨 `/boss`** : hero et cartes **sobres** (plus de halos/ombres/zooms/badges clinquants).
> **🖼️ sorts** : proxy `/api/assets-dofus` résout `img/spells/sort_<iconId>.png` (l'`id` du sort n'est pas l'icône).
> **⏳** : `dofusdbFicheInit()` (délai dur 8 s) sur **tous** les fetch DofusDB des fiches ⇒ plus de spinner infini, repli local.
> **🕒 crons** : `sync-dofensive-maps` / `sync-monster-stats` **tracent leurs 401** (`recordCronRefusal`) + les **2 lignes de crontab** documentées dans `MAINTENANCE.md` (cause des **maps manquantes**).
> **🎨 confort (2ᵉ passe, PR #669 — branche `fix/overlay-boss-marche-option-a`)** : **or décoratif retiré** de l'overlay encyclopédie **et** des fiches détaillées (interne + publique) — seuls restent les usages **sémantiques** (Ocre, résistance **Terre**, marqueur de case **boss**, couronne du boss de salle). Fiches : **barre de caractéristiques unifiée** (9 pastilles → une rangée à 3 groupes), **onglets soulignés** (plus de pastille dorée), **en-tête sobre** (1 bouton plein + liens texte, DofusDB/Dofensive), **onglet Simulation aplati** (plus de triple bordure autour de la grille), **bannière guilde déplacée en bas** de la fiche. **Rien n'a été retiré.**
> **§D — « la page bouge au changement d'onglet » (landing `/boss/[id]`)** : cause = hauteurs d'onglets différentes ⇒ la barre de défilement apparaît/disparaît ⇒ le contenu **centré** (`max-w-6xl mx-auto`) se décale de ~15 px à chaque bascule. Correctif : `html:has(.boss-detail-shell) { scrollbar-gutter: stable }` (`globals.css`, **scopé** à cette page — la PiP de l'overlay ne contient jamais cette classe, donc pas de double rail) + panneau d'onglet à **hauteur stable** (`min-h-[560px]`, `[overflow-anchor:none]`) + rangée d'onglets en `no-scrollbar`.
> **§E — affiliations Dofensive retirées des surfaces publiques** : badge « Simulation Dofensive » → « Simulation tactique » (neutre), liens Dofensive de l'en-tête / des guides / de l'**overlay** supprimés (DPLN conservé ; « Guides : » masqué s'il n'y a aucun lien), page démo neutralisée. **Conservés** : attribution de source dans `/legal/mentions` + surveillance `/status`.
> **§F — crontab VPS : cause racine corrigée ET VÉRIFIÉE (one-shot du 15/09/2026)** : les siphons « jamais exécutés / God *Inconnu* / salles de donjon manquantes » ne venaient **pas** d'un manque de lignes mais du **`%` non échappé** des `-w "%{http_code}"` → cron coupe la ligne au premier `%` → bash reçoit une quote non fermée → **exit 2, aucun log, aucune télémétrie**. **Preuve** : les **7 lignes** contenant `%` = exactement celles dont le log n'existait pas (les 13 sans `%` tournaient). 2ᵉ défaut : 3 lignes lisaient `.sigilos-cron-secret`, **fichier inexistant** → header vide → **401**. Corrigé : `\%` ×7, `$CRON_SECRET` ×3, commentaire nettoyé. **Vérifié 10/10** (`one-shot-cron.sh`) : `status-ping` / `market-expire` / `ladder-sync` → **200**, `market-expire.log` rafraîchi toutes les 10 min, siphons **200** (690 maps *unchanged*, 10 monstres synchronisés, **0 erreur**), worker ladder configuré. Reste (confort, non bloquant) : 12 lignes sans `-w` → logs illisibles (`sync-members.log` 2 Mo) ⇒ `truncate -s 0` + `-w` optionnel.

> **Preuves** : `tsc` 0 · **128/128 fichiers** (**1334 tests**) · `eslint` 0 erreur.

## ✅ PR #668 (en cours de CI) — **Marché · lot multiple (option A) — assistant complet + message Discord par objet** → branche `feat/marche-publication-discord` (dernier commit `c02404b65`)
> **Décisions user (14-15/09/2026)** : **2 à 5 objets** par lot · **prix par objet** (jamais de prix global) · **option A** = **un message Discord par objet** · maquette validée · plan `refonte-marche/PLAN-LOT-MULTIPLE.md` · **amorce des prochains fixes** `AMORCE-PROCHAINS-FIX.md`.
> **Assistant (visible)** : 4ᵉ nature « **Lot multiple** », **2 objets pré-remplis**, **une recherche catalogue par ligne** (toutes familles, l'objet choisi s'affiche avec sa vignette + « Changer », plus de nom saisi à la main, champ « unité » supprimé), **étape 3 dédiée** (nom de lot pré-rempli, conditions, composition + total), **N aperçus d'embed** (un par objet) + récapitulatif, **vignettes de nature = objets réels** par famille (surchargeables par `public/assets/dofus/natures/*.png`), **icône kamas** officielle sur tous les montants, messages zod **en français**.
> **Moteur & Discord** : `src/lib/market/bundle.ts` (pur) + `src/server/market/bundle.ts` (`createBundleListingCore`, réservation **par objet** avec garde `status = 'AVAILABLE'` **dans le `WHERE`**, libération en compensation, expiration) · migrations additives `20261201000000_add_market_bundle` + `20261215000000_add_component_discord_message` (**idempotentes**, aucun backfill) · **`syncBundleComponentMessages()`** = **un message Discord par objet** (nom, quantité, **prix**, vignette, ids stockés sur l'objet).
> **Bugs corrigés dans ce lot** : « Modifier l'annonce » d'un lot **redirigeait** (`modifier/page.tsx:53`) ⇒ débloqué ; l'édition **n'enregistrait pas** les objets/prix ⇒ `updateMarketListing` a une branche `items[]` ; l'**image de l'embed ne suivait pas** l'édition (clé `og:<statsHash>` identique) ⇒ clé **objet + jet + horodatage** + invalidation non bloquante ; clic sur un résultat de recherche qui laissait le panneau ouvert ⇒ refermé ; `prisma generate` requis après toute migration de colonne.
> **Preuves** : `prisma validate` OK · migrations jouées en local · `tsc` **0** · **127/127 fichiers de tests** · `eslint` 0 · hook pre-commit vert à chaque commit.
> **Reste (amorce §A)** : boutons **par objet** (`custom_id` 4 segments + routeur → `reserveBundleComponentCore`), étape 4 **dédiée** (bloc ping séparé + **estimation des notifiés**), **rôles pinguables redevenus configurables** (`marketAllowedPingRoleIds` plus alimenté), fiche (`3/5` + **réserver un objet**), suppression des N messages au retrait.
> **Nouveaux constats traités dans l'amorce (§B/§C)** : 🐞 overlay **encyclopédie monstres** — barre de défilement **non draggable à la souris** (`BossOverlayClient.tsx`, 5 pistes mesurables) · 🐞 **« Boss libre » absent dans l'overlay** — cause mesurée : `SpellRangeGrid` l'expose (`allowFreeCasterMove`, l.87) et la landing la passe (`PublicBossDetailClient.tsx:675`) mais **pas** `BossOverlayClient.tsx:1010` ⇒ bascule à exposer **dans le module**.

## ✅ PR #666 (prête à merger) — **Marché · LOT MULTIPLE (option A) — fondations + moteur** → branche `feat/marche-lot-multiple` (depuis `dev`)
> **Décisions user (14/09/2026)** : **2 à 5 objets** par lot (familles mélangées) · **le prix vit sur l'objet** (pas de prix global) · **option A** = **un message Discord par objet** (boutons propres à chaque objet) · maquette `refonte-marche/maquette-lot-multiple.html` **validée** · plan `refonte-marche/PLAN-LOT-MULTIPLE.md`.
> **Schéma (migration additive `20261201000000_add_market_bundle`, idempotente, aucun backfill)** : `MarketListingType + BUNDLE` · enum `MarketComponentStatus` (`AVAILABLE`/`RESERVED`/`SOLD`) · `MarketListingComponent.priceKamas/status/reservedAt/soldAt` · `MarketReservation.componentId` (réservation **par objet**) · `MarketDiscordMessage.componentId` (un message par objet) + index dédiés.
> **Moteur** : `src/lib/market/bundle.ts` (**pur** : bornes 2→5, `computeBundleTotal`, `computeItemUnitPrice`, `canReserveComponent` + refus typés, `describeBundleProgress`, `deriveBundleListingStatus`, `summarizeBundle`) et `src/server/market/bundle.ts` (`createBundleListingCore`, `reserveBundleComponentCore`, `releaseBundleComponentCore`, `settleBundleComponentCore`, `refreshBundleListingStatusCore`, `expireBundleComponentsCore`). ⚠️ **Anti-double-clic** : la disponibilité est portée par le `WHERE` (`status = 'AVAILABLE'`) ⇒ un seul `count === 1`, l'autre reçoit `ALREADY_RESERVED` ; échec de création ⇒ **libération en compensation**.
> **Preuves** : `prisma validate` OK · migration jouée en local + `migrate resolve --applied` · `migrate diff` **0 écart sur le Marché** (reste la dérive préexistante `Titan`, hors périmètre) · `prisma generate` · **`tsc` 0** · **tests 1303/1303** · `eslint` 0.
> **Reste (même branche, lots suivants)** : ① assistant — 4ᵉ nature « Lot multiple » + 2→5 lignes avec prix **par objet** ; ② nouvelle étape **« Publication »** (résumé + estimation des notifiés + aperçu du salon) ; ③ Discord — `buildMarketBundleMessages()` (N messages), `custom_id` à **4 segments** rétro-compatible, réconciliation par message ; ④ UI catalogue/fiche (avancement du lot, réservation d'**un** objet).

## ✅ DERNIÈRE PR (en cours de CI) — **Marché · clôture du debug beta (2ᵉ vague)** → branche `fix/marche-debug-beta-2` (depuis `dev` = **`7525cb9d7`**)
> **② Carte d'item** : le **texte de lore** (bloc « 8 — Description ») n'est **plus rendu** ; **① objet légendaire** = règle **pure** `hasLegendaryCapacity(nativeEffects)` (présence de **`effectId 1175`** — `GameItem.isLegendary` est **toujours `false`**, DofusDB n'expose pas le champ) ⇒ mention « **Objet légendaire** » seule.
> **③ Timeline des offres** (placement choisi) : sous la **carte d'item**, `<details>` **replié** (un équipement à 17 lignes ne pousse plus les boutons d'achat) ; pastille + libellé + date. 🔒 **§13.7** : requête **`status` + horodatages seuls**, bornée (`take: 12`), scopée par la guilde.
> **④ Désistement** depuis la fiche : `cancelMarketReservation` (la **même** action que `mkt:cancel`) est enfin exposée (`reservation.isMine`) ⇒ bouton « **Me désister** ».
> **⑤ Catalogue** : **une seule barre d'outils** (recherche → Type/Statut/Tri → bascule Cartes/Tableau) + 2ᵉ ligne sous filet (masquage, **compteur**, **réinitialisation**).
> **⑥ Image OG** : **pied supprimé** (« Annonce #… / date / SigilOS Market / pseudo » ⇒ plus de ligne horizontale en bas) et hauteur qui **réserve le titre `EFFETS`** + un `gap` par ligne ⇒ fin du recouvrement.
> **⑦ Réglages → Marché** : suppression des 3 sélecteurs de rôles (la **matrice RBAC** est la seule source de droits) + du panneau « Durées, plafonds & rappels » (le God écrit **déjà** toutes les guildes) — **0 migration**, colonnes conservées ; fiche `admin-marche` + `seed:docs`.
> **⑧ Cron `market-expire` « Inconnu »** : cause mesurée = `$CRON_SECRET` **vide** dans l'environnement de `cron` ⇒ **401** **avant** `recordCronExecution` (+ `>/dev/null` ⇒ aucun log disque ; + URL prod ⇒ Redis de prod). **Correctif** : `recordCronRefusal()` (trace **throttlée `NX EX 600`**) ⇒ le panneau affiche « **Refusé (401) : …** » ; crontab corrigée (**secret lu dans un fichier**, `-w '%{http_code}'`, **redirection du log**) + **tableau de diagnostic** en 5 points (`MAINTENANCE.md`).
> **Preuves** : suite **1303/1303** (126 fichiers) · `tsc` **0** · `eslint` **0 erreur** · **0 migration** · `seed:docs` ✅.
> ⏭️ **Nouvelles demandes user (lots séparés, 2 blocages à trancher)** : vraie **dernière étape « Publication »** (résumé + **estimation des personnes notifiées** + aperçu du salon) · **lot multiple jusqu'à 5 objets** (une carte de vente) + **multi-embed Discord** avec **boutons par objet** ⇒ **migration additive + interface Discord publique** : STOP et demande avant de coder.
## ✅ PR #663 MERGÉE — (fix Marché : **debug beta 1ʳᵉ vague**) → `dev` = **`a6c65031d`** — branche `fix/marche-debug-beta-14-09` (commits `b04ce15d9` · `f40877949` · `44ae7d395` · `4465eace7`, **10 fichiers**, +653 / −56 ; CI **verte** : `Verify & Build` **6 m 34 s** · CodeQL · Analyze · `Filter changed files` ; hook pre-commit vert ; **0 migration** ; branche supprimée)
> **Constat 1 — 🔴 « le bouton Offre de l'embed KO : ❌ Aucun profil SigilOS dans cette guilde »** (alors que le dashboard fonctionne). **Cause mesurée** : la soumission de la **modale** (`payload.type === 5`, `mkt:offer`) transmettait le **snowflake Discord** (`member.user.id`) comme `userId` ⇒ `resolveMemberContext` (`src/server/market/discord-interactions.ts:160`) ne trouvait aucun `UserProfile` (il lit l'**id interne** `User.id`) ; dans la même branche, le **gate RBAC** (`mkt → market:trade`) n'existait **que** pour `type === 3`. **Correctif** : `DISCORD_PERM_MAP` en **portée module** + gate unique **`isDiscordPrefixAuthorized()`** (fail-closed) appelé par **les deux** types + `account.userId` transmis à la modale. **Aucune règle métier dupliquée** (§13.4).
> **Constat 2 — 🧨 « +15975 Effet [15975] »** (*Pestilence de Corruption*, carte + image OG + « Jet déclaré »). **Cause mesurée en base** (sonde `_probe-effet-15975.mjs`) : `effectId` **1175** = **porteur de capacité légendaire** (`diceNum` = **id du pouvoir** `15975`, `diceSide` = 1) — **DofusDB l'écarte lui-même** de `effects` (**17** brutes / **16** normalisées) ; notre siphon copie la forme brute ⇒ **334 fiches** touchées. **Correctif (règle pure, 1 source)** : `NON_STAT_EFFECT_IDS` · `isNonStatNativeEffect` · **`isDisplayableNativeEffect`** (métadonnée `0 → 0` **+** ligne « pouvoir ») appliquées au **siphon** (`toNativeEffects`), à la **lecture client** (`enrichNativeEffects`), à l'**éditeur** (`buildNativeStatDrafts`) et au **catalogue** ; `isStatBearingStatRow` = **miroir** pour les lignes déjà persistées (**0 migration**).
> **Constat 3 — 🖼️ « icône assets ko : +5 % Mêlée (%) »** (+ « d'autres ko à signaler ? »). **Audit mesuré** (sonde `_probe-stat-icons-audit.mjs` contre `GameCharacteristic`/`GameEffect`) : `Mêlée (%)` (char **124**, `tx_resMelee`) et `Distance (%)` (`120`/`121`) **sans thème** ⇒ repli lucide ; **collision d'ids** `124` = *Sagesse* (effet) / *Mêlée (%)* (caractéristique) ⇒ carte **`THEME_BY_CHARACTERISTIC`** consultée d'abord ; **3 icônes fausses corrigées** (`82` Retrait PA, `84` Poussée, `88` Terre) + séries `414`/`416`/`417`/`418`→`421`/`240`→`244` complétées + motifs de libellé (« Mêlée », « Distance », « Poussée (fixe) », « Critiques (fixe) »).
> **Preuves** : suite **1293/1293** (125 fichiers, **+31**) · `tsc` **0** · `eslint` **0 erreur** (warnings `any` **préexistants**) · **0 migration** · 3 tests neufs + 1 mis à jour.
> ⏭️ **Reste de la vague** (diagnostiqué, non traité) : sélecteurs de rôles + panneau de durées de `Réglages → Marché` à retirer, timeline des offres (emplacement à trancher), désistement depuis la fiche, capacité légendaire en bleu, image OG (titre/ligne/pied). Détail : `debug.md`.

## ✅ DERNIÈRE PR MERGÉE — **#660** (fix Marché : **réservation nommée D49** + **densité des Pods Q15 tranchée**) → `dev` = **`b9079d02b`** — branche `fix/marche-reservation-nommee` (commit `ffcf93ade`, **7 fichiers**, +244 / −20 ; CI **verte** : `Verify & Build` **7 m 33 s** · CodeQL · Analyze ; hook pre-commit vert ; **0 migration** ; branche supprimée)
> **D49 (décision user « A1 »)** : l'embed d'une annonce `RESERVED` porte désormais **« 🔒 Réservé par X — jusqu'au JJ/MM/AAAA HH:mm »** (le bouton « Réserver au prix » était **déjà grisé** : une seule réservation à la fois, R3) · profil relu **dans la guilde de l'annonce** (isolation §16.2) · **0 requête ajoutée** hors `RESERVED` · repli neutre « Un membre de la guilde » (BUG-6) · lecture **best-effort** (un échec n'empêche jamais l'embed) · **aucune mention `@`**, jamais d'id Discord · ⚠️ **§13.7 amendé** : le **réservataire** est la **seule** identité publique (les **montants d'offres** et l'identité des **offrants** restent privés — testé). La fiche du dashboard était **déjà** conforme.
> **Q15 / `S4.0c` tranché par la donnée du jeu** (DofusDB) : « Rune Pod » `7443` = **1 PWR / +10 Pods** ⇒ densité **0,1 pt**, over max **1010** (valeur **déjà en production**) ; contrôles « Rune Vi » `1523` = +5/1 PWR ⇒ 0,2 · « Rune Ini » `7448` = +10/1 PWR ⇒ 0,1 ⇒ le « 0,25 » communautaire est **impossible** (+4 Pods n'existe pas) : c'est le **guide SEO** qui portait la coquille (**0,025 → 0,1**, colonne PWR alignée sur l'Initiative).
> **Preuves** : suite **1262/1262** (122 fichiers, **+9**) · `tsc` **0** · `eslint` **0 erreur** (1 warning **préexistant** `forumMode`) · **0 migration** · aucune donnée privée exposée.
> 🔎 **Résidus connus** : assets Dofus **A3 à valider** (`auctionHouse*` pour les familles de filtres, `icon_slot_*_forgemagie` pour la Forge) · 4 `<Image>` **bruts** de l'assistant (`market-create-client.tsx:1141,1456,1550,1681`) · nom du réservataire **absent de la carte du catalogue** (choix assumé : 0 requête par ligne).

## ✅ PR #659 — (fix Marché : **la fiche d'un lot devient vraiment compacte**) → `dev` = **`44a8952b0`** — branche `fix/marche-fiche-lot-compacte` (commit `5570f68b4`, **2 fichiers**, +106 / −34 ; CI verte `Verify & Build` **7 m 52 s** ; **0 migration** ; branche supprimée)
> **Constat user (capture, après déploiement de #657)** : « **un gros composant vide avec l'icône à droite, un vide immense au milieu et les infos écrites en petit à gauche** » ⇒ la #657 n'avait traité que la **moitié** (textes agrandis) en gardant la vignette **à droite**.
> **Correctif** : vignette **1ʳᵉ dans le DOM** ⇒ `flex-row` en compact (**64 px, à gauche, collée au texte**) / `flex-row-reverse` pour la composition « **tooltip Dofus** » (objet **avec jet**, rendu **inchangé**) · **contenu du lot** + **quantités** désormais **dans la colonne** (`text-body-sm`) · blocs du bas **réservés au mode « tooltip »** (`!compact`) ⇒ **plus aucun doublon**.
> **Tests** : `market-embed-images-by-jet` mis à jour (placement, colonne, tailles) ⇒ **1253/1253** · `tsc` 0 · `eslint` 0 · **0 migration** · captures locales (`_shot-fiche.mjs` : fiche lot desktop/mobile, équipement avec jet, catalogue, « Mon espace »).

## ✅ PR #657 — (fix Marché : **embeds & cartes selon la famille**) → `dev` = **`e54f332cc`** — branche `fix/marche-embeds-et-cartes-familles` (commits `2e987985e` + `a89bd9e8e`, **8 fichiers**, +365 / −47 ; CI **verte** : `Verify & Build` **7 m 45 s** · CodeQL · Analyze · `Filter changed files` ; hook pre-commit vert ; **0 migration** ; branche supprimée)
> **Constats user (4 captures)** : icône manquante (cube gris) pour les **lots** dans le catalogue · fiche d'un lot = « espace vide énorme » + textes minuscules · embed cosmétique / lot = **carte inutile + cadre objet vide** (« affiche juste en gros la miniature de l'item ») · carte OG d'équipement = « **+3 autres lignes** » tronquées.
> **Correctifs** : **`pickMarketEmbedImage()`** (pur) ⇒ carte PNG **seulement** s'il y a un **jet réel** (`isStatBearingStatRow`), sinon **image de l'objet en grand** (les **deux** points d'entrée Discord) · carte OG = **toutes** les lignes, **`statRowMetrics()`** (compression par paliers >12/>18/>24), **hauteur dynamique**, **image du 1ᵉʳ composant** pour un lot · icône de lot au **catalogue** (carte + tableau) et dans **« Mon espace »** · `MarketItemCard` **compacte** sans jet (vignette 80 px centrée, textes agrandis — **corrigée par #659**).
> **Preuves** : suite **1253/1253** (122 fichiers) · `tsc` **0** · `eslint` **0 erreur** · **mesure en direct** (`GET /api/og/market/[id]`, Sharp) : 18 lignes ⇒ **760 × 592**, 16 lignes ⇒ 760 × 560, lots ⇒ 760 × 560 · `npm run build` **délégué à la CI** (`next dev` local actif).

## ✅ PR #655 — (fix Marché : **lignes de métadonnées `0 → 0`**) → `dev` = **`31305cdb4`** — branche `fix/marche-lignes-metadonnees` (commit `565888447`, 9 fichiers ; CI verte `Verify & Build` 6 m 22 s ; **0 migration** ; branche supprimée)
> **Constat user (capture)** : une **monture d'apparat** (« Gladius Moldus ») affichait « Effets · +0 Échangeable : [0] · +0 Compatible avec : [0] » **et** la publication échouait sur le toast « … (Cosmétique) se vend tel quel : aucune statistique ne peut être déclarée » — cul-de-sac, l'éditeur de jet étant **masqué** pour cette famille.
> **Cause racine mesurée en base** (sonde gitignorée `_probe-cosmetic-noise.mjs`, objet `23559`) : `GameItem.nativeEffects` = `983` « Échangeable : » + `1179` ×4 « Compatible avec : », **tous** `0 → 0` ; portée **3 330** + **2 603** fiches (**4 203** `equipment`, **2 196** `cosmetics`) ; **0** ligne `0 → 0` persistée (`MarketListingStat`) ⇒ bruit **produit à la volée**.
> **Correctif** : `isStatBearingNativeEffect()` / `isStatBearingStatRow()` (`src/lib/market/effects.ts`) appliqués au **pré-remplissage** (`buildNativeStatDrafts`), à l'**écriture** (`resolveServerStats` ⇒ `guardItemFamilyPolicy` ne compte plus de stats fantômes : **le cosmétique redevient publiable**) et à la **lecture** (`withDisplayReadyStats`, `getMyMarketData`, route **OG**, composant `MarketStatLines`) ; l'assistant n'affiche/n'envoie `declarableStats` (`statEditorAllowed`). Tests : `market-effects` **38** + `market-metadata-lines` (**neuf**) ⇒ suite **1244/1244** ✅ (121 fichiers) · `tsc --noEmit` **0** ✅ · `eslint` **0 erreur** ✅ · **0 migration**.

## ✅ PR #650 — (fix Marché : **dernière itération** — **T4** défauts globaux God · **T1** garde de contrats action↔consommateur · **T5** test flaky rendu déterministe · **constats beta 1→6** : icônes, fiche d'item, offres sous réservation, embed/carte OG, « Mon espace », tutoriel) → `dev` = **`44346caa7`** — branche `feat/marche-iteration-finale-2` (commits `c1701acf2` · `f0644f32c` · `a0e79638f` · `0b96e0571` · `1f8c2ed7a`, **22 fichiers** ; CI **verte** : `Verify & Build` 6 m 14 s · CodeQL · Analyze ; hook pre-commit vert ; **0 migration**)

> **Session en cours :** **terminée** — la **dernière itération du Marché** est livrée et mergée (**#650**). Il ne reste que des **ops côté user** : déployer · `npm run seed:docs` + `npm run build:seeds` · crontab `market-expire` (VPS) · **re-siphon** `GameItem.typeId`/`superTypeId`/`superTypeName` (panneau de siphon) · DoD visuelle dark/light/mobile · S4.12 (beta 2 comptes) · Q15 (capture de la rune) · tags du forum · libellés S1.25. **T15 (BUG-10, CSS `text/plain`) = infra** (purge cache/CDN + contrôle du service `_next/static`) : **aucun correctif applicatif**. **T7/V1.1 (`WANTED` + matching) = hors itération** (lot séparé, sa PR, sa migration additive). **⚠️ 0 migration.**

## ✅ PR #648 — (fix Marché : **itération finale** — BUG-3→BUG-11 + T3/T6/T9/T10/T11/T12/T13/T14) → `dev` = **`75e7d0193`** — branche `feat/marche-iteration-finale` (commit `7e2bbcecc`, 43 fichiers)

> **Merges récents :** **#642** (S8 lot 3 « Discord & UX ») · **#643** (S8 lot 4 « Supervision & clôture » — **MVP Marché fermé**) · **#644** (clôture : `S3.13` tags forum · RBAC · carte Discord) · **#645** (sélecteurs de rôles vides) · **#646/#647** (docs recette beta) · **#648** (itération finale — **code**).
> **Session en cours :** **dernière itération du Marché — clos sur le code** : **T4** (défauts globaux God) ✅ · **T1** (garde contrats action↔consommateur) ✅ · **T5** (test flaky → déterministe, jamais désactivé) ✅ · **constats beta 1→6** ✅ (icônes normalisées à l'écriture **et** à la lecture · fiche d'item en **un seul bloc** + garde « un lot n'a jamais de jet » · offres **déposables sous réservation** + **« Lever la réservation »** (acceptation refusée tant qu'une réservation est active) · **miniature d'embed toujours fournie** + carte OG sans « ? » + objet en grand sans stats · « Mon espace » avec **vignette** et jet résumé · **tutoriel mesuré/recadré + navigation d'étape**). Reste : **T15** (infra) · **re-siphon** (ops) · **ops** §4. **⚠️ 0 migration** (T7 `WANTED` = **hors itération**).
> **Livré (vérifié) par #648 :** ✅ **BUG-3** icônes (1 seule URL = proxy auto-siphon, `src/lib/market/item-image.ts`) · ✅ **BUG-4** les 6 PNG de stats manquants + test d'intégrité · ✅ **BUG-5** carte Discord **tout-en-image** (data-URI, icône Kamas, WebP→PNG Sharp, **0 stat en texte**, `src/lib/market/og-assets.ts`) · ✅ **BUG-6** profil absent ≠ inactif · ✅ **BUG-7** bouton « Contacter » **supprimé** · ✅ **BUG-8** bouton **« Me désister »** + resync + **`rateLimit` fail-closed** sur les boutons/modale · ✅ **BUG-9** rendu **déterministe** (`format-date.ts`, `formatGroupedInteger`, 7 composants purgés de `toLocale*`) · ✅ **BUG-11/T10** familles **`typeId` → Équipements / Cosmétique / Ressources-Autres** + politique Forge/jet/lot/légendaire + **garde serveur** · ✅ **D-A** interrupteur « Preuves » retiré · ✅ **D-E** `@everyone` filtré UI **et** serveur · ✅ **régression catalogue corrigée** (filtre **NULL-safe** `src/lib/market/family-where.ts` ; cause mesurée en base : `superTypeId` **NULL partout**, siphon réparé) · ✅ UI : lignes de stats **2 colonnes**, objet **112 px**, repli d'icône sur le 1ᵉʳ composant du lot.
> **État du chantier :** module Marché **fonctionnellement complet** + **itération finale avancée** (les 9 bugs beta sont fermés). Il reste les **4 tâches ci-dessus** + **ops** ⇒ `refonte-marche/ITERATION-FINALE-MARCHE.md` (**§0bis** = état après #648 · **§3** = tableau des tâches) · **amorce à coller** : `refonte-marche/AMORCES-A-COPIER.md` § « ⏭️ MAINTENANT — SUITE APRÈS #648 ».
> **Règle de session (ratifiée 10/09) :** tant qu'une PR est **ouverte**, on la **surveille jusqu'au merge** (`gh pr checks <n>` · `gh pr view <n>`).
> **Contexte de reprise :** plan `refonte-marche/PLAN-MAITRE-MODULE-MARCHE.md` · état + **§0 sécurité bloquante** `refonte-marche/REPRISE-FIN-MODULE-MARCHE-ETAT.md` · règles projet `RULES.md` (§ *Security*) + `SECURITY.md` + `.agents/workflows/git-push.md`.
> **⚠️ Donnée à rattraper (ops)** : `GameItem.typeId`/`superTypeId`/`superTypeName` — le siphon est **réparé** (lit `raw.type.*`) mais les fiches **existantes** gardent `superTypeId = NULL` ⇒ panneau God « familles » **vide** tant qu'un **re-siphon** n'est pas relancé. La **recherche du Marché** n'en dépend plus (elle est pilotée par `typeId`).

### Fichiers à surveiller sur cette PR (les toucher ⇒ relire les règles ci-dessous)
- **CI/CD** : `.github/workflows/verify.yml` (Lint → tsc → bot tsc → tests → build) · `.github/workflows/deploy.yml` (image construite sur runner GitHub via buildx) · `Dockerfile` (stage `builder` = `npm run build`).
- **Schéma/données** : `prisma/schema.prisma` + `prisma/migrations/**` (**idempotence obligatoire**) · `src/lib/module-types.ts` · `src/server/actions/module-actions.ts` (**`GOD_LOCKABLE_MODULES`**) · `src/lib/permissions.ts` · `src/server/actions/user-actions.ts` (**`applyModule` = piège n°1**).
- **Module Marché** : `src/server/actions/market-actions.ts` · `market-admin-actions.ts` · `market-constants.ts` · `god-market-actions.ts` (**super-admin only**) · `src/server/market/{lifecycle,incidents,expiry,reservations,offers,maintenance,retention,discord}.ts` · `src/lib/market/{kamas,stat-quality}.ts` · `src/app/dashboard/[guildId]/marche/**` · `src/app/god/market/**` · `src/app/api/cron/market-expire/route.ts`.
- **Docs & tour (obligations RULES.md)** : `src/lib/docs-catalog.ts` (**la source** — puis `npm run seed:docs` **et** `npm run build:seeds`) · `src/components/tour/tour-provider.tsx` (+ `MODULE_TOUR_PHASES`) · `src/components/tour/module-tour-replay-button.tsx`.
- **Fixtures sensibles aux modules/permissions** : `tests/unit/onboarding-console.test.ts` (`allOn()`).

### Pièges connus (brief du 10/09/2026)
- `prisma migrate dev` **échoue en local** (migration `20260819000000_add_inter_guild` appliquée en base mais absente du repo → Prisma propose un **reset destructif**) ⇒ migration **écrite à la main (idempotente)** + `prisma db execute --file`.
- **Plafond mémoire Node ~2 Go** : `tsc --noEmit` et `next build` (« Running TypeScript ») tombaient en **OOM** ⇒ heap porté à **4 Go** (`NODE_OPTIONS` dans `verify.yml` **et** `Dockerfile`), commit `66ffdd724`.
- **Tests à fixtures de date figée = bombes à retardement** ⇒ toujours une référence **relative** (`Date.now() - H`).
- `npx prisma` sans version installe la **v8 RC** ⇒ toujours `prisma@7.10.0` · PowerShell : `[guildId]` est un wildcard ⇒ `-LiteralPath`.
- **Déploiement VPS bloqué par un artefact de siphon** : `public/game-data/dungeon-monsters.json` est **versionné** (commit `ca7aed455`) **mais régénéré au runtime** (cron `sync-monster-stats` + bouton God) **à travers le bind mount compose** `~/SigilOS/public/game-data:/app/public/game-data` ⇒ le fichier est « modifié » côté serveur et **`git pull` abandonne** (`Your local changes … would be overwritten by merge → Aborting`). **Débloqué** par un garde-fou dans `scripts/deploy.sh` + `scripts/deploy-cd.sh` (`GENERATED[]`, PR **#628**). **Dette structurelle recommandée** : écrire l'artefact dans un chemin **non versionné** (volume dédié) et adapter `dungeon-monsters-siphon` (OUTPUT_PATH) + lecteurs (`game-data-actions`, `data-health-actions`) + mount Caddy statique — ou cesser de versionner le fichier (`.gitignore` + `git rm --cached` + génération au deploy).

## Session 2026-09-09 (nuit) — Landing boss + Status Discord + Ko-fi + Prod bot + Gitops
> Branches : `feat/chantier-2026-09-08-titans` (PR **#613 MERGÉE** dans `dev` → déployée beta `4dc682f7`) · `fix/migrations-idempotentes` (PR **#614 OUVERTE** → `dev`).
- ✅ **Landing `/boss` parité module Succès** (`cacc71686`) : filtre **Boss / Titans** + badge TITAN (catalogue) · fiche boss **et** titan (page résout `Dungeon` **ou** `Titan`, pré-charge sorts+famille+maps serveur) · onglets Sorts / Sorts détaillés / Simu + choix map / Grades / Drops (modale) / Monstres de salle · stats PV-PA-PM + résistances · `/travel` · `SpellRangeGrid.allowFreeCasterMove` (bypass boss libre opt-in public, dashboard épinglé inchangé).
- ✅ **Status Discord anti-spam** (`fae7b556c`, partie core déjà sur dev via #611/#612) : cause = living-status retombé en création à chaque tick (ID stocké jamais réutilisé : TTL 30 j expirée 09/08→08/09 + aucune validation) + `statusFrequency` God jamais lu. Fix = **garde-fou fréquence** (`shouldSkipStatusPing`, marge 45 s, fail-soft si Redis KO) + validation **snowflake** (un `outbox:<jobId>` ne casse plus le PATCH — important car `DISCORD_OUTBOX_ENABLED=true` sur beta) + worker tick **5 min** + nettoyage anciens schedulers + TEST PING `force:true` + embed **vulgarisé** (Bot/Site/Données, zéro techno) + God : sélecteurs mode living/notif + lite (déjà persistés, sans UI avant).
- ✅ **Ko-fi E2E** (`fae7b556c`) : vraie cause = URL Ko-fi pointait `/api/kofi` (inexistant, 404 silencieux) → **`/api/webhooks/kofi`** · 401 suivant = **`proxy.ts` bloquait `/api/webhooks/*`** (pas dans `isPublicApi`) → ajouté · token robuste aux guillemets `.env` · remerciement public `#DONS-KOFI` via `kofiChannelId` (God, migration `20260909014154`) + **ping @** si 1 seul profil Discord matché (jamais d'ambigu) + `is_public:false` respecté. Tests : 7 kofi + 9 guard.
- ✅ **Prod bot ressuscité** : crash-loop 141k restarts, `TokenInvalid` — cause double : token partagé beta+prod (bagarre de sessions gateway) + mdp BDD divergent (`DATABASE_URL` vieux mdp vs `POSTGRES_PASSWORD` + spéciaux non encodés). Fix = **2e appli Discord `SigilOS Prod`** (token+AppID+secret+clé Ed25519 dédiés, intents Members+MessageContent, invite bitmask `6356836904068`) + mdp **alphanumérique** réaligné (`ALTER USER` + `.env.prod` + recreate). Tout healthy.
- ✅ **Gitops** : 6 commits titans (boss, kofi+status, worldmap, purge J+7/14/21, divers, docs) + PR #613 mergée · **10 branches remote mergées supprimées** (reste : dev/main/inter-guilde + `onboarding-suite` avec 1 commit unique à trancher : `inline-onboarding-steps.tsx` — étapes inline sans navigation, greffe peu coûteuse mais redondante) · stash `wip-unrelated` trié et droppé (24/25 déjà absorbés, 1 helper orphelin sauvé en temp) · local 100 % poussé (vérifié : worktree vide, 0 stash, ahead=0 partout).
- ⚠️ **Dette soldée ensuite** : migrations `IF NOT EXISTS` (PR #614, protège le futur deploy prod après le P3018 beta — colonne déjà créée hors migrations → `resolve --applied`).
- ⚠️ **Leçons PowerShell session** : `>` écrit en UTF-16 (patches git corrompus — passer par `cmd /c` ou .NET) · `edit` échoue sur fichiers CRLF (normaliser LF d'abord) · `.env` relu uniquement au boot (`next dev` restart obligatoire) · `npx prisma` SANS version installe la v8 RC → toujours `prisma@7.9.1`.
- ✅ Vérifs : `tsc` 0 · **587/587** tests.

## Chantier du jour (2026-09-08) — Module « Titans » (feat. de bout en bout)
> Branche `feat/slash-rework` — **non commité**. Mémo : `memo-2026-09-08-titans.md`.
- ✅ Modèle `Titan` + `UserTitanProgress` + `DjSearchMode.TITAN` + champs DJ (`titanId/titanName/questName/questUrl`) ; Admin God `TitanManager`, server actions `titan-admin-actions`/`titan-actions`, seed Gargandyas (**8062**, Osavora, dispo WE 19h→8h, 5 victoires max).
- ✅ `SuccesTitanTab` **parité fiche boss** : onglets **Stats & Sorts / Simulation (`SpellRangeGrid`) / Monstres de salle**.
- ✅ Résolution Gargandyas validée (id **8062**, ⚠️ homonyme 8069) · **icônes** = set **déjà officiel** (aucune intégration d'assets desktop, conforme règle « proposer puis valider ») · barre de vues Succès en **assets Dofus** + « Fiche Titans » à côté de « Fiches Boss ».
- ✅ DJ Titan : **taille FIXE = titan.maxMembers** (modale + `createDjPost` force) · **date/heure calquée sur `scheduleConfig`** (`DateTimePicker` allowedDaysOfWeek+hourRange, fenêtres nocturnes, verrou sélection) · cron `cleanup-inactive-posts` couvre TITAN · file d'attente déjà en place.
- ✅ UI /boss : bouton « Overlay en jeu » retiré des cartes + amber→`warning` · **overlay fix flèche retour** (deep-linked 1 seule fois + clear search).
- ✅ ~~`SpellRangeGrid.tsx` syntaxe JSX cassée~~ → **RÉSOLU (vérifié 09/09)** : `tsc` 0 sur le repo, fichier sain + prop `allowFreeCasterMove` ajoutée (bypass boss libre landing publique).
- ⚠️ Validation : mes fichiers **eslint 0 erreur** ; `tsc` vert.

## Chantier du jour (2026-09-04, suite) — Rush Sylvestre : refonte visuelle anti-slop + fil conducteur imagé
> Branche `feat/chantier-2026-09-04-cyber-rescan` — **rush non committé** (commit + PR `dev` à faire).
> Référence vs concurrent **Duffus** (capture) : consigne **inspiration, pas recopie** (100 % assets locaux).
- ✅ **Bug header ressources sidebar** (`RushChapterSidebar.tsx`) : titre écrasé → header 2 lignes, toggle compact, libellé Total/Encore selon mode, quantités `×`.
- ✅ **Fil conducteur** : ancres œufs 40px jalons liés + vignettes 32px chapitres (`RushTimelineClient.tsx`) ; strip `DofusProgressStrip` **branché puis retiré** (doublon + couverture `dofusId` trop faible — fichier conservé).
- ✅ **Panneau guilde** (`GuildStatusPanel.tsx`) : clic = filtre timeline + détail membres en ligne (top 8 + déplieur) ; modale vide supprimée ; **fix `%` jamais calculé**.
- ✅ **Hero diet** : 4 cartes KPI → 1 barre de statut ; boutons ghost unifiés ; **Signaler → icône** ; **Overlay → seul primaire** ; Niv.200 conservé.
- ✅ **Ambiance** : halo hero + glow œuf teintés au Dofus actif (CSS statique, 0 coût) + **particules** `GuideParticles` teintées + toggle Options persisté (défaut ON, reduced-motion OK).
- ✅ **Feedback overlay natif** (`RushOverlayFeedbackPanel.tsx`, nouveau) : le `Dialog` Radix portalait vers le document principal → formulaire `fixed` viewport overlay ; modale dashboard intacte.
- ✅ **Vérifs** : `tsc` 0 · `eslint` 0 erreur · 64/64 tests rush (`test:run` complet + `build` **non faits**).
- 🔴 **Reste n°1 (data, côté God)** : **backfill `dofusId`** (sélecteur existant) — débloque strip + panneau + ancres + vignettes. Puis check visuel + commit/PR.
- 📄 Mémo : `memo-2026-09-04-rush-refonte-visuelle.md` · Maquettes : `refonte-rush-sylvestre-2026.html`, `refonte-guide-sylvestre/09-refonte-ui-ux-2026.md`.

## Chantier du jour (2026-09-04) — Rush Sylvestre : Pense-bête + modale de lancement + config GOD (PR #588)
> Branche `feat/chantier-2026-09-03-rush-ui-pense-bete` (9 commits) → PR #588 (base `dev`), **MERGEABLE**.
- ✅ **Retrait bouton + modale « Ressources » du dashboard** (conservés dans l'overlay).
- ✅ **Pense-bête** : bouton + modale **lecture seule** (liste des choses à préparer, une seule croix de fermeture).
- ✅ **Modale de lancement (4 étapes)** : choix perso → **préparation** (Metamob détecté + bouton « Lier », reset alignement optionnel, **métiers requis** déclarés ✓ / non déclarés → bouton « Déclarer ») → avancée membres → prêt. **Reset « Réinitialiser ce personnage »** ré-ouvre la modale ; **reset d'un autre perso (main + mules)** depuis la modale.
- ✅ **Config UI/UX éditable GOD** : `OptimizedGuide.rushUIConfig` (JsonB) + migration `20261013000000_add_rush_ui_config` + action `updateRushUIConfig` (audit `GOD_RUSH_UI_UPDATE`). **Onglet GOD « Lancement »** : aperçu + éditeur pense-bête (CRUD sections/items, métiers + **alternative**, **réordonnancement ↑/↓**) + toggles modale. **Fallback statique** si config vide. Côté membre : `resolveRushUIConfig`.
- ✅ **UI polish** : badges activité « pack +N » sur le dashboard (`classifyTags`), **célébration au changement de chapitre** (jade), **checkboxes 3 états** (à faire / en cours doré / terminée jade).
- ✅ **Bloc alignement** → lien vers `/dashboard/[guildId]/profile` (plus d'édition inline).
- ✅ **Modale Ocre +/−** : re-render immédiat + patch Metamob en direct (état local).
- ✅ **Fix** crash `AlignmentSection` (profil) — garde-fou `ORDERS[selectedAlignment] || []`.
- ✅ **Prisma** : migrations locales bloquantes résolues (`20261009`-`20261013`) → `Database schema is up to date!` (colonne `rushUIConfig` en base).
- ✅ **Vérifs** : `tsc` 0 · `eslint` 0 · vitest 484/484 · `npm run build` OK.

## État actuel (chantier Rush Sylvestre — session UX/sync)
- ✅ **Module Rush Sylvestre** (guide) **fonctionnel** — S4 « qui peut aider » (métier+niveau), S6 micro-célébration, S5 éditeur GOD (import icônes), S7 contexte chapitre (PR #571–#576 mergés dans `dev`).
- ✅ **Positions GPS copiables** dans l'overlay (liste, modale détail, objectif courant) via `RushCoordinateChip` + helper central `getSequenceCoord` (priorité `pos_tags` > titre > tips > note) dans `rush-guide-utils.ts`.
- ✅ **Dashboard aligné sur l'overlay** : modale **Ressources** globale (bascule **Restantes/Toutes** via `aggregateRushResources`), badge Main/Mule, fix `/travel` **2D** (plus de `worldId` parasite), suppression anciennes stats-pills, sidebar chapitre (rail). **Sync chapitre** : clic sur un header de chapitre (feed) → sélection auto dans la sidebar.
- ✅ **Copie du nom** d'une ressource (sans le chiffre) → modale ressources / liste overlay / sidebar chapitre (helper `copyToClipboard`).
- ✅ **Fix clipboard overlay** : `RushCoordinateChip` consomme `src/lib/clipboard.ts` (fallback `execCommand` pour webview / PiP) → n'affiche « Copié » que sur succès réel.
- ✅ **Ressources 200 → 515** : cause racine = séquence « Ressources à prévoir » **absente en base** (seed non idempotent). Corrigé en base (insert 477 items) + **seed rendu idempotent** (CLI `seed-rush-sylvestre-cli.mjs` + action serveur `seedRushSylvestreFromGuide`).
- ✅ **Item tags** : résolution accent-insensible + dictionnaire d'alias → unresolved **38→15** ; enrichissement propagé aux séquences DB.
- ✅ **Images ressources LOCAL-FIRST** : `resolveItemImage` sert directement le WebP siphonné `/uploads/assets-dofus/items/{id}.webp` (0 appel proxy, **0 dépendance DofusDB**) ; fallback `getItemImageFallback` → proxy (siphon à la volée). Composant partagé `ResourceImage` (`onError` → proxy). Les `249` items enrichis en remote DofusDB deviennent autonomes.
- ✅ **Modale Ressources « Restantes » décrémente** : cause = le dashboard passait `completedIds` (IDs de **chapitres**) à `aggregateRushResources` (filtré sur IDs de **séquences**) → rien n'était exclu. Corrigé : `allCompletedSeqIds` (séquences validées, en incluant les chapitres validés d'un coup) utilisé pour `resourcesRemaining` ; `gateAllCompleted` (overlay) inclut aussi les séquences des chapitres complétés d'un coup. **Synchro live** (BroadcastChannel).
- ✅ **Overlay responsive / compact** :
  - Scroll des quêtes **visible** : `<main>` passe en `min-h-0 overflow-y-auto overscroll-contain custom-scrollbar` (le `flex-1` seul ne scrollait pas, `min-height:auto`).
  - Panneau « À faire maintenant » **repliable** + chips en **rangée unique** (flex-nowrap, plus de wrapping) ; **auto-repli** quand l'overlay est réduit (`defaultCollapsed`).
  - Header **compact** : masque le label « Guide de progression » + la ligne perso, paddings réduits, **menu ⋯** regroupant les actions secondaires (tutoriel/guide/reset) — détection fiable de la taille réelle via **ResizeObserver** sur le conteneur (plus de `window.matchMedia` trompeur).
- ✅ **Validation** : `tsc` 0 · `eslint` 0 erreur (fichiers touchés) · `vitest` **484/484** (dont tests `resolveItemImage`/`getItemImageFallback` et `aggregateRushResources`).

## PR / Branche
- **Session UX/sync** → branche **`feat/rush-sylvestre-ux-v2`** (créée depuis `origin/dev` = `b185e5e61`, après merge PR #586) → PR vers `dev`.
- Merge propre : aucun chevauchement de fichiers avec `dev` (seuls `package-lock.json` + `sync-actions.ts` différaient dans `origin/dev`).

## NEXT (priorités — à faire)

### 🛒 Marché — état au 14/09/2026 (3 PRs mergées : #657 · #659 · #660 · **+ debug beta en cours**) — **0 tâche de code ouverte au plan §21**
> **Vague de debug beta (5ᵉ tour)** — branche `fix/marche-debug-beta-14-09` : **3 constats fermés** (offre Discord/RBAC, ligne fantôme « +15975 Effet », icônes Mêlée/Distance + audit) ⇒ suite **1293/1293** · `tsc` 0 · `eslint` 0 erreur · **0 migration**. **Constats restants** (diagnostiqués, mesures dans `debug.md`) : `Réglages → Marché` (3 sélecteurs de rôles + panneau « Durées, plafonds & rappels » à retirer — le God est déjà global, `updateMany` cross-guild **mesuré**), timeline des offres sous la fiche (**emplacement à trancher par le user**), désistement depuis la fiche du dashboard, capacité légendaire (texte du pouvoir à sourcer), image OG (titre `EFFETS` recouvert, ligne horizontale, pied « Annonce #… / vendeur » à retirer).
> Plan maître §21 : **168 cases cochées** / **16 non cochées** = 4 DoD-ops (`S1.25`, `S1.34`, `S4.0e`, `S4.12`) + 3 historiques (`S0.7`, `S0.8`, `S4.13`) + **9 = V1.1 `S6`**. ⇒ code **clos** ; il reste des **ops**, **2 validations** et **1 PR docs** (celle-ci).
- ⏳ **Ops user (bloquant le gel §6)** : **déployer** `b9079d02b` (`sudo ./scripts/deploy-cd.sh beta <sha>`) · **crontab `market-expire`** (`MAINTENANCE.md:65` ; sans elle : ni expiration, ni rappel J+7/J+15, ni retrait J+20, ni purge) · **DoD visuelle** dark/light/mobile + `/god/market` · **`S4.12`** (2 comptes Discord, double clic simultané) · **tags du salon forum** + association (`Réglages → Marché`) · **libellés `S1.25`** · bouton God **« Rattraper les effets natifs »** · (facultatif) bouton God **« Siphon items »** pour remplir `superTypeId` (**21 748 NULL** aujourd'hui ; le Marché tient déjà via le repli `typeId` → `typeName`).
- 🎨 **Assets A3 (à valider avant intégration)** : `uiassets_assets_*/auctionHouse{Equipments,Cosmetics,Resources,Consumables}.png` (familles de filtres) · `uidarkstone_assets_all/icon_slot_rune_forgemagie.png` + `uiassets_assets_2x/auctionHouseRunes.png` (bloc Forge) · `icons_assets_2x/rune.png` / `shop.png` (option). `kamas.png` / `hammer.png` = **déjà intégrés**.
- 🔎 **Résidu code (option)** : 4 `<Image>` sans repli d'erreur (`market-create-client.tsx:1141,1456,1550,1681`) → harmoniser sur `MarketItemIcon`.
- 📌 **Infra** : `T15` (CSS `MIME text/plain` = purge cache/CDN, **aucun code**) · `npm run seed:docs` / `build:seeds` sont **inutiles sur le VPS** (le `Dockerfile` les exécute au build, `docker-entrypoint.sh` joue les seeds au boot).
- 🚫 **Hors module** : V1.1 = **`WANTED` + matching seulement** (chantier séparé, sa PR, sa migration additive) — le reste de `S6` est **écarté**.
- 🔬 **Harnais réutilisables** : `_shot-fiche.mjs` (JWT Auth.js **forgé** + Playwright sur `next dev` : captures réelles dark/light/mobile) · `_probe-og-card.mjs` (carte OG + Sharp) · `_probe-marche-etat.mjs` (comptages en base) · `_probe-cosmetic-noise.mjs` (bruit `0 → 0`). ⚠️ Piège PowerShell : les dossiers `[guildId]` sont des **caractères génériques** ⇒ `-LiteralPath` obligatoire.

- ⚠️ **Prod/Infra** — `prisma migrate deploy` sur l'env **déployé** : en local c'est réglé (`resolve --applied` sur `20261009`-`20261013`). À refaire là-bas si même cause « already exists ».
- 🔴 **Ops** — Ménage `Songes_Pour_Les_Noobs/` (177 f, 8.4 Mo) → `git rm -r --cached`.
- 🟠 **Data** — 15 `unresolved` restants (Feuille de Salace, Mesure de poivre, Bière du Chabrulé, Poiskaille en Fricassée, Kamas, « L' », phrases « ou 1 x »…) : pas d'équivalent `name.fr[]` DofusDB → mapping manuel ou acceptation.
- 🟠 **Data** — Intégrer les 15 quêtes « Apprentissage » (Ordres) : hors des 374 séquences ; mapping `alignReq`/camp requis.
- 🟡 **UI** — Liens NOOBS/DOFUSDB → modale (aujourd'hui nouvel onglet).
- 🟡 **UI** — Responsive : auto-replier aussi la barre de chapitre / le bloc recherche sur très petite hauteur ; mode compact en fallback auto très petit.
- 🟡 **Perf** — Factoriser `getSequenceHelpers` par guide / agréger « qui peut aider » par chapitre.
- 🟣 **Tech** — Nettoyer `as any` (~70 admin, ~64 timeline) ; confirmer suppression `RushOverlayQuestPanel.tsx` (orphelin).

## Références clés
- 🎨 **SOURCE D'ICÔNES OFFICIELLE (permanente, tout module)** : `C:\Users\user\Desktop\dofus_assets` (~23 770 fichiers : icônes 1x/2x, items 2x, sorts, monstres, UI…). On peut y piocher des icônes pour n'importe quel module, **UNIQUEMENT après les avoir proposées au user ET validées** avant intégration.
- Backlog canonique : `docs/ROADMAP.md` (à lire en premier en mode plan).
- 🧭 **Amorce de session générique** : `.agents/workflows/session-amorce.md` (contexte + **checklist sécurité non négociable** + méthode de preuve + vérifs + DoD + **bloc à coller** §10) — copie prête à copier : `AMORCE-GENERIQUE-A-COLLER.txt`.
- 🛒 **Marché — amorce de la phase de debug** : `refonte-marche/AMORCE-DERNIERE-PHASE-DEBUG.md` (+ `AMORCE-DEBUG-A-COLLER.txt`).
- Demandes ouvertes + annotations : `src/temp/chantier-actif.md`.
- Plan maître #223 : `refonte-long-terme-discord-compatibilite/PLAN-MAITRE-RESILIENCE-DISCORD-LONG-TERME.md` + `sigilos-discord-resilience.md`.
- 🛰️ **Indépendance totale (fiches boss/monstres, simulation, icônes) — AMORCE PRÊTE (15/09/2026)** : `amorces/amorce-2026-09-15-independance-totale.md` — 6 lots (stale-while-offline → TTL par dataset → `local-only` strict → icônes siphonnées + cache durci → siphon **tous les monstres** → fraîcheur visible), 6 décisions à trancher (D1→D6, recos incluses), **critère « test de panne »** (`DOFENSIVE_OFFLINE=1`/`DOFUSDB_OFFLINE=1` : zéro spinner, zéro icône cassée, zéro appel sortant). Contexte : local-first **pas** local-only ⇒ une ligne périmée (> 24 h) déclenche une bascule live silencieuse (c'est l'incident Dofensive down). `refonte-siphon/PLAN-REFONTE-SIPHON.md` §5.3 (limiteur anti-ban) s'applique aux lots 4-5.
- Dernier mémo détaillé : `memo-2026-09-14-marche-d49-q15.md` (D49 + Q15) · `memo-2026-09-14-marche-embeds-cartes.md` (#657/#659) · archives : `memo-2026-10-06-chantier-fiche-boss.md`.

## Vérifications globales habituelles
`npx tsc --noEmit` 0 · `npm run test:run` vert · `npm run build` OK · pas de `console.*` en prod.
