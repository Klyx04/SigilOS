{
    email admin@sigilos.fr
}

(basic-auth) {
    basic_auth {
        # Utilisateur: sigiladmin
        # Le mot de passe sera à définir via une commande sur le VPS pour plus de sécurité
        sigiladmin JDJhJDE0JHZHL0ZidXBSWllMdnV2UmVwWWVUTU9vV0ZidHAueDlkWllOOC5Ld0tSTmR3VjFSRlBvS08y
    }
}

sigilos.fr {
    # Prod coupée (Commentée pour l'instant)
    # reverse_proxy sigilos-prod:3000
    respond "Maintenance en cours. SigilOS revient bientôt." 503
}

beta.sigilos.fr {
    import basic-auth
    reverse_proxy sigilos-beta:3000
}
