{
    email admin@sigilos.fr
}

(security_headers) {
    header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
    header X-Content-Type-Options "nosniff"
    header X-Frame-Options "DENY"
    header Referrer-Policy "strict-origin-when-cross-origin"
}

sigilos.fr {
    import security_headers
    respond "Maintenance en cours. SigilOS revient bientôt." 503
}

beta.sigilos.fr {
    import security_headers

    # Serve uploaded files (proofs, achievements, guilds) from Docker volume
    handle_path /uploads/* {
        root * /app/public-beta/uploads
        file_server
    }

    # Beta application
    reverse_proxy sigilos-beta:3000
}

monitor.sigilos.fr {
    import security_headers
    reverse_proxy sigilos-grafana:3000
}
