# Active Context — SigilOS

> 📌 **Rotation (depuis le 20/09/2026)** : cette page ne garde que les **6 dernières sessions**.
> Les blocs plus anciens (26 au 20/09/2026) étaient archivés hors dépôt — ces archives ont été
> **supprimées le 20/09/2026** (`docs/arbo/ARCHIVES-TEMP-2026-09.md`) ; ils restent **intégralement**
> consultables via `git log -p -- docs/agents/activeContext.md` (l'historique git n'est pas concerné).
> En fin de session : ajouter le nouveau bloc EN HAUT, et **sortir le 7ᵉ** (récupérable via `git log`).
## 🧩 Session 25/09/2026 (SEO, lot 3 + guide Sylvestre) — **hreflang trompeur retiré · pages clés contrôlées au déploiement · case `Caddyfile` de `main` · rotation d'`activeContext` · le contenu du guide Sylvestre est verrouillé derrière un accordéon** → branche `fix/seo-derniers-restes`
> **Demande** : « c'est à faire ou pas ça ? » puis « purée j'ssaie mais je comprends rien » → lot exécuté **sans arbitrage user** (4 items sans risque) ; puis « les noms de quêtes du guide Sylvestre, ça peut amener du trafic ? ».
> **Fait** : `layout.tsx` → hreflang (`languages`) **supprimé** (mesuré : `/?lang=en` canonicalisé vers `/` ⇒ signal ignoré ; le sélecteur de langue reste fonctionnel) · `seo_check` (deploy-cd.sh) → contrôle du **statut HTTP** des pages clés (`/`, `/guides`, `/guides/rush-sylvestre`, `/boss`) · `SEO_REPRISE.md` → case « relire le `Caddyfile` de `main` après le merge » · `activeContext.md` → **rotation appliquée** (12 → 6) · **(suite, branche `fix/i18n-fr-par-defaut`)** `i18n/server.ts` → **détection `accept-language` retirée** : Googlebot (en-US) recevait l'**anglais** sur une URL dont le canonical est FR ; aucune requête EN dans Search Console ⇒ FR par défaut, langue choisie par sélecteur ou `?lang=` (jamais de langue différente pour un robot).
> **Mesures (guide Sylvestre)** : page publique en **`noindex, follow`** (construction), **0 nom de quête** dans le HTML ; données prêtes côté serveur (**400 Ko**, **374 liens DPLN**, passées en props ⇒ SSR) **mais** `PublicRushGuideClient.tsx:1423` = `{isExpanded && …}` avec `expandedMs` initialisé sur le 1ᵉʳ milestone ⇒ **les quêtes des chapitres repliés ne sont pas dans le HTML** (invisibles pour Google).
> **Preuves** : **218 fichiers / 2 428 tests** ✓ (+4) · `tsc` **0** · `eslint` **0 erreur**.
> **Reste** : chantier « guide Sylvestre indexable » (SSR de tous les chapitres, liens sortants limités, mode construction levé quand prêt) — **non codé, à trancher**.
## 🧩 Session 25/09/2026 (SEO, lot 2) — **Brouillon en vraie 404 · dates du sitemap honnêtes · origine publique explicite · checklist de bascule routée** → branche `fix/seo-finalisation`
> **Demande** : « ok fais le » (suite du lot 1, PR #734) — Search Console confirme déjà l'exclusion de la vitrine (`noindex détecté dans la balise Meta robots`, testé le 25/09 à 00h33).
> **Fait** : `guides/[slug]/page.tsx` → `meta.draft` dans la condition `notFound()` (brouillon = **404**, plus de 200 sous `noindex` seul) · `sitemap.ts` → **aucun `lastModified`** sur les 15 routes statiques (dates vraies conservées : guides, `guild.updatedAt`, boss, almanax) · `utils.ts` → `resolveAppBaseUrl()` pure (priorité env explicite, **jamais** l'en-tête `Host`) + `logger.warn` unique si aucune origine (fin du repli silencieux) · **routage doc** : `AGENTS.md` §2 → `docs/plans/SEO_REPRISE.md` ; `docs/MAINTENANCE.md` § Production → les **3 verrous à retirer** le jour de la bascule (sinon `sigilos.fr` invisible à vie).
> **Preuves** : **217 fichiers / 2 422 tests** ✓ (+7) · `tsc` **0** · `eslint` **0 erreur** · `tsc` avait attrapé un vrai défaut de typage (`ProcessEnv`) → variables passées explicitement.
> **Reste** : PR à merger · non traité : hreflang `?lang=en`, tests HTTP réels (Playwright inutilisé), `Caddyfile` absent de `main`.
## 🧩 Session 24/09/2026 (SEO) — **La vitrine `sigilos.fr` sort de l'index · le robots.txt de la prod était périmé ET cassé (20 jours) · le déploiement détecte désormais un proxy Caddy en retard** → branche `fix/seo-vitrine-hors-index`
> **Demande** : « j'ai rien compris au jargon et à ce qu'on peut faire pour améliorer le SEO aux meilleurs standards » — après le plan d'une IA SEO externe (sans accès au dépôt), croisé avec `docs/plans/SEO_REPRISE.md` et `temp/SEO-GSC-ETAT-MESURE-2026-09-21.md`.
> **Mesures (`curl` en direct, 24/09/2026)** : la bêta est **saine** (`robots.txt` 200 `text/plain`, `sitemap.xml` 200 `application/xml`) ; `sigilos.fr/robots.txt` **et** `/sitemap.xml` renvoyaient **la vitrine HTML** en 200, et `/zzz-inexistant` aussi (**soft-404**) ; le correctif `handle /robots.txt` était **sur le disque du VPS mais pas dans le conteneur** (compose montant **un fichier** : inode figé à la création) → inerte depuis le **04/09**, 2ᵉ incident après `/assets/*` (23/08) ; **sonde Caddy locale** : `respond "…\n…"` est servi **littéralement** (82 o sur une seule ligne → `robots.txt` illisible), le **heredoc** donne un vrai fichier (76 o, 5 lignes).
> **Fait** : `Caddyfile` → `X-Robots-Tag: noindex, nofollow` sur tout `sigilos.fr`, `robots.txt` en **heredoc** (sans ligne `Sitemap:` trompeuse), `sitemap.xml` → **404** · `public/maintenance.html` → `noindex, nofollow` · `scripts/deploy-cd.sh` → **`caddy_config_check`** (md5 dépôt ↔ conteneur, validation puis recréation automatique) + **`seo_check`** (ce qui est réellement servi) · docs : `MAINTENANCE.md` (récidive + automatisme) et checklist de bascule `SEO_REPRISE.md` (retirer le noindex le jour de la prod).
> **Preuves** : **216 fichiers / 2 415 tests** ✓ (+10) · `tsc` **0** · `eslint` **0 erreur** · `caddy validate` sur le Caddyfile réel → **`Valid configuration`** · sonde de comportement : `/robots.txt` 200 `text/plain` **76 o multi-lignes**, `/sitemap.xml` **404**, `/assets/ui/logo-v2.png` **200 `image/png`** · artefacts de sonde **supprimés**.
> **Reste** : appliquer sur le VPS (`git pull` + `--force-recreate --no-deps caddy` ; ensuite automatique) · **non traité** : `lastModified: now` (15 routes statiques du sitemap), hreflang `?lang=en` (canonical identique), repli silencieux de `getAppBaseUrl()`, tests HTTP réels (Playwright installé, aucun e2e), `Caddyfile` absent de `main`.

## 🧩 Session 24/09/2026 (registre + fiche boss + `/profil` Discord) — **Commentaires du staff (10 max) · fiche boss sans DofusDB et carte cliquable · Membres sans onglet doublon · `/profil` Discord enrichi** → branches `fix/boss-minimap-placement`, `feat/registre-commentaires`, `fix/profil-discord-annuaire`
> **Demande** : « supprime dofusdb sur les fiches publiques », « rend cliquable voir la carte », « supprime le lien vers dofusdb dans la modale des drop » · « affiche l'icône discord du membre », « "essai" mets essai validé », sélecteur « Recruté par » trop laid · commentaires multiples (10 max) horodatés et signés, colonne + modale au clic · « supprime l'onglet liste roster & membres mais avant déplace proprement la partie action membre, actif archivé banni exclus tous », « pas besoin de garder Discord ID / Ankama ID », retirer la flèche « profil perso » · puis le lot `/profil` Discord + annuaire.
> **Fait** : `MemberRegistryComment` (table + relation `UserProfile.registryComments`) et migration additive `20261225000000_registre_commentaires` — la note unique `staffNotes` est reprise comme premier commentaire **puis la colonne est supprimée** (aucune donnée perdue) · `addMemberRegistryComment` (RBAC, Zod borné, **plafond revérifié en base**, audit) · modale « Commentaires » (journal daté/signé + ajout, 10 max) et colonne dédiée · avatar Discord du membre, « Essai » → « **Essai validé** », « Recruté par » via l'`AsyncCombobox` partagé · onglet « Liste Roster & Membres » **supprimé** (sa gestion des statuts vit dans l'audit, flèche vers le profil perso retirée, colonnes d'ID masquées par `showIds` — God/settings inchangés) · Fiche boss/mob publique : **plus aucune sortie DofusDB** (en-tête, avis de recherche, modale de butin) · encart « Localisation du donjon » **entièrement cliquable** (image + nom + « Voir la carte → » = un seul lien) · bandeau worldmap sur une rangée (chips `h-7`, action ambrée unique) · en-tête boss en deux colonnes · `/profil` Discord enrichi : points de succès, métiers 200, activités appréciées, mules, craft légendaire, planning de la semaine (niveau retiré) · `/metiers` → `/members` · `annuaire-hub` réduit à une redirection (−142 lignes mortes).
> **Preuves** : `tsc` **0** et `npm run test:run` vert sur les **3 branches** (chacune mesurée : **214 fichiers**, 2 378 tests avec le lot boss, 2 377 avec le lot registre, 2 377 avec le lot `/profil`) · `eslint` **0 erreur** · base locale contrôlée (table + 2 index + FK, `staffNotes` absente, migration marquée appliquée) · le test 5×3 de l'encart était **faux** (tuiles décalées d'une colonne : le code était bon) — corrigé · piège `The column … does not exist` après migration consigné dans `docs/DEVELOPPEMENT.md` §7 (un `next dev` déjà lancé garde l'ancien client Prisma).
> **Reste** : suppression d'un commentaire du registre (non demandée) · **migration à jouer au déploiement** (`./scripts/deploy-cd.sh`, additive + reprise, non destructive) · contrôles visuels (registre, audit, fiche boss, bandeau worldmap, `/profil` Discord).
## 🧩 Session 24/09/2026 (suite 27) — **Tickets v2 : le BRANCHEMENT Discord est fait — un parcours publié ouvre vraiment un ticket (choix → modales → ping → salon), et l'écran « Parcours » est livré** → branche `feat/tickets-parcours` (PR #725)
> **Demande** : « Reprends le chantier « Tickets v2 » là où il s'est arrêté » — objectif : rendre les parcours **réels en jeu** (§4.2 branchement, puis §4.1 écran, du plan vivant).
> **Mesure (avant tout test)** : la base locale **n'avait pas les tables v2** (`information_schema` ne listait que 8 tables `Ticket*`) ⇒ procédure **§7 du plan** appliquée (dump hors dépôt, SQL additif des 2 migrations, `migrate resolve`, `prisma generate`) : **13 tables** `Ticket*`, colonnes `TicketBotPanel.journeyIds` et `TicketJourney.notifyRoleIds` **vérifiées en base**. `prisma migrate status` reste en **dérive préexistante** (1 migration absente du dépôt, `20260919130000_add_dungeon_slug` non appliquée) — **jamais de `migrate dev`** (il propose un reset).
> **Fait** : ① **branchement réel** — `journey-dispatch.ts` (pur : une étape → *une* réponse Discord : éphémère des choix, modale, bouton « Continuer » après une soumission, refus motivé), `src/server/tickets/journey-open.ts` (parcours **publié ET activé**, formulaire **figé** relu, brouillon `TicketDraft` lu/écrit/purgé, `advanceTicketJourney`), route d'interactions : `tb_pick`, `open`/`select_journey`, `modal_page`, `modal_open` (fail-closed via `parseTicketCustomId` ; repli **v1 conservé** et catégorie relue **dans la guilde**) ; ② **création réelle** — `internalHandleTicketCreate` accepte `journeyId`/`formVersionId`/réponses normalisées, **revalide** (`evaluateAnswers` sur la version figée), nomme le salon (`formatTicketChannelName`), écrit `journeyId`+`formVersionId` sur le `TicketRecord`, envoie l'embed + la **vue du demandeur** (`buildActionRows`, invariant #1) + le **ping** des `notifyRoleIds` (`allowed_mentions` explicite, jamais `@everyone`), puis **supprime le `TicketDraft`** ; ③ **panneaux** — `journeyIds` dans `TicketPanelSchema`/`saveTicketPanelAction`, `deployTicketPanelAction` passe par `buildPanelRows` (parcours publiés d'abord, repli catégories v1) + `buildPanelEmbed`, l'onglet panneaux choisit les parcours ⇒ « Publier » a enfin un **effet observable** ; ④ **écran « Parcours »** — `ticket-journeys-tab.tsx` (assistant 5 étapes branché sur `journey-wizard.ts`, publier / activer / supprimer, état *Brouillon* / *Publié (vN)* / *« modifications non publiées »*), entrée + `page.tsx` (`journeys`, `forms`, `teams`).
> **Preuves** : **sonde sur base réelle** (créée puis supprimée, zone volatile) — brouillon ⇒ refus « Ce motif n'est pas encore ouvert. » · étapes 1-2 `choices` · étape 3 `modal` page **0/2** (5 `fieldIds`) · étape 4 → page **1** (`TEXTS:1`) · brouillon persisté (clés `classe,q0..q4,reglement`, **TTL 30 min**, `formVersionId` = version figée) · étape 5 `open` (**9 réponses**) · clé inventée ⇒ `INVALID` · nettoyage vérifié. `tsc` **0** · **212 fichiers / 2 360 tests** ✓ (+15) · `eslint` **0 erreur**.
> **Reste** : T2 constructeur de formulaires (20 questions, 6 types, aperçu live) · T3 onglet Équipe · T5 participants (`setMemberChannelPermissionDiscord` jamais appelé), `THREAD_PRIVATE`, suppression de salon **observée**, préflight permissions · T6 SLA / auto-fermeture / purge / quota serveur (worker) · borner l'`id` de champ côté constructeur (limite 100 car. d'un `custom_id`) · `APPROVAL` **non enregistré** (aucun handler `approve`/`refuse`) — le staff agit depuis la boîte de réception, les boutons de staff n'étant **jamais** publiés dans le salon du demandeur (invariant #1).
> **Rotation** : les blocs au-delà des 6 derniers ont été sortis de cette page (règle en tête de fichier) — ils restent lisibles par `git log -p -- docs/agents/activeContext.md`.


## 🧩 Session 24/09/2026 (suite 26) — **Tickets v2 : moteur committé (PR #724 fusionnée), contrat de formulaire porté à 20 questions, règles de l'assistant « Parcours » et du tunnel d'ouverture livrées et testées** → branches `feat/tickets-v2-refonte` (fusionnée #724) + `feat/tickets-parcours` (PR #725, brouillon)
> **Demande** : « Reprends le chantier « Tickets v2 » là où il s'est arrêté » (`temp/PROMPT-REPRISE-TICKETS-V2.md`), puis « toi qui vois » et « monte à 20 les questions max (5 actuellement) ».
> **Mesure (décisive)** : ⓐ le moteur v2 était **non committé dans l'arbre, sur `dev`** (22 chemins) ⇒ branche créée + PR #724 ; ⓑ l'écran « Parcours » du brief **n'aurait rien exécuté** (la route ne résout `tb:open` que dans `TicketBotCategory`, les panneaux n'écrivent que `categoryIds`, `journeyIds` n'est jamais lu ⇒ `isPublished` que **rien** ne consomme) ; ⓒ le « 5 » des questions = **limite Discord d'une modale** : les questions 6+ étaient coupées **en silence** (`modalOverflow` signalé, jamais traité).
> **Fait** : moteur committé + **PR #724 fusionnée** (CI verte) · contrat **25→20 questions** (`TICKET_MODAL_PAGES_MAX`, `textPages`, `buildTicketModalPage` : 4 pages, aucune perdue, 21ᵉ question **refusée**) · `journey-wizard.ts` (assistant 5 étapes, slug sans collision, erreurs vs avertissements, charge utile figée par test) et `journey-tunnel.ts` (machine à états `TicketDraft` : choix → modales → « Continuer », page remplie jamais reposée, `applyTicketChoice` revalide) · routage `tb_pick:<parcours>:<champ>` + `tb:modal_page:<parcours>:<page>` (aller-retour émetteur→routeur testé) · `APPROVAL`/`THREAD_PRIVATE` non enregistrés (aucun exécutant) et signalés · **ping de rôles à l'ouverture** (demande user) : `TicketJourney.notifyRoleIds` + migration additive `20261222000000_tickets_journey_notify_roles` (vérifiée par `prisma migrate diff`, diff résiduel 0), repli sur `TicketTeam.notifyRoleIds`, `notifications.ts` (flocons validés, 25 max, **jamais `@everyone`**, aucun ping par défaut) · **doc** : guide in-app `admin-tickets` réécrit (8 sections, semé en base) + plan vivant `docs/plans/PLAN-REFONTE-TICKETS-V2.md`.
> **Preuves** : `tsc` **0** · **211 fichiers / 2 345 tests** ✓ · `eslint` **0 erreur** · CI #724 verte · migration contrôlée par diff sur base *shadow*.
> **Reste** : **écran « Parcours »** (assistant) · **branchement Discord** (`journeyIds` des panneaux, `tb:open`/`select_journey`/`pick`/`modal_page` dans la route, `internalHandleTicketCreate` avec `journeyId`/`formVersionId` — c'est ce branchement qui rendra le ping des rôles **observable**) · T2 formulaires, T3 équipes, T5 participants/`THREAD_PRIVATE`/suppression observée, T6 SLA·auto-close·purge · borner l'`id` de champ côté constructeur (limite 100 caractères d'un `custom_id`) · base locale en dérive **préexistante** et **sans les tables v2** (procédure d'application sûre en §7 du plan) · rotation d'`activeContext.md` toujours non appliquée (13 blocs au lieu de 6 ; les blocs sortis restent lisibles par `git log -p`).

## PR / Branche
- **Session UX/sync** → branche **`feat/rush-sylvestre-ux-v2`** (créée depuis `origin/dev` = `b185e5e61`, après merge PR #586) → PR vers `dev`.
- Merge propre : aucun chevauchement de fichiers avec `dev` (seuls `package-lock.json` + `sync-actions.ts` différaient dans `origin/dev`).


## NEXT (priorités — à faire)

### 🛒 Marché — état au 14/09/2026 (3 PRs mergées : #657 · #659 · #660 · **+ debug beta en cours**) — **0 tâche de code ouverte au plan §21**
> **Vague de debug beta (5ᵉ tour)** — branche `fix/marche-debug-beta-14-09` : **3 constats fermés** (offre Discord/RBAC, ligne fantôme « +15975 Effet », icônes Mêlée/Distance + audit) ⇒ suite **1293/1293** · `tsc` 0 · `eslint` 0 erreur · **0 migration**. **Constats restants** (diagnostiqués, mesures dans `debug.md`) : `Réglages → Marché` (3 sélecteurs de rôles + panneau « Durées, plafonds & rappels » à retirer — le God est déjà global, `updateMany` cross-guild **mesuré**), timeline des offres sous la fiche (**emplacement à trancher par le user**), désistement depuis la fiche du dashboard, capacité légendaire (texte du pouvoir à sourcer), image OG (titre `EFFETS` recouvert, ligne horizontale, pied « Annonce #… / vendeur » à retirer).
> Plan maître §21 : **168 cases cochées** / **16 non cochées** = 4 DoD-ops (`S1.25`, `S1.34`, `S4.0e`, `S4.12`) + 3 historiques (`S0.7`, `S0.8`, `S4.13`) + **9 = V1.1 `S6`**. ⇒ code **clos** ; il reste des **ops**, **2 validations** et **1 PR docs** (celle-ci).
- ⏳ **Ops user (bloquant le gel §6)** : **déployer** `b9079d02b` (`sudo ./scripts/deploy-cd.sh beta <sha>`) · **crontab `market-expire`** (`docs/MAINTENANCE.md:65` ; sans elle : ni expiration, ni rappel J+7/J+15, ni retrait J+20, ni purge) · **DoD visuelle** dark/light/mobile + `/god/market` · **`S4.12`** (2 comptes Discord, double clic simultané) · **tags du salon forum** + association (`Réglages → Marché`) · **libellés `S1.25`** · bouton God **« Rattraper les effets natifs »** · (facultatif) bouton God **« Siphon items »** pour remplir `superTypeId` (**21 748 NULL** aujourd'hui ; le Marché tient déjà via le repli `typeId` → `typeName`).
- 🎨 **Assets A3 (à valider avant intégration)** : `uiassets_assets_*/auctionHouse{Equipments,Cosmetics,Resources,Consumables}.png` (familles de filtres) · `uidarkstone_assets_all/icon_slot_rune_forgemagie.png` + `uiassets_assets_2x/auctionHouseRunes.png` (bloc Forge) · `icons_assets_2x/rune.png` / `shop.png` (option). `kamas.png` / `hammer.png` = **déjà intégrés**.
- 🔎 **Résidu code (option)** : 4 `<Image>` sans repli d'erreur (`market-create-client.tsx:1141,1456,1550,1681`) → harmoniser sur `MarketItemIcon`.
- 📌 **Infra** : `T15` (CSS `MIME text/plain` = purge cache/CDN, **aucun code**) · `npm run seed:docs` / `build:seeds` sont **inutiles sur le VPS** (le `Dockerfile` les exécute au build, `docker-entrypoint.sh` joue les seeds au boot).
- 🚫 **Hors module** : V1.1 = **`WANTED` + matching seulement** (chantier séparé, sa PR, sa migration additive) — le reste de `S6` est **écarté**.
- 🔬 **Harnais réutilisables** : `_shot-fiche.mjs` (JWT Auth.js **forgé** + Playwright sur `next dev` : captures réelles dark/light/mobile) · `_probe-og-card.mjs` (carte OG + Sharp) · `_probe-marche-etat.mjs` (comptages en base) · `_probe-cosmetic-noise.mjs` (bruit `0 → 0`). ⚠️ Piège PowerShell : les dossiers `[guildId]` sont des **caractères génériques** ⇒ `-LiteralPath` obligatoire.

- ⚠️ **Prod/Infra** — `prisma migrate deploy` sur l'env **déployé** : en local c'est réglé (`resolve --applied` sur `20261009`-`20261013`). À refaire là-bas si même cause « already exists ».
- 🔴 **Ops** — Ménage `Songes_Pour_Les_Noobs/` (177 f, 8.4 Mo) → `git rm -r --cached`.
- 🟠 **Data** — 15 `unresolved` restants (Feuille de Salace, Mesure de poivre, Bière du Chabrulé, Poiskaille en Fricassée, Kamas, « L' », phrases « ou 1 x »…) : pas d'équivalent `name.fr[]` DofusDB → mapping manuel ou acceptation.
- 🟠 **Data** — Intégrer les 15 quêtes « Apprentissage » (Ordres) : hors des 374 séquences ; mapping `alignReq`/camp requis.
- 🟡 **UI** — Liens NOOBS/DOFUSDB → modale (aujourd'hui nouvel onglet).
- 🟡 **UI** — Responsive : auto-replier aussi la barre de chapitre / le bloc recherche sur très petite hauteur ; mode compact en fallback auto très petit.
- 🟡 **Perf** — Factoriser `getSequenceHelpers` par guide / agréger « qui peut aider » par chapitre.
- 🟣 **Tech** — Nettoyer `as any` (~70 admin, ~64 timeline) ; confirmer suppression `RushOverlayQuestPanel.tsx` (orphelin).


## Références clés
- 🎨 **SOURCE D'ICÔNES OFFICIELLE (permanente, tout module)** : `<Bureau>/dofus_assets` (~23 770 fichiers : icônes 1x/2x, items 2x, sorts, monstres, UI…). On peut y piocher des icônes pour n'importe quel module, **UNIQUEMENT après les avoir proposées au user ET validées** avant intégration.
- Backlog canonique : `docs/ROADMAP.md` (à lire en premier en mode plan).
- 🧭 **Amorce de session générique** : `docs/agents/session-amorce.md` (contexte + **checklist sécurité non négociable** + méthode de preuve + vérifs + DoD + **bloc à coller** §10) — **remplacée le 20/09/2026 par `AGENTS.md`** (racine), lu automatiquement par les assistants.
- 🛒 **Marché — amorce de la phase de debug** : `refonte-marche/AMORCE-DERNIERE-PHASE-DEBUG.md` (+ `AMORCE-DEBUG-A-COLLER.txt`).
- Demandes ouvertes + annotations : `docs/ROADMAP.md` (source unique).
- Plan maître #223 : `refonte-long-terme-discord-compatibilite/PLAN-MAITRE-RESILIENCE-DISCORD-LONG-TERME.md` + `sigilos-discord-resilience.md`.
- 🛰️ **Indépendance totale (fiches boss/monstres, simulation, icônes) — AMORCE PRÊTE (15/09/2026)** : `amorces/amorce-2026-09-15-independance-totale.md` — 6 lots (stale-while-offline → TTL par dataset → `local-only` strict → icônes siphonnées + cache durci → siphon **tous les monstres** → fraîcheur visible), 6 décisions à trancher (D1→D6, recos incluses), **critère « test de panne »** (`DOFENSIVE_OFFLINE=1`/`DOFUSDB_OFFLINE=1` : zéro spinner, zéro icône cassée, zéro appel sortant). Contexte : local-first **pas** local-only ⇒ une ligne périmée (> 24 h) déclenche une bascule live silencieuse (c'est l'incident Dofensive down). `refonte-siphon/PLAN-REFONTE-SIPHON.md` §5.3 (limiteur anti-ban) s'applique aux lots 4-5.
- Dernier mémo détaillé : `memo-2026-09-14-marche-d49-q15.md` (D49 + Q15) · `memo-2026-09-14-marche-embeds-cartes.md` (#657/#659) · archives : `memo-2026-10-06-chantier-fiche-boss.md`.


## Vérifications globales habituelles
`npx tsc --noEmit` 0 · `npm run test:run` vert · `npm run build` OK · pas de `console.*` en prod.

