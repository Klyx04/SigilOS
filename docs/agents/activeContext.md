# Active Context — SigilOS

> 📌 **Rotation (depuis le 20/09/2026)** : cette page ne garde que les **6 dernières sessions**.
> Les blocs plus anciens (26 au 20/09/2026) étaient archivés hors dépôt — ces archives ont été
> **supprimées le 20/09/2026** (`docs/arbo/ARCHIVES-TEMP-2026-09.md`) ; ils restent **intégralement**
> consultables via `git log -p -- docs/agents/activeContext.md` (l'historique git n'est pas concerné).
> En fin de session : ajouter le nouveau bloc EN HAUT, et **sortir le 7ᵉ** (récupérable via `git log`).
## 🧩 Session 25/09/2026 (soir — fiche boss publique & incident alertes Discord) — **onglets quêtes & succès repris d'un stash orphelin + boucle d'alerte d'écriture Discord coupée** → branches `feat/boss-onglets-publics` (#756), `fix/alerte-outbox-boucle`
> **Demande** : « commit ce qui manque stp » puis « pousse stp, corrige si il faut ».
> **Mesure avant** : l'arbre était **propre** et `dev` à jour — le travail « manquant » était un **stash orphelin** (`stash@{0}` ; branche d'origine `feat/discord-audit-et-surveillance-salons` **supprimée**, ni locale ni distante), donc **invisible dans l'historique** : 4 fichiers, 465 lignes (`/boss/[dungeonId]`, `PublicBossQuestsTab.tsx`, `PublicBossAchievementsTab.tsx`, `public-boss-tabs-actions.ts`). Le stash annonçait « la partie client reste à écrire » : `tsc` était rouge sur **une** erreur (`TS2322`, `initialQuestsData` absent de `PublicBossDetailClientProps`).
> **Fait** : le stash est **sauvegardé en commit** (branche locale, **aucun push** ; le hook `tsc` a été contourné **une fois** — `--no-verify` — sur ce WIP, documenté dans le message) puis **terminé** : `PublicBossDetailClient` accepte `initialQuestsData`/`initialAchievements`, `DetailTab` gagne `quests`/`achievements`, **2 onglets** (icônes `icons/quests.png` et `icons/success.png`) **masqués** quand il n'y a rien à montrer (titans et avis de recherche ne reçoivent pas ces données) et les 2 panneaux sont rendus. Serveur : `getPublicLinkedQuests` (normalisation des noms, vrais donjons) + `getPublicDungeonAchievements` (lecture `dungeonAchievement`).
> **Preuves** : suite complète **2 545 tests verte** · `tsc` **0** · `eslint` **0 erreur** (avertissements de style préexistants) · `git status` **propre** · `git stash list` **vide**.
> **Reste** : ⚠️ **aucune capture** — rendu des 2 onglets à contrôler **à l'œil** sur un donjon qui a des quêtes et des succès · reliquats locaux à supprimer : 8 branches dont l'upstream est `gone` (déjà mergées : leurs fichiers distinctifs sont dans `dev`) et `feat/registre-modal-mules` (mergée en #753).
> **Incident — boucle d'alerte d'écriture Discord** (mesuré par le user en **bêta**, `docker logs` + SQL lecture seule) : **1 000 alertes + 1 000 jobs en 19 min** (25/09 17:23:18 → 17:42:32, ≈ 1/s), **toutes** vers le salon `1547020288380637305` (403 · code 50001 = le bot n'a **pas accès** à ce salon). Cause racine : ce salon est `PlatformConfig.godNotifyChannelId`, et `notifyGod` — qui **crée l'alerte** — la **repostait sur Discord** via `sendChannelMessage`, donc dans la **même** file `discord-outbox` (`DISCORD_OUTBOX_ENABLED=true` en bêta) que l'écriture qui venait d'échouer : nouveau job → nouvel échec → nouvelle alerte → … **Correctif** (`fix/alerte-outbox-boucle`) : `notifyGod` gagne **`webOnly`** (aucun envoi Discord) et **`dedupeKey`** (une alerte par clé et par heure, via `rateLimit` fail-closed), le worker outbox déclare l'alerte d'échec **`webOnly` + `dedupeKey` par salon** ; la déduplication est **étroite** (une alerte **métier** — don, feedback — n'en fournit pas, donc aucune alerte légitime n'est avalée). Preuves : `tests/unit/god-notify-anti-loop.test.ts` (**6 cas**, dont non-régression « alerte normale ⇒ Discord notifié ») + suite **2 551 tests verte** · `tsc` 0 · `eslint` 0 erreur. **Ops à faire par le user** : vider (ou réparer) `godNotifyChannelId` dans God → Paramètres plateforme — le correctif rend la boucle impossible, mais le salon reste inaccessible ; et « Tout marquer lu » pour les ~1 000 lignes historiques.

> **Discord — `/valider-recrue` : la modale était bien là, l'ancienne signature non** (constat user du 25/09 : la commande affiche encore « membre (La recrue…) » + « + 6 en plus »). Mesure : le **code** ouvre **déjà** la modale (`type: 9`, `custom_id: valrecrue:submit:<membre>`, champs pré-remplis depuis le profil), mais la **définition enregistrée sur Discord** déclarait encore **7 options** héritées (`pseudo-dofus`, `tag-ankama`, `recruteur`, `arrivee`, `ajouter-role`, `retirer-role`) — d'où l'affichage trompeur. Correctif (`fix/valider-recrue-modale`) : la commande ne garde que **`membre`** (la cible, obligatoire), les six autres deviennent des **champs de formulaire** (les rôles appliqués viennent du réglage RBAC de la guilde), et les lectures d'options devenues mortes sont retirées de la route. ⚠️ **Interface publique Discord** : la nouvelle signature n'apparaît qu'après une **re-synchronisation** — God → Commandes Slash (bouton de synchronisation) ou `syncSlashCommandsToDiscordAction()`. Preuves : contrat mis à jour dans `tests/unit/slash-commands.test.ts` (**1 option, aucune option héritée, les 4 champs de la modale présents**) · suite complète verte · `tsc` 0 · `eslint` 0 erreur.


## 🧩 Session 25/09/2026 (God, refonte — exécution) — **7 lots *one shot* : le mort d'abord, puis le verrou de guilde, la vue God « Modules », les logs unifiés, la lecture God, le langage et le déslop** → branches `feat/god-lot0-mort` (#747) · `feat/god-lot1-verrou` · `feat/god-lot2-modules` · `feat/god-logs-unifies`
> **Demande** : exécuter les 7 lots de `docs/plans/AMORCE-REFONTE-GOD-GUILDES.md` (arbitrages A1-A11 tranchés), 1 lot = 1 branche = 1 PR → `dev`, sans redemander la suite (méthode : mesurer avant de corriger, preuve chiffrée, aucune dette laissée).
> **Lot 0 — le mort d'abord (A7 · A8)** — PR #747 : suppression de `LifecycleServer` (placeholder **faux** : « intégré via LifecyclePanel » alors que le composant n'était monté nulle part), `src/app/god/components/lifecycle-panel.tsx` (~900 l., jamais importé hors de lui-même), `components/ghost-radar-panel.tsx`, `janitor-button.tsx` et `src/components/admin/deletion-pending-panel.tsx` (**faux positif** : aucun code n'écrit `User.deletionRequestedAt`/`scheduledDeletion`, et la purge est attribuée à tort au Janitor) + l'action `getPendingDeletionUsers()` (`super-admin-actions.ts`). L'onglet `?tab=security` ne monte plus que `GlobalLogsServer` (journal `security` filtré en base). Plafond du test `god-deslop.test.ts` **90 → 85** (3 fichiers en moins dans `src/app/god`).
> **Lot 1 — le verrou atteint enfin la carte de la guilde (A1 · G8)** (`feat/god-lot1-verrou`) : la règle pure `src/lib/module-lock.ts` gagne `resolveModuleState`/`resolveModuleGrid` (trois niveaux **distincts** : verrou plateforme > verrou de guilde > toggle de guilde) et `PLATFORM_LOCKABLE_MODULES`/`normalizePlatformNotices` ; nouveau core serveur `src/server/platform-module-state.ts` (lecture **tolérante** de `PlatformConfig.disabledModules`/`moduleNotices`, cache 30 s aligné sur `moduleCache`, effective dès le lot 2 sans casse avant migration). `getGuildModules` = **guilde ∪ plateforme** ; nouvelle action `getGuildModuleConfig` (toggles **bruts** + états effectifs issus d'**une seule** lecture — un module verrouillé n'écrase plus son toggle conservé en BDD) ; `isModuleLocked` garde les 6 pages de module qui n'avaient **aucune** garde (`presentation`, `members`, `profile`, `reaction-roles`, `tickets`, `admin/logs`) ; `updateGuildModules` refuse aussi une coupure plateforme ; `getUserContext` (canView*/canManage*) et `internalCheckPermission` (bot) consomment la **même** règle. UI : carte grisée + toggle **inerte** + badge « Indisponible — maintenance » + message du God sur la carte, « Désactivé par ta guilde » pour un toggle coupé, **le mot « staff » disparaît** (garde de test sur tout `src/`). Deux défauts corrigés au passage : le mini-jeu était gardé par le module `worldmap` (copie), et les pages `commandes`/`ladder`/`missions`/`songes`/`quete-ocre`/`kamas`/`galerie`/`worldmap` **n'exemptaient pas le God** (`bypassModules = isGod` RESTE, inchangé).
> **Lot 2 — vue God « Modules », verrou plateforme et message perso (A2 · A3 · G12)** (`feat/god-lot2-modules`) : **une migration additive et idempotente** (`20260926100000_platform_module_locks`, 4 `ADD COLUMN IF NOT EXISTS` sur `PlatformConfig` : `disabledModules`, `moduleNotices`, `disabledModulesUpdatedAt`, `disabledModulesUpdatedBy` — appliquée en local + `migrate resolve`) ; action `setPlatformModuleLock(moduleKey, locked, notice?)` **God only** (Zod borné 200 c., rate-limit 10/min, **garde d'état** `WHERE disabledModules equals`, `createGodAuditLog` / `MODULE_PLATFORM_LOCK:<key>`, invalidation de **tous** les caches modules + contexte utilisateur de chaque guilde) ; `getPlatformModuleOverview` (verrous plateforme + messages + **compteur de guildes** verrouillées par module, super-admin) ; **onglet `/god?tab=modules`** (brique `modules`, `subGodAccess: false`) avec les **icônes des modules**, une action primaire par carte (coupure) et le champ de message. Le message du God est déjà affiché sur la carte de la guilde (livré au lot 1) — la boucle A2/A3 est fermée.
**Mesure (sonde volatile sur la base locale, supprimée depuis)** : écriture réelle de `disabledModules = {songes}` + `moduleNotices = {"songes":"Migration des Songes — retour demain"}` ⇒ relecture par `getPlatformModuleState()` (`{"locks":["songes"],"notices":{…}}`) ⇒ état effectif d'une guilde qui a `songes` **ON** = `{enabled:false, lockedBy:"platform", notice:"…"}`. Base restaurée à `[]` / `NULL` après la mesure.
> **Reprise (à coller)** : `docs/plans/AMORCE-REFONTE-GOD-GUILDES.md` **§6** = bloc prêt pour les **lots 3 → 6** (état réel inclus) ; **§0** = écarts mesurés au plan + 7 leçons (brancher chaque lot depuis `dev` **après** le merge du précédent, docs en conflit par lot, `gh pr create --body` cassé sous PowerShell, payload = toggles **bruts**, `isModuleLocked` ≠ `isModuleEnabled`, sonde sur base réelle sans `DATABASE_URL` injecté par vitest, CI ≈ 8-9 min).
> **Preuves** : `tests/unit/platform-module-locks.test.ts` **11 cas** (garde d'état, journal God, refus non-God/`admin`/clé hors registre, rate-limit, message borné, compteur par guilde, **migration additive** vérifiée par lecture du SQL) · `tests/unit/module-lock.test.ts` **27 cas** · `npx tsc --noEmit` **0** · `npm run lint` **0 erreur** · `npm run test:run` **226 fichiers / 2 519 tests ✓**.
> **Lot 3 — les journaux, une seule porte (G11 · A9 · A10 · A11 · G6)** — branche `feat/god-logs-unifies` : **une seule** entrée de nav (`god-nav-config.ts` : « Sécurité & Logs » supprimée, « Journaux » → `/god/logs` ; `?tab=security` **redirige** depuis `/god/page.tsx` ; brique `security` **fusionnée** dans `logs` — mesure avant : **0** grant `security` en base, donc fusion neutre pour les délégations). `/god/logs` = **5 onglets** : journal **plateforme** (`isGodLog: true`), journal de **guilde** (A11 : `isGodLog: false`, filtre par id **interne** de guilde, **lecture seule**, réservé au super-admin — refus fail-closed pour un sous-god), **accès refusés**, **audit du Marché**, **accès délégués** (A10 : `GodAccessLog` — 20 GRANT / 28 REVOKE / 7 SYNC, **écrit depuis l'origine et jamais lu**, désormais exposé avec nom du délégué et détail borné à 160 c. ; pas de relation Prisma ajoutée, donc **aucune migration**). **A9** : la ligne `GOD_DASHBOARD_ACCESS` par visite est **arrêtée** (appel retiré du layout God, throttle horaire devenu orphelin supprimé du core) et remplacée par un **compteur agrégé par jour** (`src/server/god-access-stats.ts`, source `GodSessionLog`, série **continue** en UTC) affiché en tête de page. **G6** : `src/lib/audit-log-view.ts` (règles **pures** : presets de période, fenêtre de pagination numérotée, jours UTC, série quotidienne) + `GodPagination` dans le kit ; filtres **en base** (action, catégorie, **guilde**, recherche, `dateFrom` ajouté à `/api/god/audit-logs`), regroupement par **jour**, compteur par **famille** (`security` compté en base, `functional` = complément exact ⇒ 1 requête de plus). L'ancien `audit-feed-panel.tsx` (second panneau mort) et **2 orphelins mesurés** (`guild-manager.tsx`, `docs/_components/doc-outline-sidebar.tsx`, aucun import hors d'eux-mêmes — `user-list.tsx` est **conservé**, cité par le lot 5) supprimés. Plafond `tests/unit/god-deslop.test.ts` **86 → 85**. Preuves : `tests/unit/god-logs-unified.test.ts` (**15 cas** : règles pures, une seule porte, lectures God) + `audit-taxonomy.test.ts` et `god-deslop.test.ts` mis à jour · suite complète **227 fichiers / 2 534 tests verte** · `tsc` **0** · `eslint` **0 erreur** (`next dev` tourne ⇒ build délégué à la CI).
> **Lot 4 — « le God voit tout » (G7 · A4 · A5)** — branche `fix/god-lecture-god` : **2 causes racines de lecture** d'abord. ① **Lecture (A5)** : l'onglet Logs de la fiche guilde (`/god/guilds/[id]`, `GodGuildLogsSection`) évaluait le God avec `getUserContext(discordGuildId).canViewAuditLogs` — faux pour un super-admin qui n'est **pas membre** du serveur Discord, et un `null` d'appel en erreur affichait le **même** message qu'un refus ⇒ « ce compte n'a pas la permission de lire le journal de cette guilde » sous un en-tête « INSPECTION GOD MODE ». Désormais la section reçoit `isGod={isAdmin}` et lit par `getGlobalAuditLogs({ guildConfigId, scope: "guild" })` (super-admin), la pagination passant par la route **plateforme** (`AuditLogsClient` gagne un mode `godGuildConfigId` → `/api/god/audit-logs?scope=guild`, la route de guilde restant inchangée pour les admins) ; « **Erreur de chargement du journal** (cause) » et « **Accès refusé** » sont **deux textes distincts**, et le refus n'est plus jamais affiché à un God (`!isGod`). ② **Écriture (A4, verbatim 10)** : `createAuditLog` écrivait **toujours** `isGodLog: false` avec le `guildId` interne — une action du God sur un membre (purge, ban, déblocage, pseudo, ID Ankama, transfert…) apparaissait dans le journal de **la guilde**. La règle est posée **une seule fois**, dans `createAuditLog` : un super-admin **sans autorité** dans la guilde ciblée (ni `isAdmin`, ni `canManageMembers`) écrit dans le **journal plateforme** (`isGodLog: true`, **sans** `guildId`, guilde rappelée en `metadata.discordGuildId`) ; un super-admin qui **est** admin de la guilde garde le journal de sa guilde, et un appel **sans session** (bot, cron) n'est jamais routé par accident (fail-safe vers le journal de guilde). Aucun drapeau ne vient du client. La route `/api/god/audit-logs` gagne `actor`/`dateTo` (mêmes filtres que la route de guilde). Preuves : `tests/unit/god-sees-everything.test.ts` (**9 cas** : routage God / admin de guilde / bot, exclusion `isGodLog: false` du journal de guilde, gardes de source sur les deux messages) · suite complète **228 fichiers / 2 543 tests verte** · `tsc` **0** · `eslint` **0 erreur**. ⚠️ Reste **ops/user** : la recette visuelle (fiche guilde, 3 onglets lisibles, aucun message d'accès) n'a **pas** été capturée (pas de session God forgeable dans cette session).



## 🧩 Session 25/09/2026 (God, refonte) — **Audit croisé d'un mémo externe : 5 PR de correctifs (vérifiés sur pièces) + fiche guilde à onglets + déslop God** → branches `fix/god-onboarding-notif` (#739) · `fix/god-lock-effectif` (#740) · `fix/god-logs-retention` (#741) · `feat/god-audit-secu-split` (#742) · `feat/god-guild-logs-rbac` (#743) · `feat/god-deslop`
> **Demande** : « J'ai fait faire un petit audit à un LLM (muse spark de chez meta) → `temp/memo-2026-09-24-audit-god-refonte.md` ; fais un tour, fais un audit de ton côté, affirme/infirme ses dires et vois ce qu'on peut faire » puis « One shot le tout, PR par PR sans me demander la suite, et fais en même temps une refonte pour désloper GOD partout ».
> **Mesures (avant tout correctif)** : le mémo est **fiable** (fichiers cités réels, **numéros de ligne exacts**) mais 4 affirmations sont fausses et 3 angles morts manquaient. Base locale en lecture seule : `AuditLog` **1029 lignes dont 843 > 30 j** (864 `isGodLog`), **0 verrou** actif, `ServiceActivityLog` 22, `MarketAuditLog` 110, index réels = `pkey` + `[guildId,createdAt]` + `[actorUserId]` + `[action]` (le `[isGodLog]` annoncé **n'existait pas**) ; `EXPLAIN` reste en **Seq Scan** à cette taille (normal, l'index sert à l'échelle).
> **Fait** : ① **#739** — origine d'une guilde en **source unique** (`resolveOnboardingOrigin`/`isSelfOnboardedGuild`) : le bot crée déjà la ligne (`SYSTEM_GATEWAY`) donc l'ancien `isAutonomous = !existingAllowed` annonçait « VIP / WHITELIST » à un self-onboarding, la console God affichait « 👑 VIP Manuel », et le **kill-switch était contournable** (ligne existante + inactive ⇒ aucune gate, et l'ancien `else if (!isActive)` **réactivait** une guilde gelée) ; le bot dit désormais la même vérité en base **et** dans Discord (embed vert « auto-actif » / rouge « action requise »), une ligne inactive est refusée **fail-closed**. ② **#740** — le **verrou God est enfin effectif** : `getUserContext` ne lisait pas `disabledByGod` (trouée n°1), les pages exemptaient `isAdmin` (n°2), `internalCheckPermission` (bot) ignorait le verrou (n°3) ; règle pure unique `src/lib/module-lock.ts` (liste **dérivée du registre** — `commandes` manquait —, `admin` jamais verrouillable), écriture durcie (**garde d'état** `updateMany where disabledByGod equals`, Zod, rate-limit 10/min, `createGodAuditLog`/`GOD_MODULE_LOCK` au lieu du journal de guilde) ; les deux affirmations fausses du plan `PLAN-REFONTE-ONBOARDING.md` §9.6 corrigées dans le même commit. ③ **#741** — rétention **90 j God / 30 j guilde** (une seule constante 30 j s'appliquait à tout, y compris `isGodLog`), core `src/server/audit-retention.ts` **hors `"use server"`** (l'ancienne `cleanupGlobalAuditLogs` était une **server action sans garde** déclenchée par une simple visite de `/god/logs`), purge **par lot** 500 et par périmètre, politique unique consommée aussi par le janitor VPS. ④ **#742** — séparation audit/sécurité **filtrée en base** (taxonomie pure fermée + `in`/`notIn` croisés, `scope` platform/guild) : fin du « Security Feed » qui affichait `CONFIG_UPDATED` et du **même total** sur les deux écrans ; migration additive `20260925120000_audit_log_god_indexes` (**2 `CREATE INDEX`**, appliquée en local + `migrate resolve`). ⑤ **#743** — fiche guilde **à onglets** *Modules · Logs · Accès & RBAC* : journal de guilde **riche réutilisé** (`AuditLogsClient`, jamais recopié), compteurs 30 j (`getGuildLogsStats` branché), accès en **lecture seule** (owner, rôle « Accès Dashboard », kill-switch mapping, `rolesMapping`/`usersMapping`, matrice). ⑥ **`feat/god-deslop`** — recette de carte unique (`GOD_CARD_BASE`/`<GodCard>`), surfaces refondues passées au kit `src/app/god/ui`, **plafond mesuré** du déslop God restant (86 fichiers, allowlist ESLint).
> **Preuves** : `tests/unit/onboarding-origin.test.ts` (14) · `module-lock.test.ts` (12) + 3 cas de verrou dans `user-context.test.ts` · `audit-retention.test.ts` (12) · `audit-taxonomy.test.ts` (8) · `god-guild-tabs.test.ts` (4) · `god-deslop.test.ts` (4) · suite complète **223 fichiers verte** · `tsc` **0** · `eslint` **0 erreur** · 5 PR vertes (CodeQL + Verify & Build) mergées dans `dev`.
> **Reste** : PR #742/#743/#744 à surveiller jusqu'au vert · **déslop God complet** (86 fichiers encore en palettes zinc/violet, allowlist `sigil/no-hardcoded-colors`) tracé ici, aucune surface refondue ne peut régresser (garde `god-deslop.test.ts`) · suites possibles non codées : `ServiceActivityLog` jamais purgé/exposé, `AccessAttempt`/`GodAccessLog` (écrits, jamais affichés), export avant purge.
> **🔭 Suite (25/09, **arbitrages tranchés**, non codée)** : refonte **UI/UX produit** de la console God Guildes & Users → **plan vivant `docs/plans/PLAN-REFONTE-GOD-GUILDES.md`** + **amorce de session `docs/plans/AMORCE-REFONTE-GOD-GUILDES.md`** (cadrage, **7 lots** détaillés avec fichiers, pièges, prompt à coller). **A1-A11 tranchés par le user le 25/09** — dont **A1** : le verrou de module est une propriété de **la guilde** (carte grisée, non activable, OFF effectif si actif, masqué en navbar, URL rebouchée, bot refusé, tout côté serveur) et **`bypassModules = isGod` reste** (ne pas le « corriger ») ; **A2/A3** : message « Indisponible — maintenance » **sur la carte** + **nouvelle vue God « Modules »** (verrou global toutes guildes + message perso) ; **A7/A8** : suppression du mort (carte « Comptes (Plateforme) », `LifecyclePanel`, placeholder) ; **A9/A10/A11** : **un seul** écran de logs. Cinq défauts **mesurés** y sont consignés : ① un God lit « pas la permission de lire le journal de cette guilde » (G7 — un `null` de `getUserContext` s'affiche comme un refus) ; ② le verrou de module **n'atteint pas encore la carte de la guilde** (carte non grisée, toggle encore cliquable, libellé « Verrouillé par le staff », pas de message perso) — c'est la demande **A1/G8**, et **`bypassModules = isGod` doit rester** ; ③ l'onglet **Sécurité** affiche un **placeholder mort** (`LifecycleServer`, `page.tsx:573`) qui renvoie à `LifecyclePanel`… **jamais monté** (composant orphelin de ~800 lignes) ; ④ la carte **« Comptes (Plateforme) »** lit `User.deletionRequestedAt`/`scheduledDeletion`, **colonnes qu'aucun code n'écrit** (mesure locale en lecture seule : **0** utilisateur, **0** guilde, **0** profil en soft-delete) ⇒ « La plateforme est propre » est un **faux positif**, et la purge est attribuée au *Janitor* alors qu'elle appartient au cron `account-retention` ; ⑤ **deux entrées de nav** (« Sécurité & Logs » `?tab=security` et « Audit Logs » `/god/logs`) affichent le **même** `AuditLog`, et la navigation du God est journalisée **deux fois** (`AuditLog.GOD_DASHBOARD_ACCESS` = **748** lignes dont 72 % du journal local, `GodSessionLog` = **490**).


## 🧩 Session 25/09/2026 (Rush Sylvestre) — **Métadonnées de bloc normalisées (séparateur + tips) · Champs X et Y dédiés · Presse-papier unifié en /travel** → branche `feat/rush-block-meta-travel`
> **Demande** : « l'ajout de lien et de position dans les blocs de type separateur coté god rush sylvestre est trop en roue libre , il me faut un vrai truc pour placer proprement un lien cliquable dans ce bloc , une position , que les positions soient tjres au meme endroit dans le guide dashboard/overlay etc » puis « Position / Coordonnées Dofus : moi je veux juste avoir à rentrer x et y et le presse papier prend tjrs en compte /travel x,y » et « idem dans les quete... ».
> **Fait** : `rush-rich-meta.ts` (parseur & formateur pur sans migration : coordonnées, lien nommé/brut, note) · `RushBlockMetaEditor.tsx` (deux champs distincts `X:` et `Y:`, lien avec nom et aperçu direct) · Studio GOD : intégration sur Séparateur et Conseil/Tips (création + édition), et section 2 « Destination & Coordonnées GPS » dans l'édition de quête (`SequenceEditForm`) avec inputs `X:` et `Y:` dédiés · rendu unifié (`normalizeMeta`) : dashboard membre, guide public, overlay PiP et bandeau · presse-papier `/travel` : `parseCoordinates` génère systématiquement `/travel x,y` (ou `/travel x y worldId`).
> **Preuves** : `npx tsc --noEmit` **0 erreur** · tests complets **219 fichiers / 2 435 tests ✓** · `eslint` **0 erreur**.

## 🧩 Session 25/09/2026 (SEO, lot 3 + guide Sylvestre) — **hreflang trompeur retiré · pages clés contrôlées au déploiement · case `Caddyfile` de `main` · rotation d'`activeContext` · le contenu du guide Sylvestre est verrouillé derrière un accordéon** → branche `fix/seo-derniers-restes`
> **Demande** : « c'est à faire ou pas ça ? » puis « purée j'ssaie mais je comprends rien » → lot exécuté **sans arbitrage user** (4 items sans risque) ; puis « les noms de quêtes du guide Sylvestre, ça peut amener du trafic ? ».
> **Fait** : `layout.tsx` → hreflang (`languages`) **supprimé** (mesuré : `/?lang=en` canonicalisé vers `/` ⇒ signal ignoré ; le sélecteur de langue reste fonctionnel) · `seo_check` (deploy-cd.sh) → contrôle du **statut HTTP** des pages clés (`/`, `/guides`, `/guides/rush-sylvestre`, `/boss`) · `SEO_REPRISE.md` → case « relire le `Caddyfile` de `main` après le merge » · `activeContext.md` → **rotation appliquée** (12 → 6) · **(suite, branche `fix/i18n-fr-par-defaut`)** `i18n/server.ts` → **détection `accept-language` retirée** : Googlebot (en-US) recevait l'**anglais** sur une URL dont le canonical est FR ; aucune requête EN dans Search Console ⇒ FR par défaut, langue choisie par sélecteur ou `?lang=` (jamais de langue différente pour un robot).
> **Mesures (guide Sylvestre)** : page publique en **`noindex, follow`** (construction), **0 nom de quête** dans le HTML ; données prêtes côté serveur (**400 Ko**, **374 liens DPLN**, passées en props ⇒ SSR) **mais** `PublicRushGuideClient.tsx:1423` = `{isExpanded && …}` avec `expandedMs` initialisé sur le 1ᵉʳ milestone ⇒ **les quêtes des chapitres repliés ne sont pas dans le HTML** (invisibles pour Google).
> **Preuves** : **218 fichiers / 2 428 tests** ✓ (+4) · `tsc` **0** · `eslint` **0 erreur**.
> **Reste** : chantier « guide Sylvestre indexable » (SSR de tous les chapitres, liens sortants limités, mode construction levé quand prêt) — **non codé, à trancher**.
## 🧩 Session 25/09/2026 (SEO, lot 2) — **Brouillon en vraie 404 · dates du sitemap honnêtes · origine publique explicite · checklist de bascule routée** → branche `fix/seo-finalisation`
> **Demande** : « ok fais le » (suite du lot 1, PR #734) — Search Console confirme déjà l'exclusion de la vitrine (`noindex détecté dans la balise Meta robots`, testé le 25/09 à 00h33).
> **Fait** : `guides/[slug]/page.tsx` → `meta.draft` dans la condition `notFound()` (brouillon = **404**, plus de 200 sous `noindex` seul) · `sitemap.ts` → **aucun `lastModified`** sur les 15 routes statiques (dates vraies conservées : guides, `guild.updatedAt`, boss, almanax) · `utils.ts` → `resolveAppBaseUrl()` pure (priorité env explicite, **jamais** l'en-tête `Host`) + `logger.warn` unique si aucune origine (fin du repli silencieux) · **routage doc** : `AGENTS.md` §2 → `docs/plans/SEO_REPRISE.md` ; `docs/MAINTENANCE.md` § Production → les **3 verrous à retirer** le jour de la bascule (sinon `sigilos.fr` invisible à vie).
> **Preuves** : **217 fichiers / 2 422 tests** ✓ (+7) · `tsc` **0** · `eslint` **0 erreur** · `tsc` avait attrapé un vrai défaut de typage (`ProcessEnv`) → variables passées explicitement.
> **Reste** : PR à merger · non traité : hreflang `?lang=en`, tests HTTP réels (Playwright inutilisé), `Caddyfile` absent de `main`.
## 🧩 Session 24/09/2026 (SEO) — **La vitrine `sigilos.fr` sort de l'index · le robots.txt de la prod était périmé ET cassé (20 jours) · le déploiement détecte désormais un proxy Caddy en retard** → branche `fix/seo-vitrine-hors-index`
> **Demande** : « j'ai rien compris au jargon et à ce qu'on peut faire pour améliorer le SEO aux meilleurs standards » — après le plan d'une IA SEO externe (sans accès au dépôt), croisé avec `docs/plans/SEO_REPRISE.md` et `temp/SEO-GSC-ETAT-MESURE-2026-09-21.md`.
> **Mesures (`curl` en direct, 24/09/2026)** : la bêta est **saine** (`robots.txt` 200 `text/plain`, `sitemap.xml` 200 `application/xml`) ; `sigilos.fr/robots.txt` **et** `/sitemap.xml` renvoyaient **la vitrine HTML** en 200, et `/zzz-inexistant` aussi (**soft-404**) ; le correctif `handle /robots.txt` était **sur le disque du VPS mais pas dans le conteneur** (compose montant **un fichier** : inode figé à la création) → inerte depuis le **04/09**, 2ᵉ incident après `/assets/*` (23/08) ; **sonde Caddy locale** : `respond "…\n…"` est servi **littéralement** (82 o sur une seule ligne → `robots.txt` illisible), le **heredoc** donne un vrai fichier (76 o, 5 lignes).
> **Fait** : `Caddyfile` → `X-Robots-Tag: noindex, nofollow` sur tout `sigilos.fr`, `robots.txt` en **heredoc** (sans ligne `Sitemap:` trompeuse), `sitemap.xml` → **404** · `public/maintenance.html` → `noindex, nofollow` · `scripts/deploy-cd.sh` → **`caddy_config_check`** (md5 dépôt ↔ conteneur, validation puis recréation automatique) + **`seo_check`** (ce qui est réellement servi) · docs : `MAINTENANCE.md` (récidive + automatisme) et checklist de bascule `SEO_REPRISE.md` (retirer le noindex le jour de la prod).
> **Preuves** : **216 fichiers / 2 415 tests** ✓ (+10) · `tsc` **0** · `eslint` **0 erreur** · `caddy validate` sur le Caddyfile réel → **`Valid configuration`** · sonde de comportement : `/robots.txt` 200 `text/plain` **76 o multi-lignes**, `/sitemap.xml` **404**, `/assets/ui/logo-v2.png` **200 `image/png`** · artefacts de sonde **supprimés**.
> **Reste** : appliquer sur le VPS (`git pull` + `--force-recreate --no-deps caddy` ; ensuite automatique) · **non traité** : `lastModified: now` (15 routes statiques du sitemap), hreflang `?lang=en` (canonical identique), repli silencieux de `getAppBaseUrl()`, tests HTTP réels (Playwright installé, aucun e2e), `Caddyfile` absent de `main`.

## PR / Branche
- **Session UX/sync** → branche **`feat/rush-sylvestre-ux-v2`** (créée depuis `origin/dev` = `b185e5e61`, après merge PR #586) → PR vers `dev`.
- Merge propre : aucun chevauchement de fichiers avec `dev` (seuls `package-lock.json` + `sync-actions.ts` différaient dans `origin/dev`).


## NEXT (priorités — à faire)

### 🛒 Marché — état au 14/09/2026 (3 PRs mergées : #657 · #659 · #660 · **+ debug beta en cours**) — **0 tâche de code ouverte au plan §21**
> **Vague de debug beta (5ᵉ tour)** — branche `fix/marche-debug-beta-14-09` : **3 constats fermés** (offre Discord/RBAC, ligne fantôme « +15975 Effet », icônes Mêlée/Distance + audit) ⇒ suite **1293/1293** · `tsc` 0 · `eslint` 0 erreur · **0 migration**. **Constats restants** (diagnostiqués, mesures dans `debug.md`) : `Réglages → Marché` (3 sélecteurs de rôles + panneau « Durées, plafonds & rappels » à retirer — le God est déjà global, `updateMany` cross-guild **mesuré**), timeline des offres sous la fiche (**emplacement à trancher par le user**), désistement depuis la fiche du dashboard, capacité légendaire (texte du pouvoir à sourcer), image OG (titre `EFFETS` recouvert, ligne horizontale, pied « Annonce #… / vendeur » à retirer).
> Plan maître §21 : **168 cases cochées** / **16 non cochées** = 4 DoD-ops (`S1.25`, `S1.34`, `S4.0e`, `S4.12`) + 3 historiques (`S0.7`, `S0.8`, `S4.13`) + **9 = V1.1 `S6`**. ⇒ code **clos** ; il reste des **ops**, **2 validations** et **1 PR docs** (celle-ci).
- ⏳ **Ops user (bloquant le gel §6)** : **déployer** `b9079d02b` (`sudo ./scripts/deploy-cd.sh beta <sha>`) · **crontab `market-expire`** (`docs/MAINTENANCE.md:65` ; sans elle : ni expiration, ni rappel J+7/J+15, ni retrait J+20, ni purge) · **DoD visuelle** dark/light/mobile + `/god/market` · **`S4.12`** (2 comptes Discord, double clic simultané) · **tags du salon forum** + association (`Réglages → Marché`) · **libellés `S1.25`** · bouton God **« Rattraper les effets natifs »** · (facultatif) bouton God **« Siphon items »** pour remplir `superTypeId` (**21 748 NULL** aujourd'hui ; le Marché tient déjà via le repli `typeId` → `typeName`).
- 🎨 **Assets A3 (à valider avant intégration)** : `uiassets_assets_*/auctionHouse{Equipments,Cosmetics,Resources,Consumables}.png` (familles de filtres) · `uidarkstone_assets_all/icon_slot_rune_forgemagie.png` + `uiassets_assets_2x/auctionHouseRunes.png` (bloc Forge) · `icons_assets_2x/rune.png` / `shop.png` (option). `kamas.png` / `hammer.png` = **déjà intégrés**.
- 🔎 **Résidu code (option)** : 4 `<Image>` sans repli d'erreur (`market-create-client.tsx:1141,1456,1550,1681`) → harmoniser sur `MarketItemIcon`.
- 📌 **Infra** : `T15` (CSS `MIME text/plain` = purge cache/CDN, **aucun code**) · `npm run seed:docs` / `build:seeds` sont **inutiles sur le VPS** (le `Dockerfile` les exécute au build, `docker-entrypoint.sh` joue les seeds au boot).
- 🚫 **Hors module** : V1.1 = **`WANTED` + matching seulement** (chantier séparé, sa PR, sa migration additive) — le reste de `S6` est **écarté**.
- 🔬 **Harnais réutilisables** : `_shot-fiche.mjs` (JWT Auth.js **forgé** + Playwright sur `next dev` : captures réelles dark/light/mobile) · `_probe-og-card.mjs` (carte OG + Sharp) · `_probe-marche-etat.mjs` (comptages en base) · `_probe-cosmetic-noise.mjs` (bruit `0 → 0`). ⚠️ Piège PowerShell : les dossiers `[guildId]` sont des **caractères génériques** ⇒ `-LiteralPath` obligatoire.

- ⚠️ **Prod/Infra** — `prisma migrate deploy` sur l'env **déployé** : en local c'est réglé (`resolve --applied` sur `20261009`-`20261013`). À refaire là-bas si même cause « already exists ».
- 🔴 **Ops** — Ménage `Songes_Pour_Les_Noobs/` (177 f, 8.4 Mo) → `git rm -r --cached`.
- 🟠 **Data** — 15 `unresolved` restants (Feuille de Salace, Mesure de poivre, Bière du Chabrulé, Poiskaille en Fricassée, Kamas, « L' », phrases « ou 1 x »…) : pas d'équivalent `name.fr[]` DofusDB → mapping manuel ou acceptation.
- 🟠 **Data** — Intégrer les 15 quêtes « Apprentissage » (Ordres) : hors des 374 séquences ; mapping `alignReq`/camp requis.
- 🟡 **UI** — Liens NOOBS/DOFUSDB → modale (aujourd'hui nouvel onglet).
- 🟡 **UI** — Responsive : auto-replier aussi la barre de chapitre / le bloc recherche sur très petite hauteur ; mode compact en fallback auto très petit.
- 🟡 **Perf** — Factoriser `getSequenceHelpers` par guide / agréger « qui peut aider » par chapitre.
- 🟣 **Tech** — Nettoyer `as any` (~70 admin, ~64 timeline) ; confirmer suppression `RushOverlayQuestPanel.tsx` (orphelin).


## Références clés
- 🎨 **SOURCE D'ICÔNES OFFICIELLE (permanente, tout module)** : `<Bureau>/dofus_assets` (~23 770 fichiers : icônes 1x/2x, items 2x, sorts, monstres, UI…). On peut y piocher des icônes pour n'importe quel module, **UNIQUEMENT après les avoir proposées au user ET validées** avant intégration.
- Backlog canonique : `docs/ROADMAP.md` (à lire en premier en mode plan).
- 🧭 **Amorce de session générique** : `docs/agents/session-amorce.md` (contexte + **checklist sécurité non négociable** + méthode de preuve + vérifs + DoD + **bloc à coller** §10) — **remplacée le 20/09/2026 par `AGENTS.md`** (racine), lu automatiquement par les assistants.
- 🛒 **Marché — amorce de la phase de debug** : `refonte-marche/AMORCE-DERNIERE-PHASE-DEBUG.md` (+ `AMORCE-DEBUG-A-COLLER.txt`).
- Demandes ouvertes + annotations : `docs/ROADMAP.md` (source unique).
- Plan maître #223 : `refonte-long-terme-discord-compatibilite/PLAN-MAITRE-RESILIENCE-DISCORD-LONG-TERME.md` + `sigilos-discord-resilience.md`.
- 🛰️ **Indépendance totale (fiches boss/monstres, simulation, icônes) — AMORCE PRÊTE (15/09/2026)** : `amorces/amorce-2026-09-15-independance-totale.md` — 6 lots (stale-while-offline → TTL par dataset → `local-only` strict → icônes siphonnées + cache durci → siphon **tous les monstres** → fraîcheur visible), 6 décisions à trancher (D1→D6, recos incluses), **critère « test de panne »** (`DOFENSIVE_OFFLINE=1`/`DOFUSDB_OFFLINE=1` : zéro spinner, zéro icône cassée, zéro appel sortant). Contexte : local-first **pas** local-only ⇒ une ligne périmée (> 24 h) déclenche une bascule live silencieuse (c'est l'incident Dofensive down). `refonte-siphon/PLAN-REFONTE-SIPHON.md` §5.3 (limiteur anti-ban) s'applique aux lots 4-5.
- Dernier mémo détaillé : `memo-2026-09-14-marche-d49-q15.md` (D49 + Q15) · `memo-2026-09-14-marche-embeds-cartes.md` (#657/#659) · archives : `memo-2026-10-06-chantier-fiche-boss.md`.


## Vérifications globales habituelles
`npx tsc --noEmit` 0 · `npm run test:run` vert · `npm run build` OK · pas de `console.*` en prod.

