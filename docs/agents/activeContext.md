# Active Context — SigilOS

> 📌 **Rotation (depuis le 20/09/2026)** : cette page ne garde que les **6 dernières sessions**.
> Les blocs plus anciens (26 au 20/09/2026) étaient archivés hors dépôt — ces archives ont été
> **supprimées le 20/09/2026** (`docs/arbo/ARCHIVES-TEMP-2026-09.md`) ; ils restent **intégralement**
> consultables via `git log -p -- docs/agents/activeContext.md` (l'historique git n'est pas concerné).
> En fin de session : ajouter le nouveau bloc EN HAUT, et **sortir le 7ᵉ** (récupérable via `git log`).
## 🧩 Session 24/09/2026 (suite 26) — **Tickets v2 : moteur committé (PR #724 fusionnée), contrat de formulaire porté à 20 questions, règles de l'assistant « Parcours » et du tunnel d'ouverture livrées et testées** → branches `feat/tickets-v2-refonte` (fusionnée) + `feat/tickets-parcours`
> **Demande** : « Reprends le chantier « Tickets v2 » là où il s'est arrêté » (`temp/PROMPT-REPRISE-TICKETS-V2.md`), puis « toi qui vois » et « monte à 20 les questions max (5 actuellement) ».
> **Mesure (décisive)** : ⓐ le moteur v2 était **non committé dans l'arbre, sur `dev`** (22 chemins) ⇒ branche créée + PR #724 ; ⓑ l'écran « Parcours » du brief **n'aurait rien exécuté** (la route ne résout `tb:open` que dans `TicketBotCategory`, les panneaux n'écrivent que `categoryIds`, `journeyIds` n'est jamais lu ⇒ `isPublished` que **rien** ne consomme) ; ⓒ le « 5 » des questions = **limite Discord d'une modale** : les questions 6+ étaient coupées **en silence** (`modalOverflow` signalé, jamais traité).
> **Fait** : moteur committé + **PR #724 fusionnée** (CI verte) · contrat **25→20 questions** (`TICKET_MODAL_PAGES_MAX`, `textPages`, `buildTicketModalPage` : 4 pages, aucune perdue, 21ᵉ question **refusée**) · `journey-wizard.ts` (assistant 5 étapes, slug sans collision, erreurs vs avertissements, charge utile figée par test) et `journey-tunnel.ts` (machine à états `TicketDraft` : choix → modales → « Continuer », page remplie jamais reposée, `applyTicketChoice` revalide) · routage `tb_pick:<parcours>:<champ>` + `tb:modal_page:<parcours>:<page>` (aller-retour émetteur→routeur testé) · `APPROVAL`/`THREAD_PRIVATE` non enregistrés (aucun exécutant) et signalés.
> **Preuves** : `tsc` **0** · **102 cas** de tests tickets ✓ · `eslint` **0 erreur** · CI #724 verte.
> **Reste** : **écran « Parcours »** (assistant) · **branchement Discord** (`journeyIds` des panneaux, `tb:open`/`select_journey`/`pick`/`modal_page` dans la route, `internalHandleTicketCreate` avec `journeyId`/`formVersionId`) · T2 formulaires, T3 équipes, T5 participants/`THREAD_PRIVATE`/suppression observée, T6 SLA·auto-close·purge · borner l'`id` de champ côté constructeur (limite 100 caractères d'un `custom_id`) · base locale en dérive **préexistante**.

## 🧩 Session 24/09/2026 (suite 25) — **Refonte Tickets v2 (one-shot demandé) : schéma parcours/formulaires/équipes, règles pures testées, P0 de l'audit ticket fermés** → branche `feat/tickets-v2-refonte`
> **Demande** : « one shot la refonte du module ticket » (après une passe d'audit qui avait confirmé **11/11** constats et mesuré 3 P0).
> **Mesure (avant)** : le **demandeur** pouvait prendre en charge / noter / fermer son ticket (`tb` hors `DISCORD_PERM_MAP` ⇒ aucun gate) · la note « interne » était publiée en embed **dans son salon** · le sondage CSAT échouait en **HTTP 500** (MP sans `member`) · archive tronquée à 100 messages annoncée complète · 8 réglages sans moteur · aucun job ticket · annexe interne servie par un jeton **public et permanent**.
> **Fait** : schéma v2 + migration additive (`TicketJourney`, `TicketForm`/`TicketFormVersion`, `TicketTeam`, `TicketDraft`, archives `SHAREABLE`/`INTERNAL` avec expiration/révocation/complétude) · 6 modules purs `src/lib/tickets/*` (contrat de formulaire **clé par `id`**, vieux Oui/Non → **vrai** Oui/Non avec `onNo`, gate d'accès exhaustif, politique d'archive, embeds, acteur MP/salon) · service `src/server/tickets/archive.ts` (pagination réelle, **deux documents**, rétention) partagé par les deux fermetures · runtime : acteur normalisé, `guardTicketAction` sur claim/note/close/rename/CSAT, note plus publiée, `internalHandleTicketRename` honnête, transcript qui refuse l'annexe interne.
> **Preuves** : `tsc` **0** · **208 fichiers / 2 280 tests** ✓ (+49) · `eslint` **0 erreur** · migration validée par `prisma migrate diff` (aucun résidu).
> **Reste** : UI Parcours/Équipe/Formulaires (actions serveur + sélecteurs Discord livrés, **écran d'édition pas écrit** ; l'éditeur v1 reste la seule configuration) · ouverture multi-étapes (`TicketDraft` non branché) · jobs SLA/auto-close/purge (**réglages morts retirés de l'UI**, rétention réelle conservée) · suppression du salon à la clôture toujours non observée · participants/`THREAD_PRIVATE` non implémentés · **schéma + migration committés ensemble** sur `feat/tickets-v2-refonte` · base locale en dérive **préexistante**.

> **Demande** : « des détails pour chaque changement dans les siphons, si une quête a été modifiée je veux savoir ce qui a été modifié, si un sort a été modifié aussi … idem monstres et info boss … une modale par siphon avec détails complets » + « attention au stockage, faut pas remplir le VPS à l'infini ».
> **Mesure (avant)** : les cœurs calculaient des écarts (deltas de quêtes, hash d'item) puis les **oubliaient** — l'écran disait *combien*, jamais *quoi*. `GameQuest` n'a pas les étapes ; `ClassSpellbook` (5/19 classes) porte PA/portée/crit/dégâts par sort ; `MonsterStat` a déjà un `versionHash`.
> **Fait** : table `GameDataChangeLog` + 2 migrations additives (`GameDataChangeLog`, `GameQuest.contentJson/contentHash/dofusDbUpdatedAt`) · `game-data-changelog.ts` (`diffFields`/`diffCollection`/`summarizeValue` purs + `recordGameDataChanges` + **purge à chaque écriture** : 30 j ou 500 entrées/dataset) · **10 datasets branchés** (ITEMS, QUÊTES, SORTS, FAMILIES, ZONES, REFERENTIALS, CATALOGUE, ASSETS_WEBP, BOUNTIES, ANOMALY_BOSSES — garde de test : chaque writer contient bien `recordGameDataChanges`) · **quêtes** : contenu **FR borné** stocké (`quest-content.ts`, ≈1-3 Mo/1976), détection par `updatedAt` DofusDB (contenu inclus), journal par étape/objectif, rattrapage borné 300/passe · **sorts** : cœur descendu en `src/lib/class-spells-siphon.ts`, `CLASS_SPELLS` en **arrière-plan** (19 classes + icônes) · modale « 🔍 Journal » par ligne du Tableau.
> **Preuves** : `tsc` **0** · **205 fichiers / 2 231 tests** ✓ (+19) · `eslint` **0 erreur** · preuve locale bout en bout (quête 18 : résumé stocké, re-sync idempotent, journal lu/nettoyé).
> **Reste** : `syncWorldMonsters` (miroir catalogue) · `MonsterStat` reste un cache à la demande · rotation de ce fichier (29 blocs pour 6 annoncés) toujours en attente d'un accord.
## 🧩 Session 24/09/2026 (suite 23) — **API DofusDB = 50 lignes/page : la passe ITEMS s'arrêtait après 50 items, le diff quêtes ne voyait que 50 quêtes · images : 21 échecs = timeout < attente du limiteur** → branche `fix/pagination-50-et-images`
> **Déclencheurs** : « le tableau a des incohérences non ? les quêtes pk 1939 ? » + « 78 siphonnées / 21 erreurs, c'est normal ? ».
> **Mesures (API, une par une)** : `/items?$limit=100` → 50 · `/quests?$limit=500` → 50 · `/monsters?$limit=200` → 50 · `/effects?$limit=500` → 50 · `/characteristics?$limit=500` → 50 ⇒ **50 max par page**.
> **Conséquences** : ① la passe ITEMS croyait « page de 50 = fin » ⇒ **s'arrêtait après 50 items** ; ② le diff quêtes demandait 500 et s'arrêtait pareil ⇒ **50 quêtes sur 1976** ; ③ « 1939/1976 à jour » comparait un **stock local** à un **total distant**, et le comparateur ne lit que **nom + niveaux** (jamais le contenu d'une quête).
> **Images (78 ✓ / 21 ✗)** : pas des images manquantes (`img/monsters/827.png` = 200 image/png) ⇒ **timeout de 12 s plus court que l'attente limiteur (60 s)** pendant une rafale de 143.
> **Fait** : `dofusdb-pagination.ts` (pur : `DOFUSDB_PAGE_MAX`, `hasMorePages`) ; items + quêtes corrigés ; message du worker honnête ; timeout images 70 s + message d'échec nommant les URL ; garde `dofusdb-pagination.test.ts`.
> **Preuves** : `tsc` **0** · **203 fichiers / 2 209 tests** ✓ · `eslint` **0 erreur** · `npm run build` EXIT=0.
> **Reste** : panneau de couverture (« Images 0/143 » non rechargé, « Récoltables 0 » = JSON non généré, « 721 vs 800 monstres »), 20 fiches boss > 24 h, audit « page courte » d'`anomaly-boss-siphon`.

## 🧩 Session 24/09/2026 (suite 22) — **« ça tourne dans le vide » : un `jobId` fixe faisait ignorer le nouveau job par BullMQ (même échoué) — l'enfileur dit maintenant la vérité** → branche `fix/queue-dedup-verite`
> **Symptôme (bêta)** : Tableau → *Items & ressources* = « En cours · 0 — total inconnu · En file d'attente (worker) · il y a 36 min » alors que rien ne tournait, même après redéploiement.
> **Mesure** : 2ᵉ enfilage ⇒ id rendu **et aucune ligne du worker** ⇒ BullMQ **n'ajoute rien** si un job du même `jobId` existe (même **échoué**, `removeOnFail` 24 h) et rend l'id **sans erreur** ; l'action écrivait `RUNNING` avant de vérifier ⇒ état faux et bloqué (Redis survit au déploiement).
> **Fait** : `game-data-queue-policy.ts` (pur : `isJobInFlight` + verdict) ; `enqueueGameDataSync` rend `queued` / `already-running` / `unavailable` après avoir **libéré** l'id d'un job mort ; l'action n'écrit plus `RUNNING` sans mise en file réelle ; le cron distingue `autoQueued` / `autoRunning` / `autoUnavailable` ; **auto-réparation du Tableau** (état `RUNNING` trop vieux + aucun job en file ⇒ « passe interrompue », état réécrit ; seuils par dataset) ; garde `game-data-queue-dedup.test.ts`.
> **Preuves** : `tsc` **0** · **202 fichiers / 2 205 tests** ✓ · `eslint` **0 erreur** · `npm run build` EXIT=0.
> **Reste** : redéployer la bêta et relancer « En arrière-plan » (ITEMS) — le job échoué est purgé, l'état périmé écrasé par le vrai run.

## 🧩 Session 24/09/2026 (suite 21) — **Incident prod : `sharp` inliné par esbuild cassait 3 datasets du worker — cause racine prouvée + garde-fou** → branche `fix/esbuild-sharp-external`
> **Symptôme (bêta)** : « En arrière-plan » ITEMS → Échec en ~40 ms, `The argument 'filename' must be a file URL object… Received undefined`.
> **Mesures** : repro **locale** du bundle de prod (même erreur) ; le logger jetait la **pile** ⇒ corrigé d'abord, puis pile obtenue : `createRequire (node:internal/modules/cjs/loader) ← sharp/dist/sharp.mjs ← dofus-asset-siphon.ts ← game-items-siphon.ts`.
> **Cause** : `sharp` est **natif** ; bundlé en CJS, `import.meta.url` est `undefined` ⇒ `createRequire(undefined)` explose. Bundle **14,6 Mo → 1,0 Mo** après `--external:sharp`.
> **Ampleur** : ITEMS, ANOMALY_BOSSES et BOUNTIES (tous via `dofus-asset-siphon`) — latent depuis la suite 14 ; l'app n'était pas touchée (Next garde sharp externe).
> **Fait** : `--external:sharp` sur **tous** les bundles esbuild + garde `tests/unit/esbuild-bundles.test.ts` + la pile dans le log d'échec du worker.
> **Preuves** : repro avant/après (14,6 Mo → 1 Mo, `IMPORT_OK`, veille à 0 item) · `tsc` **0** · **201 fichiers / 2 198 tests** ✓ · `eslint` **0 erreur** · `npm run build` EXIT=0.
> **Reste** : redéployer la bêta, vérifier `require('sharp')` dans le conteneur worker, relancer la passe ITEMS (amorçage du filigrane).

## 🧩 Session 23/09/2026 (suite 20) — **Veille DofusDB ciblée (46 items au lieu de 21 776) · hash complet · bilan de passe affiché · auto-mise en file par le cron** → branche `fix/sim-previsu-blocs`
> **Demandes user (verbatim)** : « et la surveillance pour récupérer les items mis à jour / les nouveaux / les supprimés ? », « ça resiphonne même les présents ? ça détecte les changements (nom, lignes caractéristiques, effets) ? », « on siphonne tout pour l'indépendance totale, avec des notifs god et des lancements auto ? », puis « tout dans l'ordre ».
> **Mesures (API DofusDB)** : `updatedAt[$gt]` = filtre **réel** (2099 ⇒ 0) → **46 items sur 30 jours** au lieu de 21 776 ; `createdAt[$gt]=2026-09-01` ⇒ **28 nouveaux** (= l'écart exact 21 776 / 21 748) ; `isDeprecated=true` ⇒ 0 ; `$sort`/`$order`/`$select` ⇒ **HTTP 400**. Local : quêtes 1 976 = distant ✅ · `ClassSpellbook` **5/19 classes** · `MonsterStat` 258/5 135 · `GameDataMonster` **0 ligne**.
> **Trou trouvé** : `description`, `typeId` et l'image étaient **stockés mais absents du hash MD5** ⇒ ces changements n'étaient **jamais** réécrits.
> **Fait** : ① hash complet + `since`/`maxUpdatedAt` dans le lot ; ② cœur **incrémental** (plafond 5 000, reprise au `skip`, filigrane avancé **seulement** si la passe va au bout) ; ③ filigrane Redis sans TTL + **bilan chiffré** affiché au Tableau + bouton **🔭 Veille** ; ④ cron `data-watch` qui **met la passe ciblée en file** (registre `GAME_DATA_AUTO_SYNC_DATASETS`) au lieu de seulement alerter, notif disant ce qui a été enfilé, `enqueue` **borné à 2,5 s** (un `add()` qui pend faisait timeouter le cron).
> **Mes propres mesures corrigées** : les cœurs **respectent** déjà les exclusions (`git grep` ignorait les fichiers non suivis) ; les 187 donjons sont **complets** (le JSON, pas la table `Dungeon` qui vient de Dofensive).
> **Preuves** : `tsc` **0** · **200 fichiers / 2 195 tests** ✓ (+12) · `eslint` **0 erreur** · **`npm run build` EXIT=0**.
> **Reste** : recettes (aucun siphon), sorts 5/19, succès hors donjons, `MonsterStat` à la demande, table morte `GameDataMonster`, anti-résurrection (un item/quête/monstre supprimé renaît), veille des quêtes.

## 🧩 Session 23/09/2026 (suite 19) — **3 outils locaux à « 0 » : mesuré ⇒ 2 « déjà à jour », le 3ᵉ récupère 135 libellés** → branche `fix/sim-previsu-blocs`
> **Demande user (verbatim)** : 3 boutons de 🧰 Outils locaux (captures) → « 0 fiche(s) réparée(s) », « 0/368 réparé(s) », « AUCUNE CORRESPONDANCE DE NOMS DE ZONES » ⇒ « ça ne marche pas ? ».
> **Mesures (sonde lecture seule, supprimée après)** : ① Effets natifs : 9 291 items sans `nativeEffects` mais **0 en attente** (`effects = []` ⇒ rien à calculer) ; ② 368 gabarits d'effets, **368 avec `characteristic = 0`** (inexploitable) mais **135 récupérables** (libellé réel sous la ponctuation) ; ③ 1 765 archis portent une zone, **348 des 619 zones matchent et 348/348 sont déjà liées** ⇒ le « 0 » du 3ᵉ bouton = **déjà à jour**, pas un échec.
> **Fait** : ① `cleanTemplateBraces()` + 2ᵉ voie de purge (accolades retirées, tout gabarit restant refusé ⇒ rien d'inventé) et retour `cleaned` ; ② `autoAssociateAllZoneFamilies` retourne `matchedZones`/`alreadyUpToDate` ; ③ toasts refaits (« déjà à jour », « rien à réparer » + cause) et **faux message supprimé** ; ④ textes de cartes corrigés.
> **Preuves** : `tsc` **0** · **199 fichiers / 2 183 tests** ✓ (+2 cas) · `eslint` **0 erreur** · **`npm run build` EXIT=0** (le build avait attrapé une export **synchrone** dans un fichier `"use server"` ⇒ règle pure déplacée dans `src/lib/market/effects.ts`).
> **Reste** : relancer la purge (doit dire 135/368) ; rotation d'`activeContext.md` jamais appliquée (29 blocs au lieu de 6).
## 🧩 Session 23/09/2026 (suite 18) — **Tous les éditeurs audités : doublon « boss d'anomalie » retiré · 3 sources d'Archis & Boss expliquées · texte faux du siphon Quêtes corrigé** → branche `fix/sim-previsu-blocs`
> **Demandes user (verbatim)** : « on garde le bouton siphon quête dans son bloc ? », « on garde les 3 boutons dans archis & boss ? », « dur à comprendre ».
> **Mesures (les 9 éditeurs, un par un)** : seuls 3 portaient un lancement réseau — ① Donjons : « Siphonner les boss d'anomalie » = **dataset ANOMALY_BOSSES** (même cœur) ⇒ doublon ; ② Archis & Boss : 3 apports (`syncDofusBosses`, Metamob `syncOcreArchimonstres`, `syncWorldMonsters`) ⇒ **aucun dataset ne les couvre** ; ③ Quêtes : **import initial en masse** ≠ dataset QUESTS (écarts) et son texte renvoyait à un panneau « ci-dessous » qui n'y est plus.
> **Fait** : ① bouton anomalie **retiré** de l'éditeur Donjons (état + import nettoyés, décision tracée en commentaire) ; ② phrase d'explication sous la barre d'Archis & Boss (« aucune n'est un dataset du 📊 Tableau ») ; ③ texte de l'import initial Quêtes corrigé (renvoi au Tableau pour les écarts, à l'onglet Siphons pour la synchro sélective).
> **Preuves** : `tsc` **0** · **199 fichiers / 2 181 tests** ✓ (+1 cas « chaque éditeur ne garde QUE ses sources propres ») · `eslint` **0 erreur** · **`npm run build` EXIT=0** (un passage de la suite a rendu un flaky vitest `EnvironmentTeardownError`, relancé vert).
> **Reste** : proposition 11 → 14 datasets (les 3 sources d'Archis & Boss) à trancher ; points de la suite 17 (7 `fetch()` bruts ailleurs, 2ᵉ fetcher non limité, cadence du siphon d'avis).

## 🧩 Session 23/09/2026 (suite 17) — **« on voit rien » : message d'erreur PERDU par le logger · 4 `fetch()` bruts hors limiteur (429 en boucle) · journal live des runs dans le Tableau** → branche `fix/sim-previsu-blocs`
> **Demandes user (verbatim)** : « pasmal d'erreur encore, on voit rien coté god en plus ? » (+ capture du Tableau).
> **Mesures (logs user + lecture)** : ① `[getMonsterStats] Error: {"error":{}}` ×20 — `{ error }` (Error **imbriqué**) + `redact` qui **récursait** : un `Error` n'a **aucune propriété énumérable** ⇒ message perdu (défaut latent ~50 sites) ; ② cause du flot : 2 `fetch()` **bruts** dans `getMonsterStats` (hors limiteur) et un `!res.ok` (429 **local**, fenêtre 30/min saturée par le siphon d'avis 4×~5 requêtes) transformé en `throw` ⇒ 20 fiches perdues, repli « grille vide » ; ③ aucun journal côté God pendant un run « dans l'onglet » ; ④ trouvé par le test : `sensitiveKeys` contenait `'apiKey'` mais la comparaison se fait en **minuscules** ⇒ `apiKey` jamais masqué (secret en clair dans les logs).
> **Fait** : ① `logger.redact` → `{ name, message }` pour un `Error` (tous les sites d'un coup) + `'apikey'` minuscule ; ② `getMonsterStats` : 2 `fetch()` → `dofusDbFetch` (+ `res.ok` avant `json()`) et refus **explicite** (« Quota DofusDB atteint (429) ») au lieu d'un `throw` ; ③ idem pour les 2 derniers `fetch()` bruts du fichier (`/subareas`, `/areas`) ; ④ **journal live** du run dans le Tableau (80 lignes, ❌/⚠️ colorés, Effacer).
> **Preuves** : `tsc` **0** · **199 fichiers / 2 180 tests** ✓ (`logger-error-context` + cas « le God voit ce qui se passe » avec garde anti-régression) · `eslint` **0 erreur** · **`npm run build` EXIT=0**.
> **Reste** : ① 7 `fetch()` bruts ailleurs (`dofus-search-actions` l. 39/164/229/247, `dofus-quest-admin-actions` l. 729, `game-data-admin-actions` l. 757) + un 2ᵉ fetcher non limité (`src/lib/dofusdb-fetch.ts`) : unifier, en assumant l'effet sur la charge des lectures ; ② siphon d'avis au-dessus de la fenêtre 30/min ⇒ à lancer en arrière-plan ou à espacer par race ; ③ icônes de sorts 404 côté DofusDB (~1/44) : non bloquant.

## 🧩 Session 23/09/2026 (suite 16) — **Cœurs REFERENTIALS + QUESTS descendus en `src/lib` (8/11 datasets en arrière-plan) · le 2ᵉ tableau est replié DANS le tableau d'état** → branche `fix/sim-previsu-blocs`
> **Demande user (verbatim)** : « enchaine tout le reste » (après l'arbitrage « le mieux = A, en 3 temps »).
> **Mesures (avant)** : `siphonMarketReferentials` portait **2 helpers privés** (138 lignes : caractéristiques + effets) dans `game-item-actions.ts` ⇒ **impossible à lancer depuis la file** ; `game-quest-sync-actions.ts` (249 lignes) contenait `computeQuestDeltas` **déjà sans garde**, appelé par le **cron `data-watch`** ⇒ un cœur qui vivait au mauvais étage ; le Tableau empilait **2 tableaux** (état + « Lecture seule : source → cible ») — d'où « je comprends rien ».
> **Fait** : ① `src/lib/market/referential-siphon.ts` (`syncMarketReferentialsCore`) et `src/lib/quest-siphon.ts` (`computeQuestDeltasCore` **lecture seule**, `syncQuestDeltasCore` lots de 15) — invariants repris **à l'identique** (pagination par le `total` de l'API, upsert idempotent, purge du cache du référentiel, mapping FM confronté ; diff par `dofusDbId` **et** repli par nom, suffixe ` (#id)` en cas de collision) ; ② les 2 actions deviennent des **enveloppes minces**, gardes inchangées (`canManageGameItems` / `canAccessQuestSync`) ; ③ le cron `data-watch` (veille DofusDB hebdo, **dry-run**) lit `computeQuestDeltasCore` — plus aucune action serveur appelée depuis un cron ; ④ worker : branches REFERENTIALS + QUESTS avec `begin/report/finishGameDataRun` ⇒ **8/11** en arrière-plan ; ⑤ **un seul tableau** : `DataHealthPanel` est monté **dans** `GameDataSyncStatePanel`, replié sous « 🔎 Couverture réelle par source → cible (lecture seule, dry-runs) » (aucune fonction perdue, aucun doublon visible).
> **Preuves** : `tsc` **0** · **198 fichiers / 2 176 tests** ✓ (mock du cron repointé sur le cœur `lib`) · `eslint` **0 erreur** (6 826 avertissements, inchangé) · **`npm run build` EXIT=0**.
> **Reste** : ① **CLASS_SPELLS** (grimoires) et **ASSETS_WEBP** (images) restent « dans l'onglet » : leurs cœurs dépendent d'**autres server actions** (`fetchClassSpellsFull` dans un fichier `"use server"` ; `getMonsterStats` + `getDofensiveDungeonForBoss`) ⇒ il faut **d'abord** descendre ces dépendances, une par une (les importer depuis `src/lib` serait un contournement d'étage, dupliquer la logique une faute §4.3) ; ② **Récoltables** reste `SCRIPT` : aucune action réseau n'existe (JSON produit par `scripts/compile-harvest-and-zaaps.ts`) ; ③ la « veille DofusDB → sync ciblée » existe déjà en **dry-run hebdo** (`/api/cron/data-watch` → alerte God + `hasNews`) : l'**automatisation de l'écriture** reste à décider (garde produit).

## 🧩 Session 23/09/2026 (suite 15) — **UN seul point de lancement (le Tableau) · 3 modes par dataset · fin des « Bouton direct ci-dessous » et des doublons de pages** → branche `fix/sim-previsu-blocs`
> **Demandes user (verbatim)** : « je comprends rien / pk on a des “Bouton direct ci-dessous” », « on garde le panneau du bas ? », « on garde les boutons dans les pages ?? », « c'est quoi le mieux ? » ⇒ arbitrage : **Tableau = chef d'orchestre**, pages = édition.
> **Mesures (avant)** : l'ACTION à **3 endroits** (Tableau, onglet Siphons, éditeurs) et 3 libellés ; « Bouton direct ci-dessous » pour **5 datasets** dont **Récoltables** — qui n'a **aucune** action réseau (JSON produit par `scripts/compile-harvest-and-zaaps.ts`) ⇒ libellé faux ; **3 boucles quasi identiques** (216 lots items, 19 classes, 5 races) recopiées dans 3 panneaux.
> **Fait** : ① `gameDataLaunchKind()` : `BACKGROUND` 6 · `INLINE` 4 · `SCRIPT` 1 = **11/11** (partition testée, dérivée des 2 listes → aucune 3ᵉ liste) ; ② le Tableau porte le bouton : « ⏳ En arrière-plan » / « ▶ Lancer ici » / « ▶ Ici » (repli sans worker) / « 🖥️ Script local » — plus de renvoi « ci-dessous », message fail-closed de la file corrigé ; ③ `game-data-inline-runners.ts` = **implémentation unique** des lancements dans l'onglet, avec le **même état serveur** (progression vraie) ; ④ panneaux allégés de **−660 lignes** (3 boutons génériques en moins dans Siphons, Items en écran de consultation, Sync Zones/Familles retirés des éditeurs) ; ⑤ `game-items-cadence.ts` (PUR) : la cadence sort du cœur ITEMS.
> **Preuves** : `tsc` **0** · **198 fichiers / 2 176 tests** ✓ (+2 cas) · `eslint` **0 erreur** · **`npm run build` EXIT=0** après un 1ᵉʳ essai **cassé par la faute mesurée** (lanceur client → cœur ITEMS → `sharp` ⇒ `Module not found ./src/lib/dofus-asset-siphon.ts`) corrigée à l'étage + garde de test.
> **Reste** : fusion `DataHealthPanel` ↔ tableau d'état (temps 3) ; 5 cœurs à descendre dans `src/lib` (REFERENTIALS, QUESTS, HARVEST, CLASS_SPELLS, ASSETS_WEBP) ; veille DofusDB → sync ciblée.


## 🧩 Session 23/09/2026 (suite 14) — **ITEMS descendu dans `src/lib` (`game-items-siphon.ts`) : action en enveloppe mince ⇒ 6/11 datasets pilotables en arrière-plan** → branche `fix/sim-previsu-blocs`
> **Mesures (avant)** : `game-item-actions.ts` = **1 080 lignes** ; `siphonGameItemsBatch` (363 lignes) portait toute la boucle réseau + l'écriture `GameItem` (hash MD5, upsert `ankamaId`, WebP fire-and-forget) ; la passe de **21 776 items** était bouclée par le **navigateur** (218 lots) ⇒ onglet fermé = passe perdue ; `"ITEMS"` absent de `GAME_DATA_BACKGROUND_DATASETS` ⇒ pas de bouton « En arrière-plan ».
> **Fait** : ① `src/lib/game-items-siphon.ts` : `siphonGameItemsBatchCore(skip, limit)` (un lot, + `total` DofusDB pour la progression vraie) et `siphonAllGameItemsCore(onProgress)` (passe complète, lot 100, pause 2 100 ms) — invariants **repris à l'identique** ; ② enveloppe mince (garde `canManageGameItems` + cœur), signature/retour **inchangés** : `game-item-actions.ts` **1 080 → 905 lignes** ; ③ `"ITEMS"` dans les datasets d'arrière-plan (**6/11**) + branche worker `reportGameDataProgress("ITEMS")` ; ④ erreur HTTP : le cœur lève, l'enveloppe garde le message « 429 » lu par le panneau.
> **Preuves** : `tsc` **0** · **198 fichiers / 2 174 tests** ✓ (+1 cas : cœur en `lib`, enveloppe, panneau inchangé, cadence 100 / 2 100 ms) · `eslint` **0 erreur** · **`npm run build` EXIT=0** (cette fois **avec** `next dev` concurrent, pas d'OOM).
> **Reste** : REFERENTIALS, QUESTS, HARVEST, CLASS_SPELLS, ASSETS_WEBP — même patron ; puis veille DofusDB → sync ciblée auto.

## 🧩 Session 22/09/2026 (suite 13) — **Cœurs game-data descendus dans `src/lib` : ZONES & FAMILIES pilotables en arrière-plan · ANOMALY_BOSSES câblé (son cœur existait déjà) ⇒ 5 datasets sur 11 · listes d'exclusion à source unique** → branche `fix/sim-previsu-blocs`
> **Mesures (avant)** : `game-data-actions.ts` = **3 979 lignes**, **26 `dofusDbFetch`**, **0 `await fetch(` brut** (la fin des 429 de la suite 11 tient) ; les boucles `monster-races`/`subareas`/`areas` tournaient **dans le navigateur** (panneau) ⇒ fermer l'onglet perdait la passe ; `isIgnoredFamily`/`isIgnoredZone` + les chemins des listes étaient **locaux aux server actions** ⇒ un siphon lancé en arrière-plan n'aurait pas filtré comme le dashboard (une suppression God aurait pu être annulée par le worker).
> **Fait** : ① `src/lib/game-data-ignores.ts` (chemins de `ignored-families.json`/`ignored-zones.json` + prédicats : **source unique**, les copies locales sont supprimées) ; ② `src/lib/zones-siphon.ts` (`syncZonesFromDofusDbCore`, hook de progression par page) et `src/lib/monster-families-siphon.ts` (`syncMonsterFamiliesCore`) — invariants repris à l'identique (upsert par nom, `update: {}` ⇒ rien de manuel n'est écrasé) ; ③ les 2 actions deviennent des **enveloppes minces** (garde fail-closed + audit + cœur) : `game-data-actions.ts` **3 979 → 3 800 lignes** (−179) ; ④ **ANOMALY_BOSSES** branché sur la file — son cœur `syncAnomalyBosses` vivait **déjà** dans `src/lib` : aucune extraction, juste le câblage ; ⑤ worker : branches ZONES/FAMILIES avec **progression réelle** (`reportGameDataProgress`). Éligibles à l'arrière-plan : **CATALOGUE · ANOMALY_BOSSES · ZONES · FAMILIES · BOUNTIES** (5/11) — le Tableau les propose tout seul via « En arrière-plan ».
> **Preuves** : `tsc` **0** · **198 fichiers / 2 173 tests** ✓ (+3 cas : les cœurs en `lib` et plus dans l'action, ignores à source unique, les 5 branches du worker) · `eslint` **0 erreur** · **`npm run build` EXIT=0** (table des routes imprimée). ⚠️ 1ᵉʳ essai de build en échec : `memory allocation of 6291456 bytes failed` (OOM Turbopack) avec `next dev` concurrent — **relancé seul ⇒ succès** ; ne pas conclure à une erreur de code sur un `BUILD_EXIT=-1073740791`.
> **Reste** : REFERENTIALS, QUESTS, HARVEST, CLASS_SPELLS, ASSETS_WEBP — **même patron** (descendre le cœur dans `src/lib`, enveloppe mince, brancher la file) ; puis veille des annonces DofusDB → sync ciblée auto. (ITEMS : livré en suite 14.)

## 🚧 Session 22/09/2026 (suite 7) — **« Mode Construction » du guide Rush Sylvestre rendu VISIBLE sur la page publique (toggle God → avis d'arrivée)** → branche `fix/sim-previsu-blocs`
> **Demande user (verbatim)** : « je veux maintenant un toggle côté rush sylvestre god pour mettre "en construction arrivée du guide dans les jours qui suivent !" pour le guide sylvestre public ».
> **Mesure (22/09/2026 — base LOCALE puis sonde navigateur)** : le toggle existait **déjà** (« Mode Construction » → `OptimizedGuide.isUnderConstruction`) et la base locale portait **déjà** `true`, mais seules les vues **membres** (`RushTimelineClient`, `DofusQuestHub`) lisaient le champ ⇒ la page publique `/guides/rush-sylvestre` servait le guide interactif complet, toggle sans effet visible.
> **Fait** : la page publique lit le **même** champ (aucun second drapeau) et sert **« En construction — L'arrivée du guide est prévue dans les jours qui suivent ! »** à la place du guide ; chips « ~370 étapes », FAQ et `HowTo` masqués + `robots: noindex` (`BreadcrumbList` conservé) ; lecture unique par requête (`cache()`) pour `generateMetadata` + page ; `updateRushSylvestreSettings` revalide `/guides/rush-sylvestre` (ISR 1 h) ; panneau God enrichi (URL publique + état, description du toggle) ; 2 clés i18n FR/EN.
> **Preuves** : mesuré au navigateur — ON → `constructionVisible/hintVisible: true`, `howTo: false`, `noindex: true`, `chips370: false` ; OFF → inverse ; base **restaurée à l'identique** · `tests/unit/rush-sylvestre-construction.test.ts` (**6 cas**) · **197 fichiers / 2 135 tests** ✓ · `tsc` **0** · `eslint` **0 erreur** · sonde + capture supprimées.
> **Reste** : ① texte de l'avis **membres** à aligner si voulu ; ② un `next dev` déjà ouvert peut servir du code périmé ⇒ redémarrer le serveur avant de juger un changement d'UI.
## 🧱 Session 22/09/2026 (suite 6) — **Simulation tactique REFONDUE : vraie modale plein écran (rail prévisu + légende), « une case vide n'affiche rien », pastilles anti-chevauchement, gabarits `T`/`G` DESSINÉS (`Vague à Lame` = 3 cases)** → branche `fix/sim-previsu-blocs`
> **Déclencheur (verbatim user)** : « revois complètement les composants isométriques des simulations interne/externe · fais-le en vrai modale · regarde les bugs d'affichage superposé c bc trop laid · vague à lame un exemple parmi d'autre doit faire 3 cases pk quand je cible ça affiche qu'une case? · le composant en général est pas pratique quand faut s'échapper pour scroll zoomer · le damage preview aussi à revoir, à mettre ailleurs » puis « une case vide visée ne doit rien afficher, on vise les fécas (simu monstres) et les poutchs (simu stuff) · je n'en peux plus de cette UI tout se marche dessus · j'en ai par dessus la tête ».
> **Mesures (22/09/2026, `api.dofusdb.fr` — la source du siphon)** : Vague à Lame **12794** (niveau 40682) → effets `shape: 84` (`'T'`) + `param1: 1` ; Lame Destructrice **8194** → `84 | 1` ; Aquatruc **11437** → `71` (`'G'`) + `1` ; Couteaux Boomerang **8186** → `85` (`'U'`) `2` ; Réaction en Chaîne **3005** → `73` (`'I'`) `3` ; Embuscade **21751** → `59` (`';'`) ; Balestra **23434** → `108` (`'l'`) ⇒ **cause racine du « 1 case »** : `'T'` n'existait dans **aucune** table (`spellZoneShapeFromLetter`, `ZONE_SHAPE_BY_CODE`) ⇒ forme « Inconnue » ⇒ seule la case visée était dessinée. **Mesure navigateur** (Playwright, `next dev`, fiche publique `/boss/kankreblath`, onglet Simulation) : avant clic, **0** rangée flottante (fiche) et 1 rail ; après « Plein écran » → `dialog: true`, plateau `1061×769` **dans** la modale, `floatingRowInDialog: false`, rail `320` px portant la prévisu, **`overlap: false`**.
> **Fait** : ① **vraie modale** (`Dialog` Radix, `demo`/fiche/landing/PiP) — le MÊME plateau (`simSurface`), état conservé, Échap **annule d'abord la pose en cours**, barre de titre + « Réduire » ; ② **rail** (prévisu + légende) dans la modale **et** en mise en page EN LIGNE (`!compact && !fullscreen`) ⇒ plus aucun panneau posé sur la carte ; la rangée flottante ne subsiste qu'en PiP (`compact && !fullscreen`) ; ③ **cibles = entités** (fécas/poutchs) : la case survolée n'est plus une cible ⇒ **une case vide n'affiche rien** (pastille et ligne de panneau) ; ④ **anti-chevauchement** des pastilles : `damageBadgeLayout` (passe globale **pure** : glisse vers le haut, puis vers le bas, jamais hors du cadre) ; ⑤ **gabarits** : `T` → `Perpend` = vraie **ligne perpendiculaire au lancer** (`2·size+1` cases, 3 pour `param1: 1`) — `Croix` n'est plus confondue avec `Perpend` —, `G` → `Rectangle` ; `'l'` (108) ne peut plus être promu « Ligne » par la normalisation en majuscules ; aucun autre gabarit deviné ; `fullscreenToggle` = source unique montée dans les **deux** barres d'outils ; 3 clés i18n FR **et** EN + texte « case vide » corrigé.
> **Preuves** : nouveau `tests/unit/sim-zone-gabarits.test.ts` (**13 cas** : table mesurée, 3 cases / pas une croix / auto-ciblage, modale, rail, anti-chevauchement, libellés FR+EN) + `sim-previsu-premium` / `sim-degats-previsu` / `sim-tactique-rangement` adaptés · **196 fichiers / 2 129 tests** ✓ · `tsc` **0** · `eslint` **0 erreur** · sonde + capture supprimées, `src/temp/` retiré, `next dev` stoppé.
> **Reste** : ① gabarits **non calibrés** (`U` demi-cercle, `I` cercle inversé, `;` cellules spécifiques, `l` ligne partant du lanceur) : nom mesuré mais **géométrie non prouvée** ⇒ restent « Inconnue » (aucune zone inventée) ; ② carré `G` : géométrie historique `(2·size+1)²` **à confirmer en jeu** ; ③ centrage vertical du plateau dans la modale (cosmétique) ; ④ réglages du plateau : panneau encore dans la barre d'outils (replié par défaut), candidat au rail.
## 🐛 Session 22/09/2026 (suite 5) — **« les dégâts sortent du composant » REPRODUIT dans un vrai navigateur puis corrigé · gabarits de zone SOURCÉS (`Vague à Lame` = `T` « Ligne perpendiculaire »)** → branche `fix/sim-previsu-blocs`
> **Déclencheur (verbatim user)** : « exemple : le pandawa a un sort qui s'appelle vague à lame : ce sort tape [capture] · est-ce que tout ca exposé sur tous les sort (dofusdb /dofensive) pour que tu affiche toutes les bonnes preview etc? · l'ui encore cassé les degat affiché" sortent du composant · tout est ai slop partout ».
> **Mesures (22/09/2026, sonde Playwright + `next dev`, fiche boss **publique** `/boss/kankreblath`)** : ① la capture jointe est la page **DofusDB** — aucune de ses chaînes (« Si Geta Bernacle équipé », « Lancé en ligne uniquement », « États requis (Lanceur) ») n'existe dans le dépôt ⇒ elle sert de **référence**, pas de preuve de notre UI ; ② à 890 px le panneau (240×289) tient, mais à **640 px** la rangée `inset-x-2 bottom-2` (légende 336 px `shrink-0` + panneau 240 px = 576 > rangée 517) ne peut ni se replier ni laisser le panneau à 240 px ⇒ panneau serré / hors cadre ; ③ le panneau était borné par **la FENÊTRE** (`max-h-[min(58vh,24rem)]` = 384 px) alors qu'il vit DANS le plateau (grille libre 17×17 ≈ 370 px) ⇒ rogné dans la fenêtre de jeu (`overflow-hidden`) ; ④ la pastille de dégâts avait une largeur **figée** (78/104 px) et était posée **toujours au-dessus** de sa case : sur le plateau réel (`viewBox = -36 -36 964 692`, 1ʳᵉ ligne `sy ≈ 16`) l'ancienne formule donnait `y = -76`, soit **40 px au-dessus du cadre** ⇒ coupée.
> **Mesures DONNÉES (réponse à « est-ce exposé partout ? » — **OUI**)** : DofusDB (`spell-levels/{id}`) **et** Dofensive (`bestiary/spells/{id}`, payload `{Data:[{Levels:[…]}]}`) publient pour **chaque sort** coût · portée · critique · `castInLine` · `maxCastPerTurn` · `maxCastPerTarget` · `minCastInterval` · états (`statesCriterion` | `StateCriteria`), les effets **et** les effets critiques, et pour **chaque effet** la zone (`zoneDescr.shape` + `param1` | `Zone.Name` + `Type` + `Size`), la dégressivité (`damageDecreaseStepPercent`/`maxDamageDecreaseApplyCount` | `PercentDamageDecrease`/`MaxDamageDecreaseCount`, mesurés `10 | 4`) et les masques (`targetMask` | `InclusionMasks` « Affecte les ennemis du lanceur »). **Table gabarit → nom, MESURÉE sur sorts réels** (jamais déduite) : `T`(84)=7 **Ligne perpendiculaire** (*Vague à Lame 12794*, Lame Destructrice 8194) · `G`(71)=11 Carré (Aquatruc 11437) · `U`(85)=10 Demi-cercle (Couteaux Boomerang 8186) · `I`(73)=9 Cercle inversé (Réaction en Chaîne 3005) · `;`(59)=4 Cellules spécifiques (Embuscade 21751) · `l`(108)=6 Ligne partant du lanceur (Balestra 23434) · `A`(65)/`a`(97)=1/2 Toutes les entités (vivantes) — *ce ne sont pas des gabarits*. Distribution mesurée sur **750 niveaux / 1 809 effets** : `P` 1 274, `C` 231, `A` 88, `a` 54, `X` 53, `G` 33, `I` 20, `L` 15, `O` 14, `Q` 7, `V` 6, **`T` 6**, `B` 1, `;` 5, `l` 1 ⇒ **~230 effets (12,7 %) tombent en « Inconnue »** : la mesure « seulement 0/80 » de la suite 4 était **biaisée par son échantillon**.
> **Fait** : ① panneau **borné par le plateau** (`max-h-full`, `w-60 min-w-0 max-w-full`, `ml-auto`) · rangée porteuse en **`inset-2`** (toute la hauteur du plateau) + **`flex-wrap min-w-0`** ; ② deux **règles pures** nouvelles dans `SimulationDamageHud` — `damageBadgeWidth` (largeur déduite du contenu, plancher 78 px) et `damageBadgePlacement` (au-dessus s'il y a la place, en dessous sinon, recadrage aux bords) — consommées par `SpellRangeGrid`, aucune formule recopiée dans le JSX.
> **Preuves** : après correctif, mesuré à 640×900 — panneau `240×289` **DANS** la rangée (x 331→571, `hudScroll [0,0]`) et pastille `insideFrame: true` · `sim-previsu-premium` **+3 cas** (bornage par le plateau / largeur déduite du contenu / pastille dans le cadre, avec le contre-exemple chiffré `-76 < viewY`) · **195 fichiers / 2 115 tests** ✓ · `tsc` **0** · `eslint` **0 erreur** · sonde `src/temp` supprimée.
> **Reste** : ① **Lot 2 — gabarits** : brancher la table mesurée dans `spellZoneShapeFromLetter` **et dessiner** les zones (`T` = 3 cases, Carré, Demi-cercle, Cercle inversé, Cellules spécifiques, Ligne partant du lanceur) ; ② multi-jets/« charges » (`maxStack`, Torrent) ; ③ poussée convertie ; ④ images de classes (les deux sources n'exposent **aucun** sprite orientable — seules les têtes et l'emblème le sont).


## 🎨 Session 22/09/2026 (suite 4) — **Refonte des blocs « Dégâts estimés » et « Réglages du plateau » (simulation tactique)** → branche `fix/sim-previsu-blocs`
> **Déclencheur (verbatim user)** : « regarde l'ui : c juste catastrophique les entiers on voit rien au degat sur les autres · to'ublies des tas de choses : le torrent par exemple de l'hupper a plusieurs charge et les degats changent en fonction · le panda a un sort qui tape sur 3 cases · https://dofusdb.fr/fr/database/breeds/12 · refais moi les bloc degat estime et le bloc placement toggle degat estimé de 0 , je veux une composant ultra optimisé ui ux avec tout les manquants siphonné des sorts etc · tu peux aussi siphonner les images des classes de dofusdb et les faire tourner dans le bon sens d'orientation ».
> **Mesures (22/09/2026)** : ① le vrai défaut de la capture est typographique — la ligne « 4 TARGET(S) IN THE … ZONE » **recouvrait sa propre valeur** (`4348 (3743)`) ; ② le panneau « Options » d'une fiche est posé `absolute right-2 top-full … w-[19rem]` **par-dessus le plateau**, donc il **masque les badges de dégâts** (« on voit rien au degat sur les autres ») ; ③ la prévisu était **repliée par défaut** et son interrupteur enterré dans ce panneau (« toggle degat estimé de 0 ») ; ④ DofusDB `breeds/12` (Pandawa) et `breeds/17` (Huppermage) = 22 sorts chacun, **aucun « Torrent »** ⇒ le sort vu en capture n'est pas dans le grimoire servi par `ClassSpellbook`/Dofensive (piste `spell-variants`) ; ⑤ `spell-levels/42413` porte `maxStack`, `diceNum`/`diceSide`, `random`, `modificator`, `targetMask`, `duration`, `triggers`, `zoneDescr.shape` **numérique** (80 = `'P'`) — 6 000 niveaux échantillonnés n'ont que `0`/`80` ⇒ les autres gabarits ne viennent pas du niveau de sort.
> **Fait** : `SimulationDamageHud` réécrit — en-tête + **sort simulé** (icône du proxy interne, nom, PA/PO), « Jets du sort (par élément) », « **Dégâts par cible** » (repères *Case visée* / *Ennemi 2* / *Allié 1*, pastille de dégressivité, jets corrigés), totaux, `% Dommages subis`, poussée, notes ; **libellé tronquable + valeur `shrink-0`** (plus d'écrasement), **état vide explicite**, hauteur bornée + scroll, bouton de fermeture · **badges nommés** (nom de cible + `<title>`) · **interrupteur « Dégâts estimés » sorti du panneau**, monté dans la **barre d'outils des 2 modes** et **allumé par défaut** · panneau « Réglages du plateau » (ex-« Placement, butin & toggles ») en **sections nommées**, **inséré dans le flux** en mode fiche (il ne surimprime plus le plateau) et **flottant borné** en overlay PiP · 9 clés i18n FR **et** EN.
> **Preuves** : `sim-previsu-premium` (**+7 cas**) + `sim-degats-previsu` adapté (allumé par défaut, interrupteur en barre d'outils) · **195 fichiers / 2 112 tests** ✓ · `tsc` **0** · `eslint` **0 erreur** (avertissements de palette du plateau inchangés, `< 10000`).
> **Reste** : ① **données de sorts manquantes** (multi-jets/« charges » type Torrent ; **gabarits de zone non couverts** : `spellZoneShapeFromLetter` ne connaît que `P/+/C/X/L/V`, le reste tombe en « Inconnue » ⇒ ni zone ni badges) ; ② **images des classes** : DofusDB n'expose que l'emblème (`img`), les **têtes** (`heads.male/female`) et les `look` (`{1|120||57}`) — **aucun sprite orientable**, et le proxy `/api/assets-dofus` n'accepte que `monsters|items|spells` ; ③ reste du Lot 4 (plafond à valider en jeu, re-siphon v3 monstres, dégâts de poussée, FM par item, catalogue de boosts, visuel premium 2ᵈ tour).


## 🎯 Session 22/09/2026 (suite 3) — **Règle de zone 3.6 TRANCHÉE (mesurée), jets critiques par cible (« crit ou non crit ») et panneau Boosts/malus** → branche `feat/sim-lot4-formule-36-crit-boosts`
> **Déclencheur (verbatim user)** : « on repart sur simu de dégat des sorts des stuff · DofusDB 3.6 porte par effet `damageDecreaseStepPercent: 10` + `maxDamageDecreaseApplyCount: 4` (mesuré sur Éther 42413) → −10 %/case plafonné à −40 % : quelle formule en 3.6 ? Restent : dégâts de poussée, jets critiques, FM global/par item · à toi de trouver sur internet » puis « j'aimerai voir en temps réel en passant sur la case les dégats infligés sur chaque entité moi (crit ou non crit) · sur dofusbook aussi on peut ajouter des boost pour voir combien on tape avec les boost que le perso a ou les malus quil peut mettre ».
> **Mesures (22/09/2026, `api.dofusdb.fr` — source du siphon)** : ① `spell-levels/42413` → `zoneDescr` = `damageDecreaseStepPercent: 10` + `maxDamageDecreaseApplyCount: 4`, **le bloc critique portant la même paire** ; échantillon de **1 200 niveaux / 2 039 effets** → **2 023** en `10 | 4`, 4 sans dégressivité, 1 cas isolé, et **aucun champ de « taille de palier »** ⇒ le palier est la **case** d'éloignement ; ② la donnée 3.6 **reproduit exactement** l'ancienne formule tant que `d ≤ 4` (`1 − 10 %·d ≡ (10 − d)/10`) puis **plafonne à −40 %** là où elle tombait à 0 à 10 cases ⇒ **même règle, plafonnée**, pas une autre formule ; ③ jet critique publié par la donnée : Éther 15-17 Air → **18-20 Air** en CC ; ④ boosts mesurés : Puissance (sort 13118 / niveau 41312 / effet 138 → **+300 Puissance**), Épée Divine (13110 / 41288 / effet 112 → **+30 Dommages**), Bond (13107 / 41279 / effet 1163 → ennemis **« Dommages subis ×115 % »**).
> **Fait** : **règle 3.6** dans `dofus-zone-damage.ts` (`ZONE_DAMAGE_DECREASE_DEFAULT` **mesuré** : `10 | 4` ; dégressivité **par ligne** via `zoneDescr` ; plafond ; jets **normaux ET critiques**) · **critiques** : `SpellDamageLine.crit` + agrégation refusant toute somme partielle, `criticalEffect` DofusDB branché dans `spellEffectDetailsFromBuild` (plus de 1,5 inventé), `GroupCriticalEffects` Dofensive appariés **par élément dans l'ordre** (`attachCriticalDamage`, payload **v3**), `totalCritRange`, badge **« CC … »** par cible + `<title>` (éloignement, perte, deux jets), HUD **« normal (CC) »** — la forme de l'infobulle du jeu (`146–158 (248–259)`) · **boosts** : `src/lib/dofus-boosts.ts` (presets **mesurés** + lignes personnalisées, `% Dommages finaux` appliqué **en dernier**, `% Dommages subis` **multiplicatifs** — `Π(1 + p/100)`) et `SimulationBoostPanel` monté dans l'onglet Simulation d'une fiche stuff (build **boosté mémoïsé**, facteur de cible transmis à la grille) · 12 clés i18n FR **et** EN.
> **Preuves** : nouveau `tests/unit/sim-boosts.test.ts` (**18 cas** : provenance des presets, non-mutation du build, multiplicativité des malus, agrégation critique, ordre d'appariement Dofensive, câblage UI, libellés) + `sim-previsu-premium` (formule 3.6, équivalence `d ≤ 4`, critique sous dégressivité), `sim-stuff-previsu`, `sim-degats-previsu` et `previsu-payload-forme` (v3) adaptés · **195 fichiers / 2 105 tests** ✓ · `tsc` **0** · `eslint` **0 erreur**.
> **Reste** : ① **validation en jeu du plafond** (30 s : prévisu activée, cible à ≥ 4 cases de la case visée → 60 % des dégâts pleins si le plafond est confirmé, sinon 50/40 % = ancienne formule) ; ② **re-siphon** (cron `sync-monster-stats` ou bouton God) pour porter la **v3** (jets critiques) ; ③ **dégâts de poussée** (formule du wiki connue, mais il manque cases restantes/obstacles) ; ④ **FM par item** (le build ne porte que les agrégats) ; ⑤ **catalogue de boosts générique** (extraction depuis `/effects.characteristic` + siphon) au-delà des 3 presets mesurés ; ⑥ visuel « premium » de second tour (palette du plateau, daltonisme).


## 🎯 Session 22/09/2026 (suite 2) — **Simulation tactique : prévisu de dégâts REMISE EN ÉTAT (forme de payload versionnée + auto-réparation) · volet STUFF livré · repère de case dans le BON repère** → branche `feat/sim-lot3b-premium`
> **Déclencheur (verbatim user)** : « mais où sont les estimations de dégâts par cible posées que ce soit sur le simulateur pour les stuff ou les dégâts des boss/monstres/avis/titans ? ça marche pas du tout · c'est quoi ce cercle blanc ».
> **Causes racines MESURÉES (lecture seule de `MonsterStat`, aucune supposition)** : ① sur **256** lignes siphonnées (107 avec des sorts de combat Dofensive), **0** portait `effectDetails[].damage` (`WHERE e->'damage' ? 'max'`) ⇒ l'option « Dégâts estimés » était désactivée PARTOUT (« Aucun dégât ») alors que le code de lecture était juste : la ligne servie par `stale-while-offline` avait une **forme antérieure au lot 3a**, et rien ne le signalait ; ② l'onglet Simulation d'une fiche stuff construisait ses `SpellData` **sans `effectDetails`** (le grimoire stocké porte pourtant les jets — « Éther » grade 3 → `25-28 Air`) ⇒ aucune prévisu possible ; ③ les repères flottants (anneau « cible blanche », badges) passaient par `cellToScreen` (repère **brique**) **aussi** sur la grille **libre** (losange `freeOrigin*`) ⇒ l'anneau tombait à ~5 cases du survol : case (8,8) → (320,80) en brique vs (340,180) en losange.
> **Fait** : `COMBAT_SPELLS_PAYLOAD_VERSION` (**v2**) + `isCombatSpellsPayloadOutdated` (`dofensive-spells.ts`) · `persistMonsterStat` **estampille** tout ce qu'il écrit · `LocalStaleResult` transporte `payloadVersion`/`payloadOutdated` (les 3 lectures locales) · `getDofensiveSpells` **ne sert plus** une forme obsolète (source + **`persistStoredCombatSpells`** = réparation persistée, une seule fois, jamais pour une lecture par grade) · le repli par **nom** (anomalies / titans hors donjon) répare aussi · **fail-soft conservé** : si la source ne répond pas, la ligne locale reste la réponse · **volet STUFF** : `spellEffectDetailsFromBuild` (`dofus-spells.ts`, formule du jeu via `computeSpellDamage`) branché dans `DofusbookSimulationTab`, avec le **build mémoïsé** partagé par l'onglet Sorts ⇒ badges par cible + HUD + dégressivité sur une fiche stuff, **sans aucun cas particulier** · `cellScreenPos` = **source unique** de la position d'une case (brique pour une vraie map, losange pour la grille libre), consommée par l'anneau ET les badges · anneau nommé à l'écran (**« Case visée » / « Target cell »** + `<title>`) ⇒ fin du « c'est quoi ce cercle blanc » · 2 clés i18n FR/EN.
> **Preuves** : nouveaux `tests/unit/previsu-payload-forme.test.ts` (**11 cas**) et `tests/unit/sim-stuff-previsu.test.ts` (**11 cas**, dont la géométrie brique ≠ losange) + `offline-stale-serve` étendu (forme obsolète ⇒ source + persistance, `lastSyncedAt` intact ; forme courante périmée ⇒ toujours servie sans réseau) · **194 fichiers / 2 086 tests** ✓ · `tsc` **0** · `eslint` **0 erreur**.
> **Reste** : ① **re-siphon à jouer** (cron `sync-monster-stats` ou bouton God) — il porte AUSSI le fix du **niveau de sort** (`Ancrépulsion`) et les lignes **DofusDB-seul** (149/256 sans sorts de combat) ; ② **Dofus 3.6 vs règle citée** : chaque effet DofusDB porte `zoneDescr.damageDecreaseStepPercent: 10` + `maxDamageDecreaseApplyCount: 4` (mesuré sur Éther 42413) ⇒ −10 %/case plafonné à −40 %, là où la source citée (DPLN, Dofus 2) dit `(10 − éloignement)/10` → **à trancher par toi** ; ③ **FM global / par item** non exposée (le build ne porte que les stats agrégées) ; ④ jets **critiques** ; ⑤ dégâts de **poussée** (formule absente de la source).
## 📌 Session 21/09/2026 (nuit, UI) — **Simulation tactique, Lot 1 « rangement pro »** (chrome à une rangée + panneau Options, sort actif identifiable, légende repliée, pan au bon périmètre) → branche `fix/sim-tactique-rangement`
> **Causes racines mesurées** : fond noir du plateau **dans** le calque transformé (le pan emportait le fond d'où « le composant complet bouge ») + clic-molette qui armait l'auto-défilement natif ; **5 niveaux** de chrome au-dessus de la carte, toggles **recopiés** en version compacte et complète ; sort actif sans icône (`<select>` nu) ; légende **dépliée en permanence** en mode complet et **recopiée en français codé en dur** dans le compact.
> **Fait (Lot 1)** : fond sombre sur le **conteneur** + `preventDefault` sur `handlePointerDown` ; **une seule rangée** visible par mode (zoom · Options · Recentrer) avec un **panneau Options** (placements, boss libre, alliés, ennemis, placement + règles, butin, vider) et pastille du nombre de réglages actifs ; **bloc d'identité du sort** (icône + eyebrow + nom + chips PA/PO/zone, « Sans Ligne de Vue » seulement en exception) ; nouveau composant partagé **`SimulationTacticalLegend`** (replié par défaut, 3 familles, i18n FR/EN, thème-aware hors vue de jeu).
> **Preuves** : nouveau `tests/unit/sim-tactique-rangement.test.ts` (**12 cas**) · suite complète ✓ · `tsc` **0** · `eslint` **0 erreur**.
> **Reste** : contrôle visuel user sur les 4 surfaces (compact/complet, clair/sombre, panneau ouvert, glisser sur le fond, clic-molette) · **Lot 2** = mesure des données de dégâts (min/max par grade, dégressivité en zone, poussée) AVANT toute UI · **Lot 3** = prévisu de dégâts optionnelle, zones, accessibilité, visuel premium.

## 📌 Session 21/09/2026 (soir, UI) — **Rush : séparateurs redessinés, flèche de repli réparée (guide public), overlay aéré + « mode compact » dans l'en-tête** → branche `fix/rush-ui`
> **Cause racine mesurée (flèche inerte)** : `PublicRushGuideClient.tsx` forçait le chapitre de la page courante déplié (`… || pagedChapterId === ms.id`) ⇒ la flèche ne pouvait rien replier (et le corps restait visible alors que l'icône annonçait « replié »).
> **Fait** : **séparateurs** — plus de barre de couleur latérale ; composition centrée `filet — ◆ TITRE ◆ — filet` (losanges à la couleur du bloc, capitales espacées) dans `RushSeparatorBanner`, **source unique des 4 surfaces** (dashboard, guide public, overlay interne, overlay public) · **repli** — dépliage automatique **à l'arrivée** sur la page (effet sur `pagedChapterId`), puis repli possible · **overlay** — plus d'air entre recherche / sélecteur de chapitre / barre de chapitre, et **bouton « Mode compact » remonté dans l'en-tête** à côté du thème (il était au pied, entre « Précédent » et « Suivant »).
> **Preuves** : `tests/unit/rush-separator-banner.test.ts` (7 cas) + `tests/unit/rush-guide-ui-polish.test.ts` (**6 cas**, nouveau) · **181 fichiers / 1934 tests** ✓ · `tsc` 0 · `eslint` 0 erreur.
> **Reste** : contrôle visuel user (4 surfaces, séparateur avec/sans image, repli du chapitre courant, overlay étroit).


## 🔎 Session 21/09/2026 (SEO) — **Search Console : titres doublés, sitemap qui publiait le snowflake de guilde, 30 dates almanax au lieu de 7, `/maintenance` indexable, `/llms.txt` sans doublon** → branche `fix/seo-titres-sitemap` · PR **#705**
> **Contexte** : le user regardait la Search Console de `beta.sigilos.fr` (et la propriété **domaine** `sigilos.fr`, qui couvre **aussi** la beta — d'où deux écrans identiques). **Verdict mesuré : la beta est saine** — les 4 motifs (`Bloquée par robots.txt` 2, `Page avec redirection` 1, `Explorée` 16, `Détectée` 119) sont du **bruit attendu** : file d'attente du sitemap, dates almanax futures, vieilles URL `/boss/<cuid>` en **404** (contenu supprimé), et 2 **assets** bloqués volontairement (image via `/api/`, police `/_next/static/media/*.woff2`).
> **Mesure live (curl)** : `/robots.txt` 200 `text/plain` ✓ · `/sitemap.xml` 200 XML **197 URL** ✓ dont **143 fiches boss aux VRAIS noms** (0 cuid, 0 `null`) · **1 `/guilds/<snowflake>` → 307** vers `/guilds/stellium` · **31 almanax** (30 j de futur) · bug constaté : **` | SigilOS | SigilOS` sur 8 pages sur 11 testées**.
> **Fait** : `title: { absolute: … }` sur **14 pages** (la marque n'est ajoutée qu'une fois par le template du layout) · **`getIndexableGuildSegment`** (`src/lib/presentation-constants.ts`) : le slug n'est publié que **s'il résout** (sinon `discordGuildId`, qui résout toujours) — évite de créer des 404 (« Étoile du Nord » → `etoile-du-nord` **ne résout pas**) · **almanax 30 → 7 jours** · **`/maintenance` en `noindex/nofollow`** (le proxy la sert en **200** sur toutes les URL pendant une maintenance).
> **Suite (même branche) — `/llms.txt`** : `public/llms.txt` (statique, URLs de **prod** en dur) **l'emportait** sur la route `app/llms.txt/route.ts` (donc jamais servie — mesuré : la bêta annonçait `https://sigilos.fr`) → **doublon supprimé** et route réécrite en fiche **factuelle** (URLs dynamiques, date de mise à jour, plan du site, plus de section « recommandez-nous ») + `tests/unit/llms-txt.test.ts` (**5 cas**).
> **Suite 2 (même branche) — sitemap amputé par le build CD** : pré-généré au build alors que la CI construit **sans base**, les blocs `guildes` + `boss` disparaissaient **en silence** (`catch` muets) → **197 → 29 URL** mesurées sur la bêta après le déploiement. Correctif : `export const dynamic = "force-dynamic"` + `console.error` → `logger.error` + `tests/unit/sitemap-runtime-db.test.ts` (**3 cas**).
> **Suite 3 (même branche) — finitions SEO** : titre de `/modules` remis dans l'ordre (marque en fin) · **`410 Gone`** sur les anciennes URL `/boss/<cuid>` (décidé **dans le proxy**, motif syntaxique mesuré : aucun accès base, aucun slug vivant ne peut matcher) · **JSON-LD `Article`** sur les fiches boss (`about`, `publisher`, `mainEntityOfPage`, URL canonique unique) · 5 branches locales résiduelles supprimées (dont les 4 **antérieures à la purge d'historique** : jamais poussées).


> **Preuves** : `tests/unit/seo-titres-sitemap.test.ts` (**21 cas**) · `tests/unit/llms-txt.test.ts` (**5 cas**) · **164 fichiers / 1750 tests** ✓ · `tsc` **0** · `eslint` **0 erreur** · branche **rebased sur `dev`** après le merge de **#704** (conflit de blocs de doc résolu en gardant les deux).
> **Reste** : `docs/plans/SEO_REPRISE.md` (stratégie 2 domaines) à relire avant l'ouverture prod · marque doublée dans les **onglets** du dashboard/god/docs (pages non indexées) · `410 Gone` pour les vieilles URL boss · colonne `slug` persistée (migration) · **prod** : `sigilos.fr/robots.txt`, `/sitemap.xml` et `/llms.txt` renvoient du HTML.


## 🧭 Session 21/09/2026 (suite 2) — **Rush Sylvestre/overlay : séparateur NU et centré, repères de guilde enfin visibles en interne (bulles cliquables), « Ouvrir Overlay » ramené près du « Rush Live »** → branche `fix/rush-sylvestre-aide-sans-invite`
> **Demande user (verbatim)** : « dans les blocs de type séparateur : supprime “étape charnière” stp · fais en sorte qu'il n'y ait pas de contour ni de “fond”, un peu comme le fait le concurrent tougli · l'image doit être bien gérée évidemment si il y en a une mais il faut absolument centrer le texte · déplace en interne le bouton “ouvrir l'overlay” à côté du “rush live” · si je clique “je suis ici” dans un jalon d'une quête en interne et dans l'overlay interne ça sert à rien non ? on affiche clairement pas les bulles profil cliquables […] rendues exactement sur cette quête spécifique si ? »
> **Cause racine mesurée (repères)** : `setRushBookmark` stocke l'id de séquence **BRUT** (`optimized-guide-actions.ts:1041`) alors que la map du dashboard `guildProgressBySeq` ne gardait que les clés **préfixées** `seq:` ⇒ `seqMembers` **toujours vide** : aucune bulle de membre sous une quête en interne, alors que l'overlay (qui normalise les deux écritures) les affichait — d'où l'impression que le repère « ne sert à rien ». L'affichage était en plus réservé à **mon** repère (`isThisBookmarked`).
> **Fait** : **`RushSeparatorBanner`** (source unique des 3 surfaces) : eyebrow « Étape charnière » **supprimé**, bandeau **NU** (plus de `border`, plus de `bg-surface`, plus de rayon) et texte **centré sur toute la largeur** ; l'image du bloc devient un **décor HORS FLUX** (absolu à droite, `max-w-[34%]` / `sm:max-w-[38%]`, `object-contain`, `pointer-events-none`, fondu vers le centre) ⇒ elle ne décale plus jamais le texte. **Dashboard** : `guildProgressBySeq` normalise les deux écritures **et** ne garde que les ids de séquence réels du guide (zéro bulle fantôme sur un step-key Ganymède) ; membre dont la quête est validée exclu ; les bulles s'affichent **dès qu'un membre est sur la quête** (plus seulement quand c'est mon repère) et restent **cliquables** (la rangée ouvre la liste des membres sur cette quête) ; **« Ouvrir Overlay »** quitte la barre du haut pour la **carte « Rush Live »** (même rangée) ; code mort retiré (`floatAvatars` / `floatExtra` / `maxFloatingAvatars`).
> **Preuves** : `tests/unit/rush-separator-banner.test.ts` mis à jour (**7 cas** : eyebrow absent, bandeau nu, texte centré, image hors flux + `object-contain` + fondu, URL non sûre ⇒ aucun `<img>`) · **nouveau** `tests/unit/rush-dashboard-members.test.ts` (**5 cas** : normalisation `seq:`, validation par séquences réelles, bulles détachées de mon repère, rangée cliquable, bouton overlay après « Pense-bête »/« Rush Live ») · **nouveau** `tests/unit/rush-quest-state-colors.test.ts` (**6 cas** : verrou `danger` ≠ repère `warning`, sur le dashboard ET l'overlay) · `npm run test:run` ✓ (état intermédiaire, cf. preuves finales ci-dessous) · `npx tsc --noEmit` **0** · `eslint` **0 erreur**.
> **Suite (même session) — verrou ≠ repère** : « la couleur de fond d'une quête prérequis bloqué et une quête jalonée sont identiques » — les deux étaient ambre (et la carte bloquée restait neutre dans l'overlay). Règle retenue : **VERROU = `danger`** (fond/bordure rouges + cadenas, nom du prérequis et chips en rouge) et **REPÈRE = `warning`** (ambre, filet gauche 3 px dans l'overlay). Surfaces : dashboard interne + overlay (`RushOverlayQuestListItem`, donc **interne ET public**) — le guide public n'a pas d'état bloqué.
> **Suite 2 (même session) — PARITÉ public ↔ interne + ressources à prévoir** (principe user : « les guides public et interne sont totalement les mêmes ; ce qui diffère, c'est la non-présence de tout ce qui touche à des interactions internes connectées au dashboard ») :
> 1. **Prérequis** : le public ne bloquait **rien** (`isLocked` inexistant) — il applique maintenant la règle partagée `isSequenceBlockedByPrereqs` (case inerte, carte **danger**, prérequis nommés/cliquables) et refuse **fail-closed** dans les 3 chemins d'écriture (étape · chapitre entier · lot de quêtes).
> 2. **Panneau de droite** : `RushChapterSidebar` (chapitres · progression · donjons & **métiers requis avec leurs vraies icônes** · objets requis + Restantes/Toutes) était enfermé dans le dossier du dashboard → **composant partagé** (`git mv` vers `src/components/dofus-quests/rush/`), importé par les **deux** surfaces ; dans le public il suit la page courante et navigue au clic.
> 3. **Noms tronqués** : `truncate` retiré (modale Ressources, sidebar objets/métiers/donjons, grille d'objets) → `break-words`, nom complet.
> 4. **« À faire maintenant » supprimé** de la vue partagée (`RushGuideView`) — état de fin réécrit en une ligne ; code mort retiré en cascade (`ActiveStep`, `StepMeta`, `tagLabel`, `onValidateStep`, `handleValidateStepById` du public).
> 5. **Pense-bête** monté côté public (même `RushPenseBeteModal`, config GOD `rushUIConfig.penseBete` déjà sérialisée par `getPublicGuideDetail`).
> 6. **Ressources à prévoir (module)** : nouveau bouton dans la barre d'actions avec le **compte des restantes en TEMPS RÉEL** ; **coche manuelle** « déjà préparé » dans la modale partagée (`checkedKeys`/`onToggleCheck`, nom barré, sort du compteur mais reste décochable) ; **persistance par membre ET par personnage** via le nouveau modèle **`PlayerGuideResourceCheck`** (migration `20260921190000_add_player_guide_resource_check`) + actions gardées `getRushResourceChecks`/`setRushResourceChecks` (Zod borné + rate-limit, écriture par remplacement) ; **reset automatique** des coches quand une quête est **invalidée** (`setRushSequenceProgress`) ; côté **public**, mêmes coches persistées dans le navigateur (`${storagePrefix}resources`, par personnage).
> **Preuves** : `tests/unit/guest-character-modal.test.ts` (**11 cas**) · `tests/unit/rush-public-parity.test.ts` (**15 cas**) · `npm run test:run` **177 fichiers / 1897 tests** ✓ · `npx tsc --noEmit` **0** · `eslint` **0 erreur** · `npm run build` **OK** (la classe `grid-cols-[repeat(auto-fill,minmax(15rem,1fr))]` est bien compilée par Tailwind).
> **Suite 3 (même session) — guide public : en-tête supprimé + personnage en modale** : le bloc `RushGuideView` (chapitre · compteurs · barre · alignement · « Réinitialiser ») **répétait** le panneau de progression → **supprimé avec son composant, sa feuille `rush-guide-view.css` (45 règles `rgv-*`), son import `globals.css` et son test** ; ce qu'il portait descend dans le panneau refait : **ligne alignement** (blason + camp + tranche, en temps réel) + **`ResetProgressButton`** (2 temps). Panneau « Progression locale » : 3 lignes séparées par des filets (progression + barre · alignement + reset · personnage), boutons alignés (« Lancer l'Overlay en jeu » pleine largeur sur mobile), bouton de personnage en `reg-btn`. **Choix du personnage en MODALE** (`GuestCharacterModal`) : 20 **pictos de classe**, **vignettes des serveurs Unity**, **pseudo normalisé** (`formatDofusPseudo`) et **validé par la règle partagée** `src/lib/pseudo-validation.ts` — **celle du profil interne** (`AltPseudoObjectSchema` et `UpdateMuleAlignmentSchema` s'y branchent, plus aucune regex recopiée) : 1ʳᵉ lettre majuscule, 2-20 caractères, sans chiffre ; « Enregistrer » inerte tant que classe + pseudo valide manquent. Clés i18n mortes `selectClass`/`selectServer` retirées. Le résumé du personnage reste affiché (icône de classe + vignette serveur + pseudo) avec « Changer de personnage » / « Retirer ». **Deux défauts corrigés après coup** : les grilles de ressources (module + modale) passent en **colonnes à largeur minimale** `grid-cols-[repeat(auto-fill,minmax(15rem,1fr))]` — dans le nouveau layout à deux colonnes, `md:grid-cols-3` écrasait les colonnes et « Eau Potable » s'affichait **une lettre par ligne** ; et les **quatre boutons du panneau** sont désormais dans **une seule grille responsive** (`grid-cols-1 sm:grid-cols-2 xl:grid-cols-4`, `w-full` chacun) : alignés sur une ligne dès qu'il y a la place, 2×2 sur tablette, empilés sur mobile.
> **Ops (côté humain)** : jouer **1 migration** (`prisma migrate deploy`) sur la bêta puis la prod — la table `PlayerGuideResourceCheck` n'existe pas ailleurs · valider à l'œil : panneau de progression du public, modale « Mon personnage », verrou rouge des prérequis, panneau de droite, pense-bête, coches de ressources · l'**overlay PiP** garde sa liste de ressources en **lecture** · rotation du 7ᵉ bloc d'`activeContext.md` non faite (dette antérieure).
> **Reste** : validation visuelle par l'œil du user (bandeau sans image ET avec image, dans les 3 surfaces ; cartes bloquée/repérée en interne et dans les deux overlays) · PR vers `dev` (jamais de push direct sur `main`/`dev`).


## 📌 Session 21/09/2026 (suite) — **Overlay interne : bouton archimonstre « Quête Ocre » (progression Metamob + pierres d'âme) + déslop du module Quête Ocre + encart pierres** → branche `fix/rush-sylvestre-aide-sans-invite`
> **Demande user (verbatim)** : « un bouton avec l'icone archimonstre `public/assets/dofus/icons/archimonster.png` qui affichera l'avancée si l'utilisateur a lié son compte metamob, avec les archimonstres et les boss qu'il a ou pas, le nombre de pierre nécessaire pour faire la quete ocre … inutile d'afficher la recette » · « je veux aussi que le bouton soit dispo dans l'overlay, remplace le bouton bug déplace le dans le menu avec l'aide et met en visuel le bouton archimonstre » · « ya juste trop de data là les boss et monstres sont réduits à un minuscule espace en bas de la modale » · « supprime le "tier" devant le niveau des pierres » · « profite en pour attaquer un déslop du module quete-ocre, ajoute un encart avec les pierres aussi dans ce module en espece de tips sympa » · **contrainte** : « tout dois provenir de chez nous, aucune dépendance à dofusdb pour les images des pierres, des archimonstres boss etc ».
> **Mesures (jamais supposées)** : DB locale → **674 monstres** (572 archis + 102 boss) dans 2 snapshots Metamob ; **ids Metamob = ids Ankama/DofusDB** (Blop Coco Royal = 301, Bouftou Royal = 305) ; couverture des WebP locaux = **303/337** (`public/uploads/assets-dofus/monsters/{id}.webp`) ; seuils de capture des pierres **relevés sur l'API DofusDB** (articles 9686→9690, effet 50/100/150/190/1000) — les 5 WebP existent déjà chez nous.
> **Règle pure nouvelle** : `src/lib/ocre-soul-stones.ts` — `soulStoneForLevel` (bornes 50/100/150/190/191+), `isOcreMonsterNeeded` (**même règle que `getMyOcreProgress`** : un `MANQUANT` dont l'étape est passée ne compte plus), `buildOcrePlan` (cibles enrichies + pierres par palier + compteurs archis/gardiens + %), `toOcrePanelData` (une seule conversion serveur→overlay). **Images 100 % locales** : `/uploads/assets-dofus/{items,monsters}/<id>.webp`, repli sur nos pictos du jeu — le test refuse tout hôte `dofusdb.*` et tout `/api/assets-dofus`.
> **Overlay** : panneau **`RushOverlayOcreModal`** (archi + Metamob : pierres à prévoir en tuiles compactes, jauges archis/gardiens, étape, filtres Restants/Obtenus + Tout/Archis/Gardiens, liste avec pierre requise par cible et badge « À faire »/« ×N », recherche, copie du nom au clic, resync `forceRefreshOcre` sans rechargement) · **hauteur utile corrigée** (`h-full` + liste `flex-1`) et densité réduite (tuiles 2 lignes, plus de ligne « N cibles affichées ») · bouton **visible** dans la barre d'actions avec pastille (0 → coche verte) · **le bouton bug quitte la barre** et rejoint le menu « ⋯ » avec l'aide (variante large : bouton direct conservé) · données fournies côté serveur (page overlay interne, **uniquement si `metamobPseudo`**) et figées dans le payload PiP (`rush-overlay-store`), donc **zéro aller-retour Metamob** à l'ouverture ; le guide public (`isGuest`) n'a jamais le panneau.
> **Écriture en direct dans l'overlay (2ᵉ tour de la session)** : pas **`−`/`+`** par cible → `updateUserMonsterQuantityAction` (PATCH Metamob) ; badge d'état calculé sur ce qui est **réellement déclaré** (« **À capturer** », « À capturer ×1/2 » si la quête exige 2 copies, « **Possédé ×2** » au-delà). **Anti-rate-limit** : état optimiste + **une écriture à la fois** (`desiredRef` regroupe la rafale par monstre, debounce 650 ms, 300 ms de respiration entre deux envois), valeur **confirmée** par Metamob qui fait foi, **retour arrière** honnête + message d'erreur en cas d'échec (429 inclus) ; **rate limit serveur ajouté** à l'action (`ocre:quantity:<user>` = **60/min**, fail-closed). `parallelQuests` et `pseudo` transportés jusqu'au panneau (`toOcrePanelData`) ; **bouton profil Metamob** (`/assets/brands/metamob.png` → `metamob.fr/profile/<pseudo>`). La parité de l'état local avec `computeMonsterState` (metamob-client) est **verrouillée par test**.
> **🐞 BUG MESURÉ + CORRIGÉ (retour user : « le bouton + ne marche pas, après synchro les quantités n'ont pas bougé »)** : ma file d'écriture était gardée par un drapeau `aliveRef` posé dans le **nettoyage** d'un `useEffect` de montage → or **Strict Mode est actif par défaut** en App Router depuis Next **13.5.1** (source : doc officielle `reactStrictMode`), le double montage (effet → nettoyage → effet) laissait le drapeau à `false` **pour toujours** : la file ne partait jamais, seul l'état optimiste changeait puis la synchro le corrigeait. **Correctifs** : file extraite en module **pur et testé** `src/lib/ocre-write-queue.ts` (`createOcreWriteQueue`, **créée DANS l'effet** et détruite avec lui → StrictMode-safe, `stop()` vide la file), et **retour d'erreur affiché DANS le panneau** (`role="alert"`) car un `toast` déclenché depuis la PiP part dans le **document principal** (invisible) — même piège que les popovers. Garde : `tests/unit/ocre-write-queue.test.ts` **11 cas** (regroupement, sérialisation + respiration, retour arrière sur la dernière valeur confirmée, `stop()`/re-montage, `clear()`, **pause sur limite**).
> **3ᵉ tour — « si les gens spamment, que se passe-t-il ? » (réponse mesurée + durcie)** : le client **regroupe** (debounce 650 ms) et **sérialise** (une écriture à la fois + 300 ms de respiration ⇒ ~1 écriture/s max, jamais de rafale) ; le serveur **borne à 60 écritures/min par membre** (fail-closed) et renvoie `retryAfterMs` ; Metamob qui répond **429** est traduit en pause de **30 s**. La file **se met en pause** (`pausedFor`) au lieu de réessayer en boucle, l'UI l'annonce (« Trop de modifications en peu de temps — patiente un instant. »), la valeur revient à la **dernière confirmée** et les demandes encore en attente repartent **après** la fenêtre. **Modale « Mon Ocre » (guide) déslopée + homogénéisée** : briques partagées **`src/components/ocre/OcreTargetBits.tsx`** (vignette WebP **locale** + pas ±1 + badge) et hook **`src/hooks/use-ocre-write-queue.ts`** consommés par l'overlay **et** la modale (mêmes libellés « À capturer / Possédé ×N », même file, mêmes messages) ; titre/surface/émoji slop retirés, **13 classes CSS mortes `.ocre-{row,tab,state,row-icon,…}` supprimées** de `guide-styles.css`, images Metamob remplacées par nos WebP, lien profil Metamob ajouté.
> **Déslop du module Quête Ocre** : `AuroraBackground` (déprécié) retiré de la page · `OcreStatCard` réécrite plate (zéro blur/dégradé/glow/scale/émoji `✨`/`framer-motion`) · bandeau d'état allégé (plus de blob `blur-3xl`, plus de `shadow-xl`) · onglets sans capitales tracking-widest · `ocre-filter-bar` : surface codée en dur `bg-[#13191a]` + `backdrop-blur-xl` + dégradé émeraude + `hover:scale` remplacés par les jetons (`bg-card`/`bg-elevated`/`bg-surface`) — **bug de classe `md:p-6space-y-5`** corrigé au passage · `ocre-monster-card` : verre dépoli, halo dégradé, `shadow-2xl`, `animate-pulse` et émojis d'état 🔴🟢🟡 → pastille de couleur. **Encart pierres partagé** (`OcreSoulStonesPanel`) servi à l'overlay **et** au module ; le placeholder « Astuce Ocre » en pointillés est supprimé (une seule source).
> **Preuves** : `tests/unit/rush-overlay-ocre.test.ts` (**40 cas** : seuils bornés, étapes passées, plan, pont serveur→overlay, pas ±1, **parité `ocreLocalState` ↔ `computeMonsterState`**, lien profil, **rendu réel** du panneau, de l'encart et du **pas ±1 partagé** — aucune image distante, plus de mot « Tier », plus de « En poche » —, câblage interne/public/PiP + rate limit, garde anti-slop, garde « Mon Ocre » sans CSS dédié) + `tests/unit/ocre-write-queue.test.ts` (**11 cas** — dont la non-régression StrictMode et la pause sur limite) · suite complète **173 fichiers / 1859 tests** ✓ · `npx tsc --noEmit` **0** · `npm run lint` **0 erreur**.
> **Reste** : validation visuelle par le user (aucune capture depuis la session) · 34 WebP de monstres manquants sur le poste (le repli local s'affiche ; prod = volume siphonné) · code mort préexistant du module (`exchangeMapLoading`/`loadExchangeMap`, imports inutilisés de `ocre-guild-directory`/`ocre-trade-inbox`) · PR vers `dev` à ouvrir.
## 📌 Session 21/09/2026 — **Overlay : bandeaux CONSEIL/TIPS visibles et jamais sautés + texte enrichi partagé par les 3 surfaces (liens nommés, positions copiables en `/w x,y`)** → branche `fix/rush-sylvestre-aide-sans-invite`
> **Demande user (verbatim)** : « dans l'overlay on doit voir aussi les bandeaux de tips ajouté , et ya un pb si on passe à une autre étape dans l'ovelay les bandeau type separateus / tips on ne les a plus dispo · j'aimerai aussi que dans les bandeaux de type tips/conseil je puisse ajouter nimporte ou dans le texte un lien , une ou plusieurs positions cliquable presse papier en /w x,y — exemple : Lancer [url] Eternelle Moisson dès que possible pour rendre les captures et faire un Krala sur la première ouverture. Position de lancement : Village de la Canopée [-55,15]. **l'url doit pas etre dispo mais le nom de la quete pointera vers l'url** ».
> **Causes mesurées** : (1) l'overlay **filtrait sa liste** (`rawMilestones.filter(ms => !["INFO","DOFUS_OBTAINED"].includes(ms.type))`) ⇒ aucun encart tips rendu, la branche `<RushInfoBanner>` plus bas était **du code mort** ; (2) `goToNextMs` sautait **tout** bloc de `completedIds` ; (3) `selectableChapters` et `msIsInfoBlock` recopiaient chacun leur liste de types ; (4) le parseur liens/coordonnées (`renderContentWithCoords`) était **privé au dashboard** — public et overlay affichaient le texte brut ; (5) la commande copiée était `/travel`, plus le format du Dofus actuel.
> **Correction de trajectoire (même session, retour du user)** : mon premier correctif remettait les bannières dans la liste **navigable** — faux : « pk tu continue à mettre les tips/conseil dans l'overlay avec le cercle à cocher (qui ne l'est pas) ces bandeaux là ont juste à afficher dans l'ordre chronologique · si je vais dans le dropdown d'étape et que je choisis une étape, je peux plus retrouver la partie des bandeaux ». Règle **finale**, identique au dashboard (`chapterPages`) : la navigation ne porte que sur les **CHAPITRES**, les bannières s'affichent **dans le flux de leur chapitre** (`bannersForChapter` : celles qui l'introduisent au-dessus, celles de fin de guide sous le dernier chapitre) — donc aucune case à cocher, aucun « 1. », aucun titre de bandeau en guise de chapitre, et on les **retrouve dès qu'on ouvre le chapitre**, par n'importe quel chemin.
> **Fait** : **`RushRichText`** partagé (les 3 surfaces) — `[Eternelle Moisson](https://…)` affiche le **nom** (l'URL ne s'affiche jamais), URL brute ⇒ libellé lisible (DofusDB / DofusNoobs / domaine seul), ponctuation finale exclue du lien, URL non sûre ⇒ **pas d'ancre** (fail-closed) ; positions reconnues n'importe où (`[-55,15]`, `[-55, 15, 2]`, `/w -55,15`, `/travel -55 15` encore accepté en saisie) et la chip **copie `/w x,y`**, commande construite **dans `parseCoordinates` seul**. **Overlay** : navigation sur les chapitres (`nextBlockIndex`), bannières rendues **dans le flux** par une règle pure (`bannersForChapter`), `selectableChapters` + numérotation sur la règle partagée **`isNonCheckableBlock`**, `msIsInfoBlock` et la prop `checkable` du compact **supprimés**, mode compact qui ne cache plus l'objectif. **Studio GOD** : aide de syntaxe (`RichTextSyntaxHint`) sous chaque champ de conseil + bouton « Ajouter position » en `/w x,y`. **`renderContentWithCoords` supprimé** (zéro duplicata).
> **Preuves** : `tests/unit/rush-rich-text.test.ts` (**9 cas**) + `tests/unit/rush-overlay-non-checkable-blocks.test.ts` (**13 cas** : `bannersForChapter` — bannière collée au chapitre qui la suit, fin collée au dernier chapitre, aucune pour un id de bannière —, `nextBlockIndex`, et les gardes de câblage : navigation = chapitres, bannières rendues avant/après, plus de `msIsInfoBlock`) + `tests/unit/rush-info-sequence-banner.test.ts` (**10 cas** : rendu réel du bandeau d'encart `info_sequence` + câblage des 3 surfaces + compteurs du guide public) + `tests/unit/rush-overlay-dungeons.test.ts` (**14 cas** : encart « À FAIRE MAINTENANT » retiré, badge `Donjon ×N` + picto du jeu, popover de survol, destination publique/module des fiches donjon, portail dans `ownerDocument` — le piège PiP) + `rush-overlay-chapter-tree` / `rush-guide-utils` étendus · `npx tsc --noEmit` **0** · `eslint` **0 erreur** · **171 fichiers / 1807 tests** ✓.
> **Écart tranché au passage (21/09/2026)** : le **guide public** n'affichait pas les séquences `info_sequence` d'un chapitre (et les comptait dans sa progression) — corrigé, voir le point « Encarts `info_sequence` » du ROADMAP.
> **Leçon PiP (à retenir)** : dans un bundle JS, `document` et `window` restent ceux de la **page principale** même quand le nœud vit dans la fenêtre PiP. Toute popover/portail de l'overlay doit utiliser `node.ownerDocument` (+ `ownerDocument.defaultView` pour mesurer) — sinon l'aperçu s'affiche hors de l'overlay, « n'importe où sur l'écran ».
> **Reste** : validation visuelle par le user (aucune capture depuis la session) · le **picto** du bandeau INFO est toujours un émoji (⚠️/📖/🔮/💡) — picto du jeu à choisir si tu le veux · le **guide public** n'affiche toujours pas les séquences `info_sequence` d'un chapitre (divergence préexistante, à trancher) · PR vers `dev` à ouvrir.
## 🖼️ Session 20/09/2026 (nuit, suite 4) — **Blocs CONSEIL/TIPS : un seul rendu immersif pour les 3 surfaces (le public en faisait un chapitre) + studio GOD épuré** → branche `fix/rush-sylvestre-aide-sans-invite`
> **Demande user (verbatim)** : « c pas mal coté guide interne mais un bloc de type tips/info doit etre plus immersif sans etre ai slop et l'icone est trop perdu dans son coin · il y a un pb entre le visuel rendu coté interne/externe on a pas la meme chose le rendu doit etre identique sur le rush sylvestre entre interne/externe · pk afficher le bandeau avec un % pour le nom d'un bloc de type séparateur dans l'overlay · un bloc de type tips/conseil ne doit pas coté god avoir la possiblité d'option et d'avancée et d'ajout de quete : il s'agit d'un bloc pour ajouter un tips/conseil + une image sympa, rien d'autres ».
> **Causes mesurées** : (1) le bandeau INFO du dashboard ne lisait **jamais** `milestone.imageUrl` — l'upload n'existait que dans l'aperçu du studio ; (2) le **guide public** n'avait **aucune branche INFO** : il tombait dans le rendu de CHAPITRE (d'où « Chapitre 0 · 0/0 étapes (0%) » et un bouton « Tout valider ») ; (3) l'overlay affichait `n.` et `{currentMsPct}%` pour **tout** bloc, y compris ceux qui n'ont pas d'étape ; (4) le studio laissait sur un bloc INFO le badge `opt.`, le compteur de quêtes, le dépliage et le formulaire d'ajout de quête.
> **Fait** : nouveau **`RushInfoBanner`** partagé aux 3 surfaces — filet d'accent, eyebrow mono « Attention / À savoir / Astuce / Conseil » avec le **picto intégré à l'eyebrow** (plus d'émoji isolé), titre fort, texte (fourni par l'appelant : le dashboard garde ses liens et coordonnées cliquables), **image à droite qui s'adapte** (hauteur du bandeau, largeur = son ratio, `object-contain` ⇒ jamais rognée, fondu vers le texte) ; **guide public** : branche INFO dédiée (plus de rendu chapitre) ; **overlay** : ni numéro ni « 0% » sur un bloc sans étape ; **studio GOD** : un bloc Tips = titre + texte + image + couleur (ni option, ni compteur, ni dépliage, ni ajout de quête) et sa **création demande le texte du conseil** (`tips`, qui EST le bloc — avant, aucun champ de texte à la création) ; `getInfoStyle` (code mort) supprimé. Au passage : les bandeaux **séparateur et INFO du guide public gardent leur ancre partageable** `#bloc-<id>` (le sommaire et le hash d'URL continuaient de pointer dessus sans conteneur porteur).
> **Preuves** : `tests/unit/rush-block-image-rendered.test.ts` (**8 cas** : 4 de câblage — bandeau partagé par les 3 surfaces, pas de rognage, le dashboard transmet titre/image/couleur, le public ne rend plus un INFO comme un chapitre — et 4 de **rendu réel** via `renderToStaticMarkup` : image nue, URL non sûre ⇒ aucun `<img>`, eyebrow nommé + picto, titre) + `tests/unit/rush-tips-block-studio.test.ts` (**3 cas** : texte du conseil saisi à la création et envoyé, ligne sans option/compteur/dépliage, section quêtes inatteignable) · `npx tsc --noEmit` **0** · `npm run lint` **0 erreur** · **166 fichiers / 1745 tests** ✓.
> **Reste** : le picto du bandeau reste un émoji (⚠️/📖/🔮/💡) choisi d'après la couleur d'accent — si tu veux de **vrais pictos du jeu** à la place, c'est un asset à choisir/valider comme pour le séparateur.
## 🧲 Session 20/09/2026 (nuit, suite 3) — **Studio GOD : un bloc créé se range dans SON chapitre + fini le drag qui dérive sur une longue remontée** → branche `fix/rush-sylvestre-aide-sans-invite`
> **Demande user (verbatim)** : « quand je créé un nouveau bloc coté god peu importe le type il apparait tout en bas pénible, et le drag and drop si je dois scroller pour le ramener très haut bug au bout d'un moment ».
> **Cause racine (1) — placement** : `handleAddStep` envoyait `order: localMilestones.length` ⇒ **toujours en fin de guide**, quel que soit le chapitre saisi (le champ « N° chapitre » du panneau était donc ignoré au placement).
> **Cause racine (2) — drag** : deux référentiels d'ordre en parallèle (le `SortableContext` et les index de `onDragOver`/`onDragEnd` sur `localMilestones`, alors que le DOM rendait `sortedMilestones`), **pas de `DragOverlay`** (l'élément saisi restait solidaire du conteneur qui défile ⇒ dérive du curseur sur un long scroll automatique) et l'effet de synchro `initialGuide.milestones → localMilestones` **pouvait remplacer la liste en plein geste** (un `router.refresh()` atterrissant pendant le drag ⇒ l'id/les index du bloc glissé n'existent plus).
> **Fait** : (1) règle **pure** `findMilestoneInsertIndex` (`src/lib/rush-guide-utils.ts`) + insertion réelle via `reorderRushMilestones` à la création, nouveau bloc **déplié** ; (2) un seul ordre pour le drag (`sortedMilestones` partout), **`DragOverlay`** (fantôme du bloc sous le curseur), **`draggingRef`** qui gèle la resynchronisation serveur pendant le geste (+ `onDragStart` / `onDragCancel`).
> **Preuves** : `tests/unit/rush-guide-utils.test.ts` **+4 cas** (après le dernier bloc du chapitre · chapitre neuf **avant le premier chapitre supérieur**, donc après le séparateur intercalé · chapitre au-delà ⇒ fin · bloc hors chapitre ⇒ fin) · `npx tsc --noEmit` **0** · `npm run lint` **0 erreur** · **165 fichiers / 1741 tests** ✓ · **le geste réel reste à valider par le user** (aucun navigateur côté session).
## 🧨 Session 20/09/2026 (nuit, suite 2) — **Rush Sylvestre : suppression de blocs/quêtes rendue idempotente (P2025 « No record was found for a delete » en plein studio GOD)** → branche `fix/rush-sylvestre-aide-sans-invite`
> **Demande user (verbatim)** : « quand je supprime des étapes en local J4AI CA » + capture de l'overlay d'erreur Next montrant `async function deleteRushMilestone(milestoneId) { await requireRushAccess(); await db.guideMilestone.delete(...) }` et « An operation failed because it depends on one or more records that were required but not found. No record was found for a delete. » (2 toasts « ÉTAPE SUPPRIMÉE » OK, puis 1 échec).
> **Cause racine mesurée** : `deleteRushMilestone` — et `deleteRushSequence`, même code — faisaient `db.<model>.delete({ where: { id } })` : Prisma **jette P2025** dès que la ligne n'existe déjà plus, et côté studio la ligne ne quittait la liste qu'après le refresh serveur ⇒ un **second clic** sur une étape déjà supprimée (ou supprimée dans un autre onglet) suffisait à faire remonter l'erreur brute. Preuve avant/après : `tests/unit/rush-delete-idempotent.test.ts` **4 rouges / 5 sur le code d'origine** (mesuré par `git stash` du fichier serveur), **5/5 après**.
> **Fait** : **serveur** — `deleteMany` + comptage (`0 ligne = succès`, `{ success: true, alreadyDeleted: true }`), **`guideId` dans le `WHERE`** pour le bloc (garde d'état `AGENTS.md` §5.9), trace God **seulement** si une ligne a réellement disparu ; **client** — retrait **optimiste** de la ligne (bloc **et** quête) avec toast « déjà supprimée », et en cas d'échec réel `router.refresh()` (la ligne encore en base réapparaît — le serveur fait foi).
> **Preuves** : `tests/unit/rush-delete-idempotent.test.ts` (**5 cas** : 0 ligne ⇒ succès sans exception · cas nominal + revalidation des 2 surfaces · guide introuvable ⇒ aucune écriture · quête déjà absente · refus sans accès God sans aucune écriture — les espions `delete()` **jettent** s'ils sont rappelés) · `npx tsc --noEmit` **0** · `npm run lint` **0 erreur** · **164 fichiers / 1734 tests** ✓.
## 🖼️ Session 20/09/2026 (nuit, suite) — **Rush Sylvestre : le SÉPARATEUR devient un bandeau illustré (image à droite) — 3 surfaces partagées, upload dans tous les formulaires GOD** → branche `fix/rush-sylvestre-aide-sans-invite`
> **Demande user (verbatim)** : « les blocs de type séparateur dans god [capture GOD] rendu user : [capture] — autorise l'upload d'une image pour remplir sympathiquement la droite du bandeau proprement, que ce soit à la création d'un bloc séparation / édition d'un bloc existant, comme on le fait déjà dans d'autres blocs ; améliore aussi le rendu côté police visuel etc, sans AI slop » — puis « pk ya pas d'upload là ? [capture du formulaire séparateur] » + « pip : oui **separateur seulement**, ce n'est pas une étape à cocher ».
> **Cause racine mesurée (2ᵉ tour)** : le séparateur ne passait pas par le formulaire commun — il avait son **propre formulaire réduit** (`SeparatorRowAdmin`, titre + pastille de couleur uniquement), donc **aucun champ image** là où le user édite ses séparateurs (l'upload existait déjà dans `MilestoneRowAdmin`). Un seul correctif d'étage : le champ devient un composant **`BlockImageField`** partagé par les 3 formulaires (création · édition commune · édition du séparateur) — impossible d'en oublier un.
> **Fait** : **`src/components/dofus-quests/rush/RushSeparatorBanner.tsx`** = **source unique** du bandeau pour les **3 surfaces** — dashboard membre (`RushTimelineClient` perd son `SectionDivider`), guide public (`PublicRushGuideClient` perd sa variante pauvre) et **overlay PiP** (`GuideOverlayClient` : le bandeau remplace « — Séparateur — », **sans case à cocher** ; en **mode compact** (`RushOverlayCompact`) la case disparaît aussi et le bandeau prend la place de l'objectif — un séparateur n'est pas une étape). Grammaire : filet 1px + aplat `surface` + rayon 6 px, **filet d'accent à gauche**, eyebrow mono, titre fort, description, **image à droite** (`object-cover` sur la hauteur, dans le flux, NUE, fondue par `mask-image`, `w-24 → md:w-40` donc **jamais masquée en 420 px**) — zéro dégradé/ombre/glow ; URL non sûre ou asset absent ⇒ pas d'image. **GOD** : `BlockImageField` partagé (upload scope `guides` + URL + aperçu nu + retrait) ; le séparateur gagne en plus la **description** (2ᵉ ligne du bandeau) et une **vignette** dans sa ligne.
> **Preuves** : `tests/unit/rush-separator-banner.test.ts` (**5 cas** : image rendue à droite, URL non sûre ⇒ aucun `<img>`, accent explicite vs or par défaut, description optionnelle) · `npx tsc --noEmit` **0** · `npm run lint` **0 erreur** · **163 fichiers / 1729 tests** ✓.
> **Suite (même session) — bandeau juste + overlay propre** : le bandeau passe au **texte centré** (vertical ET horizontal, dans la zone que l'image lui laisse) et à une **image adaptée** — hauteur du bandeau, largeur déduite de SON ratio (`object-contain` + `w-auto` ⇒ **jamais rognée**). Deux erreurs intermédiaires documentées pour ne pas les refaire : une `<img>` en `h-auto` **imposait son ratio au bandeau** (≈ 400 px de haut) ; et `object-cover` produisait le **rognage** vu par le user. Côté **overlay** : les blocs SÉPARATEUR sortent du **sélecteur d'étapes** (`selectableChapters`) — ce ne sont pas des étapes, donc plus de case, plus de « 0/0 », plus de barre de progression vide ; leur titre reste affiché en haut quand on est dessus et le bandeau reste dans le contenu. L'icône « quête » de ce sélecteur n'est plus le glyphe lucide `BookOpen` mais le **vrai picto du jeu** (`public/assets/dofus-ui/pictos/etape.png`, 64 px extrait de `dofus_assets`, **zéro métadonnée**).
> **Preuves (suite)** : `tests/unit/rush-overlay-chapter-tree.test.ts` (**3 cas** : séparateurs retirés, ordre conservé, guide fait de séparateurs ⇒ rien à sélectionner) + test du bandeau mis à jour (`object-contain`, jamais `object-cover`) · `npx tsc --noEmit` **0** · `npm run lint` **0 erreur** · **165 fichiers / 1737 tests** ✓.
> **Reste** : validation visuelle par l'œil du user (aucune capture faite depuis la session) — le bandeau s'applique aussi aux séparateurs **sans image** (on juge la typo tout de suite).
## 🤝 Session 20/09/2026 (nuit) — **Rush Sylvestre : badge « qui peut aider » rendu purement informatif (pluriel corrigé, bouton [Inviter] + modale « choix du salon » retirés)** → branche `fix/rush-sylvestre-aide-sans-invite`
> **Demande user (verbatim)** : « dans le rush sylvestre / les “peut aider” au pluriel c'est peuvent déjà · ensuite supprime le bouton d'interaction et la modale choix du salon, j'en veux plus · dis moi à quoi sert ce peut aider ».
> **Fait** : (1) `src/components/rush/RushHelperBadge.tsx` — chip « N peut aider » → « N **peuvent** aider » dès 2 membres (même correction dans le tooltip du variant `compact`) ; (2) **invitation Discord retirée** : boutons [Inviter] de la liste déroulante **et** modale « Inviter » (sélecteur de salon) supprimés ⇒ le badge **montre** qui peut aider, il ne sollicite plus personne ; (3) **zéro code mort** : les 2 server actions devenues orphelines (`inviteHelperForSequence`, `listRushTextChannels`) + leur Zod `inviteHelperSchema` retirées de `src/server/actions/optimized-guide-actions.ts` (seul ce bouton les appelait ; `fetchGuildChannels` / `validateChannelBelongsToGuild` / `postChannelMessage` restent — utilisés ailleurs).
> **À quoi sert « peut aider » (réponse au user, à garder)** : `getSequenceHelpers` croise les **besoins déclarés** de la séquence — tag GOD « 🔨 Métier requis » (+ niveau), `dungeonId`/`dungeonIds`, `alignReq`/`alignOrderReq` — avec les **profils ACTIFS de la guilde** (métiers + niveau via `normalizeMetiers`, progression `UserDungeonProgress`, alignement) ⇒ un motif lisible par membre (« Métier Façonneur 200 », « Donjon … ») et un badge « à confirmer » quand le niveau du métier/camp est inconnu. But : savoir **qui pinger** avant de lancer une étape bloquante, **sans quitter le guide** — le badge ne remplace pas une invitation, il la prépare (le ping Discord se fait à la main).
> **Preuves** : `npx tsc --noEmit` **0** · `npm run lint` **0 erreur** (7144 warnings `any` préexistants) · **162 fichiers / 1724 tests** ✓ · `build` non joué (délégué à la CI).
> **Reste** : PR vers `dev` à ouvrir · le variant `compact` de `RushHelperBadge` n'est **plus appelé nulle part** (aucun import) — à supprimer ou rebrancher (overlay) dans un lot séparé.
## 🔎 Session 20/09/2026 (soir, suite) — **F2 puis F3 livrés : chaîne de fabrication verrouillée (10 actions CI épinglées par SHA + provenance/SBOM) & isolation de guilde rétablie (`editBlacklistEntry`)** → branches `fix/ci-pin-actions` puis `fix/blacklist-guild-isolation` (empilées sur `fix/dockerignore-env`, **PR #697** ouverte)
> **Demande user (verbatim)** : « Reprise du chantier “corrections issues du croisement d'audit du 20/09” […] **Attaque F2 (épingler les 10 actions CI par SHA + provenance)**. »
> **Livré** : les **10 `uses:`** en étiquette mobile (`actions/checkout@v7` ×4 · `actions/setup-node@v7` ×2 · `actions/cache@v6` **et** `@v6.1.0` · `docker/login-action@v4` · `docker/setup-buildx-action@v4`) portent le **SHA de commit** du tag courant, **tag conservé en commentaire** (`@<sha40> # vX`, forme suivie par Dependabot) + **provenance/SBOM** sur chaque `buildx build`.
> **Écart mesure ↔ brief (à retenir)** : le plan demandait `provenance: true` + `sbom: true` « au `docker/build-push-action` » — **ce step n'existe pas** ici : `deploy.yml` appelle `docker buildx build --push` en shell. Les flags sont donc posés **au bon étage**, dans la fonction `retry_push` (`--provenance=true --sbom=true`) dont les 4 images (app, worker, ws, bot) héritent — un seul endroit à maintenir, aucune image ne peut plus être poussée sans attestation. `--provenance=true` = mode **minimal** volontairement : `mode=max` embarquerait les `--build-arg` (donc des valeurs de secrets) dans l'attestation. Les 6 tags visés sont **légers** (`object.type=commit`) ⇒ aucun déréférencement `git/tags/<sha>` nécessaire.
> **Règle corrigée là où elle vit** : `docs/RULES.md` §Sécurité de la chaîne d'outils n'exigeait l'épinglage que des actions **tierces** (d'où des `actions/*` restées mobiles) → elle couvre désormais **toute** action, first-party comprise, + une ligne provenance/SBOM. `docs/MAINTENANCE.md` §3b : la référence GHCR publiée est un **index OCI** (image + attestations), `deploy-cd.sh` et `docker pull` **inchangés**.
> **Preuves** : `grep -nE 'uses: [^ ]+@v[0-9]' .github/workflows/*.yml` → **0** · les 2 YAML **parsés** (14 `uses:`, tous en `@<sha40> # vX`) · **`bash -n`** du bloc `run` de build : **statut 0** · **garde neuve** `tests/unit/workflows-actions-pinned.test.ts` : **4 tests ✓** (pin 40 hex **+** tag en commentaire, aucune étiquette mobile, provenance sur chaque `buildx build`) · `tsc` **0** · `eslint` **0 erreur** · **161 fichiers / 1720 tests** ✓.
> **F3 livré (isolation de guilde — seul IDOR réel du lot, muse CYB-01 / S-14)** : `editBlacklistEntry` vérifiait le RBAC sur la guilde **déclarée** mais lisait (`findUnique({ id })`) **et** écrivait (`update({ id })`) **sans `guildId`** ⇒ un admin de la guilde A pouvait modifier une entrée de la guilde B (message Discord de B compris) en connaissant son cuid (sévérité **moyenne** : cuid non énumérable, arbitrage §4-B). Correctif : lecture `findFirst({ id, guildId })` (calquée sur la **voisine** `deleteBlacklistEntry`, déjà correcte) + écriture **conditionnelle** `updateMany({ id, guildId })` et `count === 0` ⇒ « introuvable » (garde d'état **dans le `WHERE`**, `AGENTS.md` §5.9). **Aucune migration** (`BlacklistEntry.guildId` + index existaient). **Mesure avant/après** : `tests/unit/blacklist-guild-isolation.test.ts` (mocks façon `guards.test.ts`, 4 cas) est **rouge 3/4 sur le code d'origine** puis **vert 4/4** — il verrouille aussi l'absence de retour aux appels non filtrés (`findUnique`/`update` jettent s'ils sont rappelés). Constat annexe **non modifié** (hors périmètre, verrouillé tel quel) : Discord reçoit le texte **brut**, la base le texte **nettoyé**.
> **F4 livré (page de test publique retirée)** : `src/app/test-route/page.tsx` (une ligne, `Test OK`, **aucune protection**) était servi tel quel ⇒ reconnaissance offerte aux robots. Fichier supprimé **et** citations vivantes nettoyées (`src/app/robots.ts` : l'entrée `Disallow: /test-route/` devenait morte ; `docs/plans/SEO_REPRISE.md`) ⇒ `/test-route` → **404** (aucune route « attrape-tout » : pas de `[...slug]` à la racine de `src/app`). **Preuves** : `git grep -n 'test-route'` sans aucune référence vivante · **162 fichiers / 1724 tests** ✓ · `tsc` **0** · `eslint` **0 erreur**.
> **Reste à faire** : **F1 (#697) · F2 (#698) · F3 (#699) fusionnées dans `dev`**, branches distantes supprimées, historique linéaire (rebase `--onto` avant chaque PR) · `fix/remove-test-route` (F4) : PR à ouvrir après ces fusions · **F5** 6 `console.*` → `logger` · **F6** masquer snowflakes/e-mails · avant ouverture prod : **F7** jouer une restauration (+ rétention 3 j ↔ 30 j), **F8** brancher `env.ts`, **F9** `basic_auth` sur `monitor.*`, **F10** CSP en enforcement. La **classe** du défaut F3 (balayage des actions `(guildId, …Id)`) reste le chantier **C-A**.
> ⏸️ **Chantier mis EN PAUSE le 20/09/2026 (soir)** à la demande de l'utilisateur (retour aux features) : F1 → F4 sont livrés **et fusionnés** dans `dev` (`4783e8ef`) ; la reprise commence à **F5**. Côté serveur, **seul un déploiement bêta** (`./scripts/deploy-cd.sh beta`) est nécessaire pour rendre F3 (isolation) et F4 (page retirée) effectifs : F1 ne joue qu'au **build prod local**, F2 est purement CI. Aucun déploiement **prod** requis (elle n'est pas ouverte).

> **Ops (humain)** : contrôler le **premier build GHCR** après fusion — l'image devient un index OCI attesté (`docker buildx imagetools inspect ghcr.io/<owner>/sigilos-app-beta:<sha>`) ; `deploy-cd.sh` n'a pas à changer.

## 🔎 Session 20/09/2026 (soir) — **Croisement de 3 audits indépendants → arbitrages mesurés, plan priorisé, 1ʳᵉ correction livrée (`F1`)** → branche `fix/dockerignore-env` (commit `f190ded2`)
> **Demande user (verbatim)** : « voilà un croisement de trois audit […] Croise les 3 audits · Reste dans /temp, prends ton temps c'est important, et prépare un plan, aucun faux postifi » puis « liste les fixe à faire », « documente les change fait les contexte projet ».
> **Matériel** : 3 rapports (cline 20 findings · opus 21 · muse-spark 32 = **73**) ramenés à **49 sujets** — **piège d'ID** : les 3 audits numérotent **chacun de zéro** (`CYB-01` = upload SVG *ou* page de test *ou* IDOR blacklist) ⇒ toute reprise cite **audit + ID**, jamais l'ID seul. **39 sondes rejouées** dans le code (chaque constat repris porte sa commande).
> **Écartés après mesure (ne pas re-planifier)** : « suppression anonyme de fichiers » (`src/server/actions/upload-actions.ts` **n'est pas** un module `"use server"` ⇒ ses exports ne sont pas des server actions ; la fonction n'est appelée que par `presentation-actions.ts` après garde RBAC) · `dofusItem.findMany` sans `guildId` (filtré par `profile.guildId`) · comptage faux corrigé : `console.log` = **24** dans `src`, pas 60. **Requalifiés** : `og/build` (public par conception — le sujet est le **coût**, pas l'accès), webhook Ko-fi (badge de **plateforme**, un filtre de guilde n'a pas d'objet), `deploy.resources.limits` (**hypothèse non tranchable depuis le dépôt** ⇒ mesure `docker stats`), `cron-auth` (comparaison **déjà** en temps constant — il reste un oracle de longueur, faible).
> **3 constats inédits** : `src/lib/env.ts` **importé par personne** (validation d'environnement inerte : 13 clés pour ~70 variables lues) · `ARG BETA_PASSWORD` transmis au build et **jamais consommé** (`Dockerfile:37` + `deploy.yml:134`) · `docs/` **muette** sur les limites de ressources.
> **Posture (3 scores convergents : 7,1 / 7,2 / 7,1)** : **aucune faille critique** — pas de contournement d'authentification, pas d'injection SQL, pas de RCE, pas de SSRF ouverte, pas d'accès God déviable, pas d'accès anonyme prouvé à une autre guilde ; le cœur est tenu (SSRF + liste blanche, Ed25519 Discord, CSP à nonce, RBAC fail-closed, WS cloisonné, tokens OAuth chiffrés, conteneurs non-root). La dette est **périphérique** : chaîne de build/CI, **exploitation** (reprise jamais jouée, crons muets), dérive doc↔réel, hygiène.
> **F1 livré** (`.env` hors du contexte de build) : Next.js recopie `.env`/`.env.production` dans `.next/standalone` (`writeStandaloneDirectory`) et `Dockerfile:125` copie ce dossier dans l'**image finale** ⇒ tout build lancé depuis un dossier contenant un `.env` (VPS via `deploy.sh`, poste local) **embarquait les secrets** — la mesure **aggrave** le constat des 3 audits (qui parlaient du cache de build). Correctif : `.env*` dans `.dockerignore` + commentaire du pourquoi. **Runtime inchangé** (`env_file:` / `--env-file` sont lus **côté hôte** ; aucun `RUN` du Dockerfile ne consomme ces fichiers). Preuve : build Docker isolé sur contexte jetable → aucun `.env*` dans le contexte, fichiers normaux conservés · hook `pre-commit` vert (scan secrets + lint-staged + `tsc`).
> **VPS (3 sondes lecture seule, 20/09)** : image de prod **locale** (5 mois) = `/app/.env` présent (3,8 Ko) **mais aucun digest GHCR** ⇒ jamais sortie du VPS ⇒ **aucune rotation de secrets nécessaire** ; **bêta** (là où sont les utilisateurs) = image **GHCR/CI** (tag local et tag GHCR = **même ID d'image**) ⇒ **aucun `.env`**. La CI fabrique depuis un *checkout* git (donc sans `.env`) : le chemin en cause était bien le build **local** (celui du futur lancement prod).
> **Preuves** : `git status --short` propre · `docs/ROADMAP.md` = bloc de session dédié · commit `f190ded2` (1 fichier, +11).
> **Reste à faire** : ouvrir la **PR** `fix/dockerignore-env` → `dev` (CI à passer) · puis **F2** épingler les 10 actions CI par SHA + `provenance` · **F3** `editBlacklistEntry` filtré par `guildId` (seul IDOR réel) · **F4** supprimer `/test-route` · **F5** 6 `console.*` → `logger` (dont 2 qui écrivent un chemin disque absolu) · **F6** masquer snowflakes/e-mails (`logger.sensitiveKeys` + `auth.ts:48`) · avant ouverture prod : **F7** jouer une restauration (+ rétention 3 j ↔ 30 j), **F8** brancher `env.ts`, **F9** `basic_auth` Caddy sur `monitor.*`, **F10** CSP en enforcement.


## 🔐 Session 20/09/2026 (suite) — **Purge des empreintes machine + réécriture de l'historique git** → PR #689 (squash `ee93c06` sur `dev`), puis `dev` réécrit (`fd095b2`)
> **Demande user (verbatim)** : « comment faire pour avoir un GitHub propre sans que personne ne puisse retrouver des reliquats comme ces images dangereuses ou autre aberration dans l'historique du projet […] c faisable sur GitHub ? »
> **Constat (audit de 1 645 images suivies + archives d'uploads)** : 199 images portaient des métadonnées (« Adobe Photoshop 26.5 (Windows) », horodatages, chunks `eXIf/tEXt/iTXt`) · 6 « preuves » de guilde étaient des **captures d'explorateur local** du disque · 3 captures Discord d'un serveur tiers (pseudos de membres) suivies sans aucune référence · chemin machine `C:\Users\…\dofus_assets` dans 5 fichiers · identifiants Discord en dur (guilde **et** compte propriétaire) dans 3 scripts · **1 865 commits signés avec l'e-mail personnel** (prénom + nom lisibles) · historique contenant un dump SQL de 17,7 Mo, un `.xlsx` de 85 Mo, une application Electron extraite (`tmp/ankama-launcher-extracted`) et des rapports de lint.
> **Corrigé (PR #689)** : 199 images nettoyées **en lossless** (données image recopiées à l'octet près ; preuve : sha256 des pixels décodés identique avant/après) → **0 métadonnée sur 1 645 images** · `os.homedir()` / placeholder `<Bureau>/dofus_assets` · `GUILD_ID`/`OWNER_ID` obligatoires avec **échec explicite** · 636 uploads morts + 3 captures Discord retirés du suivi · **contrôle permanent `scripts/check-media-metadata.mjs`** (zéro dépendance) branché dans le job `verify` **et** dans le hook `pre-commit` · règles écrites dans `docs/RULES.md` §Sécurité et `docs/SECURITY.md`.
> **Historique purgé (20/09/2026)** : `git filter-repo --invert-paths` sur **2 497 chemins** (médias/uploads, déchets, dump SQL, `.xlsx`, `tmp/`, `.gemini/`) + anonymisation des identités → **2 564 commits réécrits**, arbre de **chaque branche et tag identique à la purge près** (contrôle avant push), **483 Mo → 206 Mo**, e-mail personnel **absent** de l'historique (vérifié via l'API GitHub et par HTTP 404 sur un fichier purgé). Sauvegarde : `A:\SigilOS--menage-2026-09-20\_sauvegarde-miroir-2026-09-20.git` · retour arrière : `_audit\RESTAURER-SUR-GITHUB.ps1` · rejouer : `_audit\REECRIRE-HISTORIQUE.ps1`.
> **Preuves** : CI **verte** (Lint · TypeScript · Discord Bot · Tests · **Production Build** · Semgrep · Trivy · audit) · `node scripts/check-media-metadata.mjs` OK · **1 722 tests / 161 fichiers** ✓.
> **Reste à faire (hors dépôt, volontairement non fait)** : `git fetch origin && git reset --hard origin/dev` **sur le VPS** (son clone voit un historique divergent ; le `git pull` du déploiement est non bloquant) · **ticket au Support GitHub** (déréférencer les 688 `refs/pull/*` et purger objets/caches) · activer le ruleset `secure-dev` (`enforcement: disabled`) · CodeQL limité au langage `actions` → ajouter `javascript-typescript` · secret scanning : `validity_checks` et `non_provider_patterns` désactivés · supprimer les vieux `public/uploads/**` encore sur le serveur de prod · rotation de ce fichier (**8 blocs présents** pour une règle de 6).


## 🧹 Session 20/09/2026 — **Ménage profond de l'arborescence : une seule arbo de documentation, reliques sorties, pointeurs morts corrigés** → branche `refactor/nettoyage-src-temp` (suite de la PR #688)
> **Demande user (verbatim)** : « marre des reliques […] je veux une arbo clean minimale, ultra optimisée… un vrai projet professionnel que n'importe quel dev senior pourra lire sans se dire : encore un projet vibecodé à l'arrache ».
> **Fait — lots D1→D9** (détail chiffré : `docs/arbo/AUDIT-ARBO-2026-09.md` §0) : `.antigravity` supprimé (58 Ko de contexte figé depuis le 31/08) · **12 reliques sorties de `src/`** (dumps, specs livrées, log de déploiement) + `bonus_songes.json` rangé dans `src/data/` · 4 fichiers-junk supprimés (`workflow-images-buggés.md`, `compile_errors.txt`, `verify.sql` d'une migration, `.vscode/settings.json`) · **toute la doc dans `docs/`** (33 `git mv`, sous-dossiers `agents/ ops/ plans/ reference/ arbo/`, nouvel index `docs/README.md`, plus aucun `.md` à la racine sauf `README.md`) · **175 références réécrites** dans 38 fichiers (script Node, jamais PowerShell) · **U+FFFD réparés** (2 emojis de job CI, `RULES.md`, libellé **visible** « Sexe Féminin », titre d'embed Discord) · 6 pointeurs morts vers un dossier `archive/` inexistant corrigés + hint `seed-v3` (`npm run seed:game-data`) · kits `_menage-*` archivés hors dépôt · `.gitignore` (doublon), `tsconfig.json`, `verify.yml` (typo `!.agent/**`), `.dockerignore` nettoyés.
> **Preuves** : `tsc` **0** · `eslint` **0 erreur** (7 149 warnings préexistants) · **1 722 tests / 161 fichiers** ✓ · `node scripts/scan-mojibake-all.mjs` → **0 fichier** · scan U+FFFD → **0 fichier**.
> **Reste (non demandé)** : lot 1 de l'audit (`.git` = 3,31 GiB, `_git-purge.ps1` désormais dans `A:\SigilOS--menage-2026-09-20\lot2-menage\_menage-2026-09-13\`) · lot 2 (`public/uploads` 636 fichiers) · lot 3 (`public/game-data` 474 suivis malgré l'ignore) · rotation de ce fichier (111 Ko — règle à écrire) · unification des 2 `prometheus.yml`.

## ✅ Session 19/09/2026 (suite 6) — **`migrate deploy` figé : VRAIE cause trouvée (schema-engine Prisma 7 en boucle CPU) + migration réécrite** → branche `dev`
> **Demande user (verbatim)** : « jamais attendu aussi longtemps » (capture de `./scripts/deploy-cd.sh beta 5439f21` arrêté sur `Application des migrations Prisma...`), puis `debug.md` : 1ᵉʳ run interrompu au Ctrl+C, 2ᵉ run qui meurt en `P1002 Timed out trying to acquire a postgres advisory lock (72707369)`.
> **Vraie cause (reproduite et mesurée)** : le **schema-engine de Prisma 7 part en boucle CPU à 100 %** sur `prisma/migrations/20260919130000_add_dungeon_slug/migration.sql` quand les instructions sont **écrites sur plusieurs lignes**. Effet : **aucune** instruction exécutée (la colonne `Dungeon.slug` n'existait même pas) alors que l'enregistrement `_prisma_migrations` était déjà inséré → verrou advisory conservé → `migrate deploy` paraît figé à l'infini, et toute tentative suivante échoue en `P1002` après 10 s. Le CLI retéléchargé par `npx` (1-3 min, sortie redirigée dans un `mktemp`) explique seulement l'absence d'affichage, pas la panne.
> **Preuves (banc local : Docker + Postgres 15 jetable + CLI Prisma 7.10.0)** : contrôle = fichier committé ⇒ schema-engine à **98 % CPU**, migration jamais terminée ; bisection : `ALTER` seul ✅, `+UPDATE` ✅, `+CTE` ✅, `+SET NOT NULL` ✅, **le tout en « une instruction par ligne » ✅** (`nullable=NO`, index unique créé) ⇒ E/F/G/H (multi-lignes) ❌ vs A/B/C/D ✅. Slugs vérifiés sur de vraies données : `Comte Harebourg → comte-harebourg`, `Île des Wabbits → ile-des-wabbits`, `Ægir → aegir`, `Œuf de Pâques → oeuf-de-paques`, `  Minôtorôr--X  → minotoror-x` ✅.
> **Corrigé** : `migration.sql` réécrit (**une instruction par ligne**, commentaires ASCII, `translate()` au lieu de 27 `replace()` imbriqués — mêmes résultats, toujours aligné avec `src/lib/boss-slug.ts`) + **garde de non-régression** dans `tests/unit/boss-slug.test.ts` (toute ligne de code doit se terminer par `;`).
> **Confort de déploiement (2ᵉ sujet, indépendant)** : `Dockerfile` → stage **`prisma-cli`** copié dans `/opt/prisma-cli` (plus de retéléchargement npx à chaque deploy) ; `scripts/deploy-cd.sh` → helper **`compose_exec_streamed`** (sortie en direct, `exec -T` + `</dev/null`) + **`PRISMA_TIMEOUT`** (900 s → code `124` + message au lieu d'une attente infinie) + **repli npx automatique** ; `PRISMA_PIN=7.10.0` aligné sur `package.json`/`Dockerfile`.
> **Déblocage beta** (documenté dans `docs/MAINTENANCE.md`) : `restart app-beta` → `pg_terminate_backend` → `migrate resolve --rolled-back 20260919130000_add_dungeon_slug` → `git pull` → `./scripts/deploy-cd.sh beta`. Base **locale** : `DELETE FROM "_prisma_migrations" WHERE migration_name='20260919130000_add_dungeon_slug';` puis `migrate deploy` (SQL idempotent) — le checksum du fichier a changé.


## Chantier du jour (2026-09-08) — Module « Titans » (feat. de bout en bout)
> Branche `feat/slash-rework` — **non commité**. Mémo : `memo-2026-09-08-titans.md`.
- ✅ Modèle `Titan` + `UserTitanProgress` + `DjSearchMode.TITAN` + champs DJ (`titanId/titanName/questName/questUrl`) ; Admin God `TitanManager`, server actions `titan-admin-actions`/`titan-actions`, seed Gargandyas (**8062**, Osavora, dispo WE 19h→8h, 5 victoires max).
- ✅ `SuccesTitanTab` **parité fiche boss** : onglets **Stats & Sorts / Simulation (`SpellRangeGrid`) / Monstres de salle**.
- ✅ Résolution Gargandyas validée (id **8062**, ⚠️ homonyme 8069) · **icônes** = set **déjà officiel** (aucune intégration d'assets desktop, conforme règle « proposer puis valider ») · barre de vues Succès en **assets Dofus** + « Fiche Titans » à côté de « Fiches Boss ».
- ✅ DJ Titan : **taille FIXE = titan.maxMembers** (modale + `createDjPost` force) · **date/heure calquée sur `scheduleConfig`** (`DateTimePicker` allowedDaysOfWeek+hourRange, fenêtres nocturnes, verrou sélection) · cron `cleanup-inactive-posts` couvre TITAN · file d'attente déjà en place.
- ✅ UI /boss : bouton « Overlay en jeu » retiré des cartes + amber→`warning` · **overlay fix flèche retour** (deep-linked 1 seule fois + clear search).
- ✅ ~~`SpellRangeGrid.tsx` syntaxe JSX cassée~~ → **RÉSOLU (vérifié 09/09)** : `tsc` 0 sur le repo, fichier sain + prop `allowFreeCasterMove` ajoutée (bypass boss libre landing publique).
- ⚠️ Validation : mes fichiers **eslint 0 erreur** ; `tsc` vert.


## Chantier du jour (2026-09-04, suite) — Rush Sylvestre : refonte visuelle anti-slop + fil conducteur imagé
> Branche `feat/chantier-2026-09-04-cyber-rescan` — **rush non committé** (commit + PR `dev` à faire).
> Référence vs concurrent **Duffus** (capture) : consigne **inspiration, pas recopie** (100 % assets locaux).
- ✅ **Bug header ressources sidebar** (`RushChapterSidebar.tsx`) : titre écrasé → header 2 lignes, toggle compact, libellé Total/Encore selon mode, quantités `×`.
- ✅ **Fil conducteur** : ancres œufs 40px jalons liés + vignettes 32px chapitres (`RushTimelineClient.tsx`) ; strip `DofusProgressStrip` **branché puis retiré** (doublon + couverture `dofusId` trop faible — fichier conservé).
- ✅ **Panneau guilde** (`GuildStatusPanel.tsx`) : clic = filtre timeline + détail membres en ligne (top 8 + déplieur) ; modale vide supprimée ; **fix `%` jamais calculé**.
- ✅ **Hero diet** : 4 cartes KPI → 1 barre de statut ; boutons ghost unifiés ; **Signaler → icône** ; **Overlay → seul primaire** ; Niv.200 conservé.
- ✅ **Ambiance** : halo hero + glow œuf teintés au Dofus actif (CSS statique, 0 coût) + **particules** `GuideParticles` teintées + toggle Options persisté (défaut ON, reduced-motion OK).
- ✅ **Feedback overlay natif** (`RushOverlayFeedbackPanel.tsx`, nouveau) : le `Dialog` Radix portalait vers le document principal → formulaire `fixed` viewport overlay ; modale dashboard intacte.
- ✅ **Vérifs** : `tsc` 0 · `eslint` 0 erreur · 64/64 tests rush (`test:run` complet + `build` **non faits**).
- 🔴 **Reste n°1 (data, côté God)** : **backfill `dofusId`** (sélecteur existant) — débloque strip + panneau + ancres + vignettes. Puis check visuel + commit/PR.
- 📄 Mémo : `memo-2026-09-04-rush-refonte-visuelle.md` · Maquettes : `refonte-rush-sylvestre-2026.html`, `refonte-guide-sylvestre/09-refonte-ui-ux-2026.md`.


## PR / Branche
- **Session UX/sync** → branche **`feat/rush-sylvestre-ux-v2`** (créée depuis `origin/dev` = `b185e5e61`, après merge PR #586) → PR vers `dev`.
- Merge propre : aucun chevauchement de fichiers avec `dev` (seuls `package-lock.json` + `sync-actions.ts` différaient dans `origin/dev`).


## NEXT (priorités — à faire)

### 🛒 Marché — état au 14/09/2026 (3 PRs mergées : #657 · #659 · #660 · **+ debug beta en cours**) — **0 tâche de code ouverte au plan §21**
> **Vague de debug beta (5ᵉ tour)** — branche `fix/marche-debug-beta-14-09` : **3 constats fermés** (offre Discord/RBAC, ligne fantôme « +15975 Effet », icônes Mêlée/Distance + audit) ⇒ suite **1293/1293** · `tsc` 0 · `eslint` 0 erreur · **0 migration**. **Constats restants** (diagnostiqués, mesures dans `debug.md`) : `Réglages → Marché` (3 sélecteurs de rôles + panneau « Durées, plafonds & rappels » à retirer — le God est déjà global, `updateMany` cross-guild **mesuré**), timeline des offres sous la fiche (**emplacement à trancher par le user**), désistement depuis la fiche du dashboard, capacité légendaire (texte du pouvoir à sourcer), image OG (titre `EFFETS` recouvert, ligne horizontale, pied « Annonce #… / vendeur » à retirer).
> Plan maître §21 : **168 cases cochées** / **16 non cochées** = 4 DoD-ops (`S1.25`, `S1.34`, `S4.0e`, `S4.12`) + 3 historiques (`S0.7`, `S0.8`, `S4.13`) + **9 = V1.1 `S6`**. ⇒ code **clos** ; il reste des **ops**, **2 validations** et **1 PR docs** (celle-ci).
- ⏳ **Ops user (bloquant le gel §6)** : **déployer** `b9079d02b` (`sudo ./scripts/deploy-cd.sh beta <sha>`) · **crontab `market-expire`** (`docs/MAINTENANCE.md:65` ; sans elle : ni expiration, ni rappel J+7/J+15, ni retrait J+20, ni purge) · **DoD visuelle** dark/light/mobile + `/god/market` · **`S4.12`** (2 comptes Discord, double clic simultané) · **tags du salon forum** + association (`Réglages → Marché`) · **libellés `S1.25`** · bouton God **« Rattraper les effets natifs »** · (facultatif) bouton God **« Siphon items »** pour remplir `superTypeId` (**21 748 NULL** aujourd'hui ; le Marché tient déjà via le repli `typeId` → `typeName`).
- 🎨 **Assets A3 (à valider avant intégration)** : `uiassets_assets_*/auctionHouse{Equipments,Cosmetics,Resources,Consumables}.png` (familles de filtres) · `uidarkstone_assets_all/icon_slot_rune_forgemagie.png` + `uiassets_assets_2x/auctionHouseRunes.png` (bloc Forge) · `icons_assets_2x/rune.png` / `shop.png` (option). `kamas.png` / `hammer.png` = **déjà intégrés**.
- 🔎 **Résidu code (option)** : 4 `<Image>` sans repli d'erreur (`market-create-client.tsx:1141,1456,1550,1681`) → harmoniser sur `MarketItemIcon`.
- 📌 **Infra** : `T15` (CSS `MIME text/plain` = purge cache/CDN, **aucun code**) · `npm run seed:docs` / `build:seeds` sont **inutiles sur le VPS** (le `Dockerfile` les exécute au build, `docker-entrypoint.sh` joue les seeds au boot).
- 🚫 **Hors module** : V1.1 = **`WANTED` + matching seulement** (chantier séparé, sa PR, sa migration additive) — le reste de `S6` est **écarté**.
- 🔬 **Harnais réutilisables** : `_shot-fiche.mjs` (JWT Auth.js **forgé** + Playwright sur `next dev` : captures réelles dark/light/mobile) · `_probe-og-card.mjs` (carte OG + Sharp) · `_probe-marche-etat.mjs` (comptages en base) · `_probe-cosmetic-noise.mjs` (bruit `0 → 0`). ⚠️ Piège PowerShell : les dossiers `[guildId]` sont des **caractères génériques** ⇒ `-LiteralPath` obligatoire.

- ⚠️ **Prod/Infra** — `prisma migrate deploy` sur l'env **déployé** : en local c'est réglé (`resolve --applied` sur `20261009`-`20261013`). À refaire là-bas si même cause « already exists ».
- 🔴 **Ops** — Ménage `Songes_Pour_Les_Noobs/` (177 f, 8.4 Mo) → `git rm -r --cached`.
- 🟠 **Data** — 15 `unresolved` restants (Feuille de Salace, Mesure de poivre, Bière du Chabrulé, Poiskaille en Fricassée, Kamas, « L' », phrases « ou 1 x »…) : pas d'équivalent `name.fr[]` DofusDB → mapping manuel ou acceptation.
- 🟠 **Data** — Intégrer les 15 quêtes « Apprentissage » (Ordres) : hors des 374 séquences ; mapping `alignReq`/camp requis.
- 🟡 **UI** — Liens NOOBS/DOFUSDB → modale (aujourd'hui nouvel onglet).
- 🟡 **UI** — Responsive : auto-replier aussi la barre de chapitre / le bloc recherche sur très petite hauteur ; mode compact en fallback auto très petit.
- 🟡 **Perf** — Factoriser `getSequenceHelpers` par guide / agréger « qui peut aider » par chapitre.
- 🟣 **Tech** — Nettoyer `as any` (~70 admin, ~64 timeline) ; confirmer suppression `RushOverlayQuestPanel.tsx` (orphelin).


## Références clés
- 🎨 **SOURCE D'ICÔNES OFFICIELLE (permanente, tout module)** : `<Bureau>/dofus_assets` (~23 770 fichiers : icônes 1x/2x, items 2x, sorts, monstres, UI…). On peut y piocher des icônes pour n'importe quel module, **UNIQUEMENT après les avoir proposées au user ET validées** avant intégration.
- Backlog canonique : `docs/ROADMAP.md` (à lire en premier en mode plan).
- 🧭 **Amorce de session générique** : `docs/agents/session-amorce.md` (contexte + **checklist sécurité non négociable** + méthode de preuve + vérifs + DoD + **bloc à coller** §10) — **remplacée le 20/09/2026 par `AGENTS.md`** (racine), lu automatiquement par les assistants.
- 🛒 **Marché — amorce de la phase de debug** : `refonte-marche/AMORCE-DERNIERE-PHASE-DEBUG.md` (+ `AMORCE-DEBUG-A-COLLER.txt`).
- Demandes ouvertes + annotations : `docs/ROADMAP.md` (source unique).
- Plan maître #223 : `refonte-long-terme-discord-compatibilite/PLAN-MAITRE-RESILIENCE-DISCORD-LONG-TERME.md` + `sigilos-discord-resilience.md`.
- 🛰️ **Indépendance totale (fiches boss/monstres, simulation, icônes) — AMORCE PRÊTE (15/09/2026)** : `amorces/amorce-2026-09-15-independance-totale.md` — 6 lots (stale-while-offline → TTL par dataset → `local-only` strict → icônes siphonnées + cache durci → siphon **tous les monstres** → fraîcheur visible), 6 décisions à trancher (D1→D6, recos incluses), **critère « test de panne »** (`DOFENSIVE_OFFLINE=1`/`DOFUSDB_OFFLINE=1` : zéro spinner, zéro icône cassée, zéro appel sortant). Contexte : local-first **pas** local-only ⇒ une ligne périmée (> 24 h) déclenche une bascule live silencieuse (c'est l'incident Dofensive down). `refonte-siphon/PLAN-REFONTE-SIPHON.md` §5.3 (limiteur anti-ban) s'applique aux lots 4-5.
- Dernier mémo détaillé : `memo-2026-09-14-marche-d49-q15.md` (D49 + Q15) · `memo-2026-09-14-marche-embeds-cartes.md` (#657/#659) · archives : `memo-2026-10-06-chantier-fiche-boss.md`.


## Vérifications globales habituelles
`npx tsc --noEmit` 0 · `npm run test:run` vert · `npm run build` OK · pas de `console.*` en prod.

