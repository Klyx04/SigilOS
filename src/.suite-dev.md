Plan complet — Cartes de quête
text
1. Supprimer la colonne verticale Noobs/DB/Dofus à droite de la carte.

2. Padding conditionnel :
   - carte active : p-2.5
   - carte simple : p-2

3. Icône Dofus réduite (w-5/h-5 au lieu de w-10/h-10), intégrée
   directement dans la ligne de tags, au même niveau visuel que
   "Combat solo", "Songes", etc. Pas de récompense isolée à droite.

4. DB et Noobs conservent leur texte complet ("DofusDB" et "Noobs"),
   car ce sont deux sites différents avec des explications différentes
   de la même quête. Mais en version compacte :
   - text-[8px] au lieu de text-[9px]
   - padding px-1.5 py-1 au lieu de px-2 py-1
   - favicon w-3 h-3 au lieu de w-3.5 h-3.5
   - ExternalLink réduit à w-2 h-2 ou supprimé
   - Alignés horizontalement côte à côte (gap-1.5), sous la ligne de
     tags, jamais en colonne verticale à droite.

5. Le badge "À FAIRE MAINTENANT" doit être sur la même ligne que le
   titre de la quête (pas au-dessus, en pleine largeur), pour ne pas
   ajouter de hauteur inutile à la carte.

6. Agrandir la zone cliquable du bouton de validation :
   - minimum 36x36px sur desktop
   - minimum 44x44px sur mobile
   - conserver l'icône et la logique actuelles, ne changer que la
     zone interactive (padding autour de l'icône).
Plan complet — Timeline et espacements
text
7. Limiter la largeur du conteneur qui wrap timelineItems avec
   max-w-5xl mx-auto, pour éviter le vide global à droite de l'écran
   sur les grands moniteurs, indépendamment du contenu des cartes.

8. space-y 2 → 1.5 dans MilestoneRow (contenu déplié).

9. space-y 3 → 2 dans ChapterBlock (liste des milestones).
Points supplémentaires repérés en dehors des blocs de quête
text
10. BUG SUSPECT — positionnement du tooltip ContextualHelp :
    Le popover utilise actuellement "left-1/2 -translate-x-1/2" dans
    certaines versions du fichier mais "left-12 -translate-x-12" dans
    d'autres. Le second cas semble être une erreur de conversion
    (12px au lieu de 50%). Vérifier la valeur actuelle dans le fichier
    de production : si c'est "left-12 -translate-x-12", corriger en
    "left-1/2 -translate-x-1/2" pour que le tooltip reste centré sous
    l'icône d'aide, quelle que soit sa position dans la page.

11. VÉRIFICATION — le bouton central de la navigation flottante
    (icône MapPin dans la pilule verticale à droite de l'écran) doit
    appeler exactement la même fonction que le bouton "REPRENDRE MON
    ÉTAPE" de la barre d'actions (resumeRush), pas une fonction séparée
    comme scrollToActive qui ne viderait pas la recherche en cours.
    Confirmer que les deux boutons appellent bien resumeRush().

12. VÉRIFICATION — le sticky top-20 de la barre de recherche/actions :
    confirmer que 20 (5rem/80px) correspond bien à la hauteur réelle
    du header au-dessus, pour éviter que la barre de recherche ne
    passe sous le header ou laisse un espace visible au scroll.
Ordre d'application recommandé
Donne ce plan à DeepSeek dans cet ordre, en lui demandant un diff complet avant application :

text
Applique ces 12 points dans cet ordre, sans toucher à autre chose :
- Points 1 à 6 : uniquement dans SequenceRow
- Points 7 à 9 : uniquement dans le conteneur timeline, MilestoneRow, ChapterBlock
- Points 10 à 12 : vérifications ciblées, corrige seulement si le
  problème est confirmé présent dans le fichier actuel

Donne-moi le diff complet avant application, puis lance TypeScript
et ESLint pour confirmer qu'aucune régression n'est introduite.
Les points 10 à 12 ne sont pas urgents comme le reste — ce sont des vérifications, pas des certitudes de bug — mais ça vaut la peine de les faire confirmer/corriger pendant qu'il est déjà dans le fichier, plutôt que de rouvrir un chantier séparé plus tard.